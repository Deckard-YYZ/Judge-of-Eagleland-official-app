# 桌面案件模拟游戏架构扩展
## 多语言边界、内容输入交互点与固定声音旁白

**文档版本：** 1.1  
**更新日期：** 2026-09-18  
**状态：** ActionInput 文字与 sherpa-onnx 语音输入已接入；TTS 为本版新增设计，尚未实施验收  
**关联文档：** `Desktop_Case_Game_Architecture_v1.0.md`、`Desktop_Case_Game_Localization_Implementation.md`、`Desktop_Voice_Sherpa_Implementation.md`  
**替代文档：** `Desktop_Case_Game_Architecture_Extension_ActionInput_v1.0.md`；本文为可完整阅读的修订版，不是仅含差异的补丁  
**技术基线：** React + TypeScript + Vite + Tauri 2 + SQLite  
**核对代码基线：** `Deckard-YYZ/Judge-of-Eagleland-official-app`，提交 `169eee41ab2944cb577fa008952b4c5471e8c74b`。此前 ActionInput / KWS 架构核对使用 `332e1c6`；本次确认 main 已新增语音测试工具提交。代码依据见附录 D。

> 本文不替换已经完成的 Game Core、内容校验、存储和 UI。`actionInput` 仍是**内容走到某个位置时等待玩家给出指定输入的小交互点**，不是另一条玩法。
>
> **本版明确优先使用 sherpa-onnx 作为语音能力供应实现：输入采用已接入的本地 KWS，输出新增固定声音 TTS。** 二者分别服务于“识别玩家输入”和“朗读内容”，共用技术依赖不等于共用模型、会话或游戏规则。
>
> TTS 是当前内容的可选旁白表现：默认给 `text` / `actionInput` 步骤附加 `narration`，不新增挑战队列、TTS 游戏命令、音频进度存档或通用音频编排框架。
>
> “必须”表示架构约束；“首版默认”是本版确定的实现默认；“条件扩展”仅在明确需要时实施。类型片段说明边界，正式工程仍以现有 Zod Schema 和推导类型为源。文中保留的 ActionInput 基础章节用于维护已实现约束，不是要求重新开发。

### 本版修订摘要

| 修订项 | v1.0 | v1.1 |
| --- | --- | --- |
| 语音首选 | whisper.cpp 转写，sherpa-onnx 为备选 | **sherpa-onnx 本地 KWS 为当前首选与已接入实现**；Whisper 仅保留历史参考 |
| 语音归一化 | 默认转写后调用 Text Matcher | 当前语种的同源词典生成 KWS 配置，整轮归一化为 ActionId / unknown；不强制转写 |
| 输入契约 | 以 SpeechToText 示例说明 | 沿用实际 `VoiceInputService` / `VoiceInferenceBackend`；Core 命令不变 |
| 已实现状态 | ActionInput / 检查点待开发 | 文字、KWS 与 v3 存档作为维护基线；真实识别效果以人工记录为准 |
| 朗读 | 未纳入 | 新增可选 narration、固定逻辑音色、TTS 服务与播放生命周期 |
| 收音协调 | 原则上避免媒体回声 | 明确朗读与收音互斥、实际停止播放后才开麦、迟到合成不得复播 |
| 版本 | ActionInput 引入 save/content v3 | **TTS 不升级 save v3**；本版建议仅将含旁白字段的新内容升为 content v4 |

本次文档修订未修改仓库代码、未运行项目测试、未启用音频设备。实施记录中的通过结果是项目记录，不是本次重新验收。

---

## 目录

1. 扩展范围与已确认语义
2. 现有架构基线与覆盖关系
3. 总体结构与最小改动集
4. 多语言与识别字典的边界
5. 内容中的 `actionInput` 模型
6. 触发时机与规律性出现
7. 状态归属与最小恢复检查点
8. 游戏命令、判定与原子提交
9. 输入轮次、取消与防止重复处罚
10. 文字匹配规则
11. 语音识别接入
12. 视觉动作识别接入
13. UI、内容投影与无障碍替代
14. 存档、内容版本与迁移
15. 内容与字典校验
16. 平台接入、资源与隐私
17. 目录、接口与实施分工
18. 测试、联调与验收
19. 实施顺序与完成标准
20. 技术参考与优先考察项
21. TTS 旁白：范围、内容配置与语言
22. 旁白服务、固定音色与平台适配
23. 旁白播放、收音互斥与生命周期
24. TTS 内容版本、资源、缓存与诊断
25. TTS 实施步骤与验收矩阵

附录 A：完整交互内容片段与双语示例  
附录 B：最小检查点算法参考  
附录 C：实施决策与禁止扩大的范围  
附录 D：代码依据及官方参考索引  
附录 E：带旁白的双语内容与纯投影参考

---

## 1. 扩展范围与已确认语义

### 1.1 交互点的定义

一个输入交互点属于当前正在呈现的内容。内容到达该步骤时，要求玩家完成一个指定动作，例如 `salute`。文字、语音、摄像头是完成同一要求的不同输入方式，而不是三个分别计分的玩法。

```text
前面的内容 / 播片
       ↓
actionInput：要求敬礼
       ↓
文字、语音或摄像头得到 ActionId / unknown
       ↓
正确 → 后面的内容
明确错误 → 可扣状态，继续等待
未知 → 不扣状态，继续等待
```

交互可以在固定内容位置出现，也可以按游戏进度规律出现。是否出现、何时出现，由内容和现有进度规则决定。识别器不决定剧情何时发生。

### 1.2 三种判定结果

| 结果 | 定义 | 游戏行为 |
| --- | --- | --- |
| 目标动作 | 识别得到的稳定动作 ID 等于当前目标 ID | 当前交互点通过，继续内容 |
| 其他已知动作 | 识别得到另一个受支持的动作 ID | 应用本步骤配置的错误效果，仍停在这里 |
| `unknown` | 没匹配到、证据不足、存在歧义或不属于支持的动作库 | 不修改游戏状态，继续等待 |

目标正确之前没有失败次数上限，也没有“达到次数就自动通过”的隐含规则。错误是否扣属性、扣多少、是否设置 Flag，由该内容步骤明确配置；未配置即没有惩罚。

**权限拒绝、设备不存在、识别服务失败不是错误动作。** 它们属于技术状态，不得施加游戏惩罚。

### 1.3 首版默认

| 事项 | 本文默认 |
| --- | --- |
| 成功后果 | 只通过当前输入点并继续内容，不增加奖励体系 |
| 输入方式 | 同一时间只启用一种；文字始终作为可完成游戏的替代路径 |
| 识别语言 | 跟随玩家当前选定的语种；一轮输入期间固定该语种 |
| 字典加载 | 普通应用配置模块，按语种索引；不新建翻译系统或远程 Provider |
| 输入时长 | 可设录音/采样的技术上限；到时无确定结果是 unknown，不是游戏失败 |
| 原始数据 | 不写入存档；默认不上传、不长期保存音视频和转写文本 |
| 正确或明确错误 | 经已有 GameSession 提交，保存后才发布游戏状态 |
| unknown、取消、设备错误 | 仅更新局部输入 UI，不增加存档 revision |
| 普通内容步骤 | 保持本地播放位置；不为了此功能把每个文字/视频步骤都写入存档 |
| 游戏逻辑 | 不接触媒体设备、模型 SDK、DOM、语言文案或识别置信度 |
| 语音输入供应 | 当前优先 sherpa-onnx KWS；不并行维护 Whisper 后端 |
| 旁白输出供应 | 优先 sherpa-onnx TTS，另选 TTS 模型；不复用 KWS 模型 |
| 旁白触发 | 指定内容位置首次进入时播放，可停止/重播；失败保留文字 |
| 旁白持久化 | 不保存播放状态、秒数或“听过”标记；不因 TTS 升级存档 Schema |

语音输入当前优先采用已接入的 sherpa-onnx 本地 KWS，不再要求先试 Whisper；固定声音朗读也优先使用 sherpa-onnx TTS。它们是服务供应实现的选择，不改变三分支判定、统一提交和存档事实。模型质量、占用与打包效果仍须独立验收。[C11][C14]

### 1.4 明确不做

不做独立 `ChallengeSession`、`ChallengeRepository`、挑战排行榜、挑战结果页、成功奖励系统、任务分支图、全局事件总线或事件溯源。

不默认增加永久错误次数、完整尝试历史、无限增长的防重日志；不将案件伪装成挑战，也不将挑战伪装成一个案件。

不为这次扩展更换状态管理方案、重新拆分 SQLite 表、引入 Unity/Godot，或重做已经闭环的本地化。

TTS 首版不做自由对话、声音克隆、实时变声、语音助手、自动云端回退、双向边播边听、多优先级播报队列或逐字字幕时间轴。固定预置声音即可。

---

## 2. 现有架构基线与覆盖关系

### 2.1 已落实且应保留的边界

`GameSession` 协调纯规则计算和 revision 条件提交，SQLite 保存整局 JSON，React 只展示已提交状态。多语言保持规则与表现分离；历史保存稳定 ID 与实际数值变化。[C1][C2][C3][C4][C5]

ActionInput 已接入现有 Story：正确输入推进检查点，错误输入提交允许的效果并停在当前步骤，unknown 不提交。语音入口输出同一个 RecognizedAction，沿同一 `submitStoryInput` 命令进入 Core；本版不重建这些路径。[C6][C12][C13]

| 已有机制 | 本版处理 |
| --- | --- |
| pendingStoryIds / completedStoryIds | 继续安排内容，不增加输入或旁白平行队列 |
| GameStoryStep 的 actionInput 分支 | 保留；仅给 text/actionInput 增加可选旁白配置 |
| GameSession.dispatch() | 只提交已有游戏事实；旁白状态不进入该入口 |
| GameState.storyCheckpoint | 保留 save v3 的单检查点；不保存音频播放位置 |
| applyGameEffects / storyInput 反馈来源 | 文字和语音继续复用；TTS 不应用游戏效果 |
| GameSessionView / GameContentView | 保留只读投影；扩展朗读文本、实际内容语种与逻辑音色 |
| VoiceInputService / VoiceInferenceBackend | 保留供应无关的 RecognizedAction 输出；当前供应为 sherpa-onnx KWS |
| 现有 i18n / 诊断 | 补旁白按钮、稳定错误码和有界诊断；不新建翻译或日志系统 |

### 2.2 当前进度与本次新增边界

当前内容已支持 actionInput，StoryPlayer 已按 storyCheckpoint 恢复；普通文字/视频播放位置仍是局部状态。当前语音原生实现是 sherpa-onnx KeywordSpotter，不是全文转写器。[C1][C6][C11][C12]

项目的语音实施记录描述了选定语种的同源关键词生成、单轮聚合、取消与迟到结果隔离，并将真实短口令、噪声、回声和目标机器验收列为待测。本次确认的最新提交增加语音测试工具，但工具存在不等于识别质量已达标。[C11][C15]

**这次真正新增的是旁白输出服务及其内容/UI/平台接入。** 语音供应实现从 Whisper 优先改为 sherpa-onnx，不要求新增或改写 GameCommand。TTS 的合成结果、播放结束和失败也不构成新的游戏事实。

### 2.3 对原 v1.0 文档的增量覆盖

| 原文档内容 | 本文的覆盖规则 |
| --- | --- |
| 历史结果快照包含显示内容的早期描述 | 以已经实施的 v2 事实存档为准，不恢复旧文案快照 |
| Story 步骤只有文字、视频、特效 | 增加 `actionInput`，不改变案件选项图 |
| 未完成 Story 一律从头恢复 | 纯演出不变；有输入事实时，从最近提交的输入检查点恢复 |
| `completeStory` 只检查队首 | 还必须检查该 Story 是否仍有未通过的输入点 |
| 属性反馈依附案件 | 增加内容输入来源，不创建虚假 caseId |
| 只有案件结算修改属性 | 内容输入也可通过同一纯效果函数修改允许的属性与 Flag；旁白不修改 |
| 扩展 v1.0 先试 Whisper / 默认语音转写 | 以第 11 节 sherpa-onnx KWS 路径和当前代码为准，不强制增加 ASR 中间步骤 |
| 扩展 v1.0 未包含旁白 | 以第 21–25 节的可选 narration 为增量；不升级 save v3 |
| 结局选择顺序 | 默认不改；需要输入影响尚未确定的结局时另作内容时序约定，见第 8.8 节 |

现有通过 `useSyncExternalStore` 订阅的实现可直接保留，不需要重新引入原文档曾作为可选方案提到的 Zustand。[C3][C4]

---

## 3. 总体结构与最小改动集

### 3.1 扩展后的运行链路

```text
内容到达 text / actionInput
       │
       ├── 可选 narration → 当前内容语言文本 → sherpa-onnx TTS → 音频播放
       │                                        （不提交游戏状态）
       │
       └── actionInput → 当前选择的一种输入
                            ├── 文字 → 按语种 Text Matcher
                            ├── 录音 → 按语种同源关键词 → sherpa-onnx KWS
                            └── 摄像头 → 关键点 / 动作识别（后续）
                                               ↓
                                      RecognizedAction
                                      ┌────────┴────────┐
                                   unknown            known
                                   局部提示              ↓
                                              GameSession.dispatch
                                                      ↓
                                         Core 比较目标 / 计算效果
                                                      ↓
                                      整局状态一次保存 → 发布 / 反馈
```

语音输入与旁白输出在设备层协调：**游戏正在说话时不收音；录音轮次有效期间不启动游戏口令播报。** 当前文本、步骤与选择语言各自的边界见第 4、21、23 节。

### 3.2 最少新增的概念

ActionInput 的基础概念保持不变：稳定 ActionId、按语种词典、actionInput 步骤、已知动作命令、单检查点与输入入口。它们已经形成实现基线。

TTS 只补：可选 narration 配置、供应无关的旁白服务、sherpa TTS 平台实现、局部播放控制与一个收音/播报互斥约定。不要按每一项建立独立框架。

“输入控制器”可以是一个 hook 加几个无框架函数，不需要复杂 class、依赖注入容器或任务运行时。识别实现通过构造参数或 props 装配，沿用当前项目做法。

### 3.3 必须保持的依赖方向

| 层 | 负责 | 不负责 |
| --- | --- | --- |
| `shared` | 无框架的语言与动作 ID 类型 | 翻译、识别、游戏状态 |
| `content` | 目标动作、错误效果、步骤顺序、旁白配置、显示内容与引用校验 | 相机、录音、模型实例、玩家状态 |
| `game` | 已知动作与目标比较、效果、检查点、内容完成 | 字符串识别、模型、权限 |
| `application` | 统一提交及必要的输入提交桥接 | 绕过 Core 修改属性 |
| `input` | 字典 Matcher、识别结果归一化、适配器契约 | 保存数据库、决定惩罚 |
| `ui` | 设备入口、输入提示、局部轮次、展示已提交结果 | 直接结算状态、修改案件 |
| `platform` | 媒体设备、Worker/原生推理、音频播放、必要的 Tauri 能力 | 案件或输入点业务规则 |
| 旁白服务（可位于 platform + UI 装配） | 合成、播放、取消、当前音色与技术状态 | 判定动作、修改属性、推进 Core、持久化“听过” |

不要求新增一个 `input/` 目录就移动所有已有代码。只要依赖方向清楚，可按项目当前组织方式合并文件。

---

## 4. 多语言与识别字典的边界

### 4.1 既有多语言主链路不变

UI 自身文字继续走 `I18nProvider`；案件、剧情等显示内容继续由 `GameSessionView` 加载当前语言内容并投影；规则与存档不保存 locale 或语言文案。现有实现已将这三部分分离。[C1][C4][C8]

新增交互说明，例如“请向监察官敬礼”，属于内容语言包。新增按钮，例如“开始录音”“再试一次”“改用文字”，属于 UI 消息目录。旁白复用该内容的 blocks，或使用同一步骤的 narrationText；“停止朗读”“重播”等仍属于 UI 消息目录。

### 4.2 识别字典就是普通的按语种配置

```ts
// src/shared/action.ts：与 shared/locale.ts 一样保持无框架。
export const ACTION_IDS = ["salute", "wave"] as const;
export type ActionId = (typeof ACTION_IDS)[number];

// 以下类型中的 AppLocale 直接复用现有 shared/locale.ts。
export type ActionLexicon = Readonly<
  Partial<Record<ActionId, readonly string[]>>
>;

export const actionLexicons = {
  "zh-CN": {
    salute: ["敬礼", "行礼"],
    wave: ["挥手"],
  },
  "en-US": {
    salute: ["salute"],
    wave: ["wave"],
  },
} as const satisfies Readonly<Record<AppLocale, ActionLexicon>>;
```

这不是 UI 翻译目录。Matcher 直接读取 `actionLexicons[inputLocale]`，不调用 `t()`，不读取 `GameContentView`，也不需要专门的 `RecognitionI18nProvider`。

`salute` 是稳定的游戏动作 ID，不是英文显示文案。替换它会影响规则引用，不能作为普通翻译修改。新增 `bow / kneel` 等 ID 前，应明确对应的匹配词和实际可用的识别能力，不把示例列表当作已支持功能。

词典继续随应用代码发布。现有 sherpa KWS 关键词/音素配置从此词典生成，不手工维护第二份语音动作表。Matcher 直接读字符串别名，KWS 读生成结果；共享的是动作语义和词典源，不要求执行同一个匹配函数。[C11]

未来确需随内容包更新，再把数据来源放到现有内容加载边界中；首版不建设独立远程词库系统。

### 4.3 词典与显示内容不互相推导

不能自动把显示名称当作唯一可接受词，也不能把识别别名全部展示成内容标题。译者改写剧情句子不应改变规则的目标 ID；开发者增加词典别名也不应修改存档。

词典的校验是“动作引用、别名冲突、交付能力”校验，不是通常的文案 key 一一对应校验。不同语种可以有不同数量的别名。对于当前交付内容，所支持语言必须存在可用的文字完成路径。

### 4.4 选择语言与一轮输入

首版只使用已有语言选择入口，不再增加第二套设置页面。每次开始录音、开始视觉采样或提交文字时，捕获当前选定语种。

| 事件 | 行为 |
| --- | --- |
| 切换界面语言，尚未开始输入 | 下一轮直接使用新语种字典 |
| 录音或识别中切换语言 | 取消本轮，丢弃迟到结果，下一轮使用新语种 |
| 已经提交 known，正在保存时切换语言 | 已解析的 ActionId 命令继续提交，不重新识别；完成后用新语言显示 |
| 内容语言回退 | 只影响显示，不静默修改玩家选择的识别语种 |
| 当前语种的模型不可用 | 显示可翻译的能力错误，允许文字输入；不自动使用另一语种猜测 |

取消尚未提交的媒体任务不能撤销已经进入 GameSession 的写入。两者需要区分。

### 4.5 视觉动作与语种

摄像头通常输出相同语义的动作 ID，不需要翻译关键点。确有不同语种对应不同动作约定时，可用 `gestureProfiles[inputLocale]` 选择该语种的识别配置；仍然是普通配置选择，不进入 UI 本地化管线。

Core 始终只收到 `salute / wave / ...`。设备状态、模型支持语种、词典版本都不写进游戏事实。

### 4.6 旁白语言与输入语言的不同来源

输入词典/KWS 继续使用玩家**选定语种**；剧情旁白使用 GameContentView 中**实际成功加载的内容语种**。英文内容回退成中文时，朗读中文而不偷偷改变玩家的英语输入词典。

TTS 的发音词典、数字读法规则与 actionLexicons 不是同一份数据：前者解决“怎么读文本”，后者解决“哪些玩家输入映射为动作”。不能为修正旁白人名发音而改动玩家动作词典。详见第 21.5 节。

---

## 5. 内容中的 `actionInput` 模型

### 5.1 最小规则结构

以下为已接入的 v3 输入分支基线。TTS 只在 v4 给该分支增加可选 narration，见第 21 节；不重新设计输入规则：

```ts
export type GameEffects = Readonly<{
  attributeDeltas: Readonly<Record<string, number>>;
  setFlags: Readonly<Record<string, boolean>>;
}>;

export type ActionInputStep = Readonly<{
  id: string;
  type: "actionInput";
  targetActionId: ActionId;
  wrongEffects?: GameEffects;
}>;
```

`GameEffects` 的正式 Schema 复用当前效果结构与整数限制，不另建一套数值体系。省略 `wrongEffects` 表示错误时没有数值/Flag 后果，但仍不能通过。

首版不加 `onSuccess`：成功本身就是通过当前步骤。以后明确需要成功改 Flag，可按相同效果结构增加字段，不要预建通用事件脚本。

### 5.2 一个内容序列

```json
{
  "skippable": false,
  "steps": [
    { "id": "arrival", "type": "text" },
    {
      "id": "request_salute",
      "type": "actionInput",
      "targetActionId": "salute",
      "wrongEffects": {
        "attributeDeltas": { "authority": -2 },
        "setFlags": {}
      }
    },
    { "id": "acknowledgement", "type": "text" }
  ]
}
```

这仍然是一个 Story，不需要把输入单独包装成另一种全局 `kind: challenge`，也不需要新建 ChallengeDefinition 表。

### 5.3 表现结构

现有语言包按 Story ID 和 Step ID 提供 `blocks`，输入步骤复用它存放交互说明。[C1]

以下为输入表现的基础形状；v4 在相同分支增加可选 ContentNarrationView，不让模型或错误效果进入 UI：

```ts
export type ActionInputStepView = Readonly<{
  id: string;
  type: "actionInput";
  targetActionId: ActionId;
  blocks: readonly Readonly<TextBlock>[];
}>;
```

`targetActionId` 可用于判断某种输入方式是否可用、选择示例图等，不授权 UI 判定成功。`wrongEffects` 不穿过内容表现边界；是否扣状态由 Core 读取规则决定。

显示某个动作名称时，优先直接使用内容说明或现有 UI 文案；不要仅为两个动作强制加入第二套动作名称翻译目录。确需公共名称时，再在现有消息目录增加少量 key。

### 5.4 不修改案件结构

`actionInput` 不放入 `ChoiceTarget`，不当作 `resolution`，不在 `CaseProgress.history` 中写入识别结果。识别器不得选择案件选项或提交最终判决。

内容可安排在案件之间、案件处理前后或现有全局剧情位置。未来明确要求在正文某一段中间显示时，可以让另一内容宿主使用同一个输入组件与规则入口，但不需要现在增加任意位置嵌入系统。

### 5.5 跳过与完成约束

首版默认：**包含 actionInput 的 Story 整体 `skippable: false`。** Validator 拒绝相反配置，避免全局“跳过剧情”直接绕过输入要求。

纯视频的失败替代文本仍可使用；技术故障不等于输入成功。改用文字、说出同义词或使用支持的另一输入方式，都是完成同一个输入要求，而不是跳过。

---

## 6. 触发时机与规律性出现

### 6.1 固定内容位置

直接把输入步骤写在所需 Story 的指定位置，继续由现有 `storyRules` 或 `initial.storyIds` 安排 Story。页面 mount、组件 render、摄像头启动都不能自行触发新的游戏交互事实。

例如“完成第一个案件后，出现监察官来访并要求敬礼”，使用现有 `caseResolved` 条件将对应 Story 入队即可。该 Story 内的文字、输入、后续文字按原数组顺序显示。

### 6.2 有限游戏流程中的规律出现

现有剧情队列按 Story ID 去重，已完成的同一个 Story 不会再次入队。这个行为保留，不应简单删除完成记录来实现循环。[C9]

对于首版有确定长度的流程，例如“每处理三案检查一次，共九案”，推荐仍用现有规则表达：

```text
完成案件数达到 3 → inspection_003
完成案件数达到 6 → inspection_006
完成案件数达到 9 → inspection_009
```

每个出现位置有不同 Story ID，内部可以使用相同 `targetActionId: salute` 和相同模板结构。三次出现不是三个独立玩法，只是三个内容位置。

```json
[
  {
    "id": "inspection_at_003",
    "when": { "all": [{ "type": "resolvedCountAtLeast", "count": 3 }] },
    "storyId": "inspection_003",
    "order": 300
  },
  {
    "id": "inspection_at_006",
    "when": { "all": [{ "type": "resolvedCountAtLeast", "count": 6 }] },
    "storyId": "inspection_006",
    "order": 600
  }
]
```

规则与内容可手写；数量多时用一个简单的构建脚本展开模板和唯一 ID。脚本产物仍是现有 `game.json` 与语言包，不需要建设运行时调度器或完整内容编译器。

### 6.3 模板、动作与出现位置不是同一个 ID

| 概念 | 示例 | 是否代表一次独立出现 |
| --- | --- | --- |
| 动作 ID | `salute` | 否，可在任意内容中重复使用 |
| 内容模板 | “监察检查” | 否，可以复用制作结构 |
| Story ID | `inspection_006` | 首版代表这一局的一个确定内容单元 |
| Step ID | `request_salute` | 在所属 Story 内唯一 |
| 本次输入位置 | `inspection_006 / request_salute` | 是，正确通过只影响这个位置 |

不能保存全局 `saluteCompleted = true` 并据此让后续所有敬礼要求自动通过。

### 6.4 真正无限重复或现实时间周期：条件扩展

如果产品后来明确需要“无限游戏里每三案一次，复用同一个 Story 定义”，才给**既有内容队列**增加出现实例，例如 `occurrenceId + storyId`，并用单调游戏进度序号防重复。检查点届时引用 occurrenceId，而不是建立独立挑战队列。

如果需要“游戏保持前台时每隔五分钟出现”，必须先确定计时口径、暂停/最小化、退出恢复、播片中是否延后、错过周期是否补发。不能直接在 React 内放 `setInterval` 并把每次回调当作内容已触发。本文不把这类未确定的现实时间调度加入首版。

有限进度中的周期性内容已由第 6.2 节覆盖；无限实例化与现实时间调度不是首版必做项。

---

## 7. 状态归属与最小恢复检查点

### 7.1 哪些数据放在哪里

| 数据 | 所有者 | 是否持久化到 GameState |
| --- | --- | --- |
| 当前 Story 队首、完成记录 | 现有内容流程 | 是，保持原字段 |
| 属性与 Flag 的实际值 | Game Core | 是 |
| 已通过输入点之后的恢复位置 | 内容恢复检查点 | 是 |
| 目标动作、错误效果 | 内容规则 | 否，随绑定内容版本读取 |
| 当前显示语言、输入语言选择 | App 设置 / 本轮输入快照 | 否 |
| 当前输入方式、录音提示、相机预览 | 输入 UI | 否 |
| 当前本地文字/视频步骤、播放秒数 | StoryPlayer | 否 |
| 当前 narration 请求、自动播过标记、播放状态 | 旁白服务 / 所属 UI | 否 |
| 合成音频、音频缓存 | 平台层；缓存可删除重建 | 否 |
| 本轮是否已消费结果、取消令牌 | 输入控制器 | 否 |
| 视频帧、音频、临时转写、关键点 | 识别适配器 | 否 |
| 错误总次数、历史每轮输入 | 首版不建模 | 不新增 |

### 7.2 已有的单检查点字段

当前队列一次只处理一个 Story，因此不需要按所有交互点建立进度表。现有 save v3 已采用下面的单检查点契约；本版 TTS 不再增加检查点字段：[C10][C12]

```ts
export type StoryCheckpoint = Readonly<{
  storyId: string;
  resumeStepId: string;
}>;

// 在现有 GameState 上增量增加，其他字段不变。
// storyCheckpoint: StoryCheckpoint | null
```

含义：**重新进入队首 Story 时，从 resumeStepId 开始显示；它之前的输入点已经通过。**

`null` 表示该 Story 尚无已提交检查点，从它的第一步恢复。队首 Story 仍由 `pendingStoryIds[0]` 决定；检查点中的 storyId 是归属校验，不是第二个可独立切换的“当前 Story”。

它不是实时播放游标，不跟随每次文字“继续”、视频进度或 CSS 动画变化。

### 7.3 首版的检查点更新规则

| 操作 | 保存行为 |
| --- | --- |
| 浏览普通文字/视频/特效步骤 | 不新增提交，仍是本地播放位置 |
| 到达输入点，尚未输入 | 不单独提交“开始挑战”命令 |
| unknown 或取消 | 不提交；退出后可重播最近检查点以来的普通内容 |
| 明确错误 | 一次保存惩罚及 `resumeStepId = 当前输入步骤` |
| 正确输入，后面还有步骤 | 一次保存 `resumeStepId = 紧接着的下一步骤` |
| 正确输入且已经是 Story 最后一步 | 同一次保存内完成 Story，移出队首并清空检查点 |
| 普通尾部内容完成 | `completeStory` 保存完成记录并清空检查点 |
| 队首切换为另一个 Story | 检查点必须清空，新 Story 从自身起点开始 |

选择“错误后从当前输入点恢复”，是为了不在已扣属性后还迫使玩家重看同一段前置内容。unknown 没有事实变化，不为它增加存档频率。

### 7.4 例子：成功之后不能重新要求输入

```text
步骤 A：文字
步骤 B：输入敬礼
步骤 C：视频
步骤 D：文字
```

在 B 正确输入后提交 `resumeStepId = C`。即使程序随后在 C 播片时退出，再进入也从 C 开始，不回到 B。

在 B 错误输入后，提交属性变化和 `resumeStepId = B`。再打开时仍在 B，已扣的值从存档读取，不执行“恢复时再次扣除”。

在 B 只有 unknown 时未产生提交，重启可回到 A。这是首版允许的重播范围，不代表玩家已完成事实丢失。

### 7.5 同一 Story 有多个输入点

```text
A 文字 → B 敬礼 → C 文字 → D 挥手 → E 文字
```

B 成功之后检查点到 C。D 发生错误后检查点到 D。重启时只要求 D，不重复 B。

规则层不能仅判断“该 Story 包含输入点”，必须找到**检查点及其后面的第一个 actionInput**，并只允许提交给这个输入位置。旧 B 回调不得作用于 D，即使两个输入点的目标恰好相同。

### 7.6 完成 Story 的规则

`completeStory(storyId)` 除原有校验外，必须确认检查点之后已经没有未通过的 actionInput。

检查点为 null 时从第一步开始扫描；检查点不为空时从 resumeStepId 开始扫描。若仍有输入点，返回 `STORY_INPUT_REQUIRED`，不能因 UI 发了“继续”或“跳过”而直接完成。

这不要求 Core 验证玩家是否真的看完每一段普通文字或视频。Core 保护的是不能跳过会产生游戏事实的输入要求，不是实现播放防作弊。

### 7.7 检查点不变量

必须检查：非空检查点属于当前队首；resumeStepId 在该 Story 中存在；已经完成的 Story 不能仍持有检查点；队列为空或本局 ended 时检查点必须为空。

有效写入只能把位置推进到当前输入点或其下一步，不回退到已经通过的输入点。保存中的检查点仍然是旧已提交值，候选位置不提前发布。

非法检查点是存档/内容兼容错误，不能静默回到故事起点。否则可能重复要求已经完成的输入。旧存档迁移的 null 默认值是明确的版本转换，不等于容忍任意损坏位置。

---

## 8. 游戏命令、判定与原子提交

### 8.1 识别结果契约

```ts
export type RecognizedAction =
  | Readonly<{ type: "known"; actionId: ActionId }>
  | Readonly<{ type: "unknown" }>;
```

适配器可以在自己的诊断对象里带置信度、拒识原因或时间，但提交给规则的业务值只有动作 ID。unknown 原因可用于 UI 提示，不扩展成新的惩罚分支。

### 8.2 已有的输入提交命令

```ts
export type SubmitStoryInputCommand = Readonly<{
  type: "submitStoryInput";
  storyId: string;
  stepId: string;
  actionId: ActionId;
}>;
```

不带本地化字符串、原始音视频、玩家输入方式、模型得分、处罚数值或 `isCorrect`。正确与否必须由 Core 比较内容中的 `targetActionId`。

首版不为每轮输入增加持久化 attemptSeq；防重复主要通过一次性输入轮次和现有会话锁完成。这里不宣称对任意第三方重复投递提供分布式“恰好一次”语义，详见第 9 节。

### 8.3 规则校验顺序

收到 `submitStoryInput` 时依次检查：

1. 状态和内容满足既有不变量，本局不是 ended。
2. `storyId` 是当前队首且定义存在。
3. 命令 actionId 属于已实现的稳定动作集合。
4. 从恢复检查点计算下一个未通过的输入步骤。
5. 命令 stepId 与该输入步骤一致；否则按过期/非法输入拒绝。
6. 使用规则定义比较目标，再计算完整 nextState。
7. 再次验证不变量，经 GameSession 一次提交。

比较动作的函数只是 `actionId === targetActionId`。媒体识别难度不能被带进这个函数。

### 8.4 正确输入

没有额外奖励时，正确输入只推进检查点。若是 Story 最后一步，内部复用内容完成辅助函数，在同一个 nextState 中移除队首、追加完成记录、清空检查点，并按已有规则处理 ending → ended。

最后一步正确时，调用的是校验通过后的内部内容完成辅助函数，不是再 dispatch 一条公开 completeStory 命令；公开命令的“仍有输入就拒绝”保护不能因此被关闭。

不能先派发“正确输入成功”给 UI，再由 UI 另发一个命令保存通过位置。否则关闭进程可能让显示和存档不同步。

### 8.5 明确错误

读取当前步骤的 `wrongEffects`，使用公共效果函数更新属性/Flag。属性继续按已有 min/max 裁剪；反馈使用实际变化量，不显示超出边界的名义扣分。

把检查点设为当前输入点，保持 Story 未完成。不得改写案件节点、历史裁定或已有案件快照。

错误本身是正常的游戏结果，不是 `TransitionFailure`。保存成功后，可返回局部 `inputFeedback` 表示“识别到了其他动作，请重试”；真正的过期命令、内容非法或保存失败才走技术错误路径。

省略错误效果或属性已到下限时，仍应给出错误提示。沿用当前统一提交入口时，known 错误可以照常提交一次，即使数值恰好没变；首版不为这点优化引入复杂 no-op 提交协议。

### 8.6 unknown 与设备错误

unknown 不进入 `dispatch`，不保存 raw text、不增加 revision、不影响 Flag。UI 保持等待并允许重新输入。

设备错误不转换成一个假的动作 ID。对于 `PERMISSION_DENIED / DEVICE_UNAVAILABLE / MODEL_LOAD_FAILED` 等错误，显示稳定 code 对应的文案并提供替代输入方式。

### 8.7 效果与反馈的最小复用

现有 `applyGameEffects` 已供案件与输入步骤复用。它返回新数值及实际变化量，不负责案件完成、剧情入队或结局判断。本版继续沿用，TTS 不调用它。[C7][C12]

建议的反馈来源：

```ts
export type FeedbackSource =
  | Readonly<{ type: "case"; caseId: string }>
  | Readonly<{ type: "storyInput"; storyId: string; stepId: string }>;
```

既有 `attributeFeedback` 使用该 source。错误但无属性变化时，可新增简单 `inputFeedback`，只描述 `wrongAction` 等稳定状态；它不是持久化事件日志。

同一输入点的反馈可锚定当前输入面板或属性栏，不能为复用动画填入一个不存在的 caseId。历史案件的变化快照不包含后来的输入处罚，历史回看也不重新执行这些处罚。

### 8.8 与案件解锁、全局 Flag、结局的关系

**首版默认不改变现有全局结算时机。** 输入命令只处理当前步骤及配置的属性/Flag，不自动调用完整案件结算或全局解锁流水线。

现有实现会在案件最终结算时选定结局并锁定 endingId。[C7] 因此默认语义是：

| 输入发生时点 | 对结局的含义 |
| --- | --- |
| 某次后续结局判断之前 | 已保存的属性/Flag 可被后续原有结局规则读取 |
| endingId 已经锁定之后 | 可按内容继续完成输入，属性可变化，但不重新选择结局 |
| ended 之后 | 拒绝新的游戏输入命令 |

只要仍有合法队首内容，输入可发生在 playing 或 ending 阶段；因此不会因原有“结局已锁定、普通前置剧情尚未播完”的顺序而卡死。

若策划明确要求“最后一次输入必须影响最终结局”，再将该输入安排在锁定之前，或局部增加“完成指定前置输入后再选择结局”的规则。不能隐式声称所有输入后果都会立即影响结局，也不必为了未确定需求现在全面重排推进系统。

错误 Flag 只能来自内容声明的合法 Flag，不得借它直接设置案件已完成、跳转案件节点或修改历史结果。是否允许后续全局规则读取某个 Flag，是明确的内容设计决定。

### 8.9 保存失败与操作可见性

| 情况 | 行为 |
| --- | --- |
| 成功提交 | 发布新数值/检查点，随后显示反馈或继续内容 |
| 明确未写入的保存失败 | 保留旧已提交位置和数值；提示重试，不假装已通过 |
| revision 冲突或写入结果不确定 | 按现有机制进入 needsReload；取消识别，显式重载，不自动重放输入 |
| 临时反馈监听器异常 | 不撤销已经成功的提交 |
| 播放器渲染失败 | 不从媒体回调重新施加惩罚；恢复只读已提交事实 |

这些处理复用当前 GameSession 的锁、条件写入与不确定写入恢复策略。[C3][C5]

---

## 9. 输入轮次、取消与防止重复处罚

### 9.1 一轮输入的最小定义

一轮是一次明确文字提交、一段录音，或一次摄像头采样过程。每轮最多产生一个最终 RecognizedAction，并且这个最终结果最多被消费一次。

控制器用局部 `consumed` 标记、递增 generation 和取消信号即可。不需要把这些状态写入存档。

```text
idle → capturing / recognizing → final result → consumed
                                      ↓
                            unknown：等待下一轮
                            known：提交并等待保存
```

标记 consumed 必须发生在第一次 await 之前，不能等数据库保存成功后再锁住本轮。否则同一个回调源可能在等待期间重复提交。

### 9.2 三个输入入口分别如何划分

| 方式 | 一轮结束 | 下一轮开始 |
| --- | --- | --- |
| 文字 | 点击提交或确认 Enter；输入法组合输入期间不提交 | 用户再次明确提交 |
| 语音 | 本轮录音结束，KWS 汇总整轮命中并归一化 | 再次点击录音 |
| 摄像头 | 得到一个稳定动作，或本轮采样到技术上限 | 用户明确“再试一次”，或经稳定中立姿态重新启用 |

首版可以使用显式“开始识别 / 再试一次”，不强制自动连续监听。若做自动重试，必须区分新的动作与持续保持同一个姿态。

**短暂遮挡、掉帧或置信度下降不等于用户回到了中立姿态。** 不能用几帧 unknown 自动解锁并不断处罚。

### 9.3 摄像头的最低保护

```text
用户开始本轮
     ↓
连续采样，形成一个动作判断
     ↓
锁住本轮并停止继续投递
     ↓
若 known，等待保存结果
     ↓
等待用户显式重试，或观察到明确稳定的中立姿态
```

只使用固定冷却时间不足以定义新动作。持续保持错误手势时，冷却结束后仍不应自动再扣一次。

用户显式点击“再试一次”属于重新发起输入。需要更强的动作自然交互时，可再要求先放下手；这个交互体验可以调整，但不得退回按帧计罚。

### 9.4 迟到回调与会话归属

本轮开始时捕获：当前会话实例、Story ID、Step ID、语种、输入方式和本地 generation。回调处理前确认这些归属仍然有效。

切换 Profile/存档、离开输入点、切换输入方式、改变输入语种、进入 needsReload 或销毁页面时，取消本轮并使 generation 失效。SDK 的 cancel 不能保证不再回调时，也必须在接收端丢弃结果。

不允许一个旧麦克风请求完成后，对新 Profile 的同名 Story 提交动作。仅比较 stepId 不足以识别会话切换。

### 9.5 已提交命令与媒体取消的区别

点击正确输入后已经进入 GameSession 的命令不能通过关闭相机撤销。UI 等待保存，媒体设备可以停止；最终以保存结果为准。

保存中禁用下一轮输入。不要把尚未处理的语音片段/视频结果排成提交队列，等锁释放后逐个处罚。

### 9.6 Core 的保护及首版限制

Core 的队首、Step ID 和检查点校验会拒绝已经通过步骤的旧成功结果；GameSession 的 BUSY 会拒绝重叠命令。尚停在同一输入点的两次错误，业务上可以都合法，所以 Core 不能仅凭相同 actionId 判断重复。

因此首版的防重边界明确为：**受控本地输入入口保证一轮只消费一次，统一会话不自动重放。** 不增加永久尝试日志，也不声称仅靠 BUSY 就能抵御所有迟到重复。

未来确实引入跨进程命令重放、服务器回执或可靠重试协议时，再给命令和存档增加有界幂等信息；那不是目前三个输入按钮必须承担的复杂度。

---

## 10. 文字匹配规则

### 10.1 目标是词典匹配，不是自然语言理解

文字入口直接检测当前语种词典中的词/短语。当前 KWS 语音入口不调用 Text Matcher，而使用同源词典生成的关键词配置；将来改用 ASR 时才可在适配器内部“转写后调用 Matcher”。二者都不知道当前目标，先识别动作，再由 Core 比较。

不能先只找目标动作，找不到就当 unknown；那样永远无法识别动作库中的其他动作并执行明确错误分支。

### 10.2 推荐匹配流程

```text
输入和词典别名使用相同规范化
       ↓
查找当前语种的全部受支持别名
       ↓
收集不同的 ActionId
       ↓
0 个 ID → unknown
1 个 ID → known
多个不同 ID → unknown（歧义，不处罚）
```

同一个动作的多个别名同时出现，只得到一个 ID。不要因“敬礼”和“行礼”同时命中同一个动作就误判为歧义。

建议的规范化是 Unicode NFKC、首尾空白处理、连续空白合并，以及英文大小写统一。不要随意删除所有分隔符，把原本不连续的字母/文字拼成动作词。

英文按单词/短语边界匹配，不能把 `waveform` 中的 `wave` 当作挥手。中文可按明确别名子串匹配；不自动引入繁简转换、拼音、音近字容错或跨语种翻译。

### 10.3 包含语义与否定句

本需求是“是否包含字典中的词”，不是判断玩家的真实意图。按这个默认规则，中文“不要敬礼”仍然包含“敬礼”；英文 “do not salute” 也会匹配 `salute`。

这必须在内容和测试中明确，而不是声称系统已经理解否定。UI 应引导玩家直接说动作词或短句。若产品以后要求严格口令，可以把模式改为规范化后的整句相等，或配置有限的明确短语规则；不能在首版悄悄接入大模型语义裁决。

### 10.4 示例预期

以下基于第 4 节两个动作的示例词典：

| 语种 | 输入 | 预期 |
| --- | --- | --- |
| zh-CN | 敬礼 | known(salute) |
| zh-CN | 我现在行礼 | known(salute) |
| zh-CN | 挥手 | known(wave) |
| zh-CN | 敬礼，然后行礼 | known(salute) |
| zh-CN | 敬礼还是挥手 | unknown，多动作歧义 |
| zh-CN | salute | unknown，不查英文词典 |
| zh-CN | 不要敬礼 | known(salute)，这是字面包含规则 |
| en-US | SALUTE! | known(salute) |
| en-US | I salute you. | known(salute) |
| en-US | waveform | unknown，不匹配单词内部 |
| en-US | salute or wave | unknown，多动作歧义 |
| en-US | 敬礼 | unknown，不查中文词典 |
| 任意 | 空白输入 | unknown |

本表直接约束**文字 Matcher**。KWS 语音入口共享词典源与单动作/歧义语义，但音频识别不等于对字符串执行相同算法；实际漏报/误报须另测。增加别名必须显式修改同源词典、重新生成关键词资源并回归。未来 ASR 路径的转写错字也不能让 Matcher 临时放宽规则。

### 10.5 冲突与安全边界

规范化后的同一个别名不能属于两个不同动作。存在包含关系的别名也要测试多动作歧义，不能靠词典对象插入顺序选择结果。

首版采用固定字符串匹配，不让内容包携带任意正则表达式或执行代码。输入过长时可限制长度并给出提示，不能把巨大文本丢给复杂的回溯表达式。

---

## 11. 语音识别接入：sherpa-onnx 为当前首选

### 11.1 供应实现决策与范围

**当前优先尝试并已接入 sherpa-onnx 本地关键词检测（KWS），不再优先 Whisper。** 这是输入服务的供应实现，不改变 actionInput 内容、GameState、submitStoryInput、检查点或保存后发布机制。[C11][C12][C13]

项目选择有限词库的识别路径，不需要获得完整转写。sherpa-onnx 的可定制 KWS 支持配置关键词并输出命中；声学参数和关键词编码属于适配层，不交给案件作者维护。[T12][T13]

“选用较轻量的 KWS”是本项目选型方向，不是承诺 sherpa 所有模型都更小、更快，也不意味着同一个模型可以同时做 KWS 和 TTS。

### 11.2 当前运行链路

```text
actionLexicons[玩家选定语种]
             ↓ 构建期生成关键词 token / 音素及 ActionId 标签
        sherpa KWS 配置
             ↓
玩家主动开始录音 → 本轮有限音频 → KWS 检测所有已配置动作
                                      ↓
                         汇总整轮命中的不同 ActionId
                                      ↓
                  0 个 → unknown；1 个 → known；多个 → unknown
                                      ↓
                         既有 acceptRecognized / submitStoryInput
```

必须检测当前语种词典中的所有受支持动作，而不是只加载当前目标；不能将一次最先命中直接当作整轮最终结果。同一动作被多次命中仍算一个动作，不同动作同轮出现按歧义拒识。[C11][C13]

### 11.3 沿用已有供应无关契约

实际公共边界位于 `src/shared/voiceInput.ts`，核心形状如下。正式代码已有阶段回调、停止/取消信号、错误码和诊断 context；下面只摘出决定架构的部分，不是要求用简化片段覆盖完整接口。[C16]

```ts
// VoiceInputRequest 复用现有正式类型，不在此建立另一份请求模型。
export interface VoiceInputService {
  readonly available: boolean;
  recognize(request: VoiceInputRequest): Promise<RecognizedAction>;
}

export interface VoiceInferenceBackend {
  readonly available: boolean;
  infer(request: VoiceInferenceRequest): Promise<RecognizedAction>;
}
```

Game Core 不依赖 sherpa 类型；UI 不得到 KeywordSpotter，也不直接判定 target。未来确需换供应实现，只要保持 known/unknown 与取消、单轮语义即可。不是每种后端都必须实现一个假的 SpeechToText 接口。

### 11.4 当前平台基线，不把它误写为全平台承诺

仓库当前固定 `sherpa-onnx =1.13.8` 的 shared API，Windows 目标下接入；实施记录采用 `sherpa-onnx-kws-zipformer-zh-en-3M-2025-12-20`，运行时离线，准备资源的开发流程与游戏运行分开。[C11][C14]

现有输入契约为 16 kHz 单声道、有界 float32 PCM，录音上限 8 秒。真实采样率需要转换，不能仅改字段；这些是**当前 KWS 服务参数**，不能当作 TTS 合成音频也必须 16 kHz 的要求。[C16]

本次不主动升级 SDK、不另接 Python 运行时、不建设第二个 Whisper 后端。以后选定 TTS 模型所需 API 与现有版本不兼容时，才显式升级公共 SDK/DLL，并同时回归 KWS。一个供应库可以承载两个独立服务。

### 11.5 单轮、取消、歧义和技术失败

继续保留现有约束：主动开麦；一轮只消费一次；结束录音和取消整轮是不同操作；取消、切页、切语言、切 Profile 或 needsReload 使迟到结果失效。raw PCM 不进入存档、日志或普通报告。[C11][C13][C16]

无命中、多个不同动作或不受支持标签归一化为 unknown，不处罚。权限/设备/模型加载/桥接失败是技术错误，不伪装成已知错误动作。

取消 UI 轮次不等于原生 SDK 已停止执行。原生在途调用未退出时继续遵守原有 BUSY/有界并发约束，不为了让按钮看起来可用而积累后台推理。实际任务取消能力由锁定版本验证。[C11]

### 11.6 词典生成与扩词

先修改正式 ActionId 与当前语种 actionLexicons，再通过现有准备脚本生成语音关键词资源；不要只编辑生成文件。扩词必须包含对应文字完成路径和语音负样本回归。[C11]

sherpa 所需 token/音素和 TTS 发音词典是不同资源。输入词典的每个别名可以生成一个 KWS 标签映射；旁白的自由文本不能自动并入可接受动作库。

触发阈值、boosting score 和最大解码路径是识别实现参数，不能把某个阈值直接称为相同数值的正确率。静音模型烟测只证明加载和调用链路，不证明真实短词可用。[T12][C11]

### 11.7 Whisper / ASR / Web Speech 的位置

whisper.cpp 与 sherpa ASR 保留为**将来需求变化时的替代参考**，不在当前实施清单中同时建设。若未来需要完整转写，可以在输入适配器内部使用“ASR → Text Matcher”，游戏契约仍不变。[T6][T9][T10]

浏览器 SpeechRecognition 仍不作为正式离线跨 WebView 保障；它的环境能力与数据去向需独立验证。本次不要在本地后端失败后静默切换浏览器或云识别。[T14]

### 11.8 验收状态与 TTS 的新增交接

继续记录当前模型的正确词/其他已知词/噪声/无关语音/多动作结果，以及冷暖启动、资源、取消和安装包表现。已增加测试入口不能替代这些数据；本次不宣称已完成真实麦克风验收。[C11][C15]

TTS 接入后，新增重点是**游戏自身音频不能被当作玩家输入**。输入算法和命令不变，在第 23 节规定的静音边界内开始采集。TTS 合成结束不等于播放结束，不能在音频仍在输出时开麦。

---

## 12. 视觉动作识别接入

### 12.1 优先使用关键点与少量规则

对于“敬礼”这种有限动作，优先考察 MediaPipe Pose Landmarker；需要更细手指姿态时再加 Hand Landmarker。它们分别提供人体和手部关键点，Web 端有官方接入示例。[T1][T2]

这提供的是可观察信息，不是现成的“敬礼正确”规则。动作条件由项目定义，在本地识别模块中输出 ActionId / unknown。

```text
摄像头帧
    ↓
人体关键点（必要时增加手部关键点）
    ↓
少量几何条件 + 短时稳定性
    ↓
候选动作 / 无明确证据
    ↓
稳定且唯一 → known
不足或冲突 → unknown
```

不需要把全套人脸、手部、人体追踪全部加载才能开始。首个样本应先验证最小关键点方案能否满足实际构图。

### 12.2 “敬礼”规则的考察方向

下面是待校准的设计方向，不是已经验证的模型或通用标准：

| 观察量 | 候选规则用途 |
| --- | --- |
| 手腕与头部侧边位置 | 判断手是否接近规定区域 |
| 肩、肘、腕关系 | 排除手只是自然放置或伸向其他方向 |
| 可选手指/手掌信息 | 根据美术与玩法定义区分近似姿态 |
| 关键部位可见性 | 遮挡或超出画面时拒识 |
| 时间上的保持/变化 | 避免偶然经过目标位置被立即判定 |

距离和角度应在一致坐标系中计算，按身体尺度归一化，避免不同分辨率和距离导致阈值失效。归一化图像的 x/y 缩放不相同时，计算像素距离/角度前处理纵横比；不能直接混合手部与人体模型各自不同的世界坐标系。

镜像预览可以只作用于显示层，规则明确使用原始帧坐标；不要显示镜像和算法镜像重复处理。首版是否左右手都接受需要明确，在测试样本中覆盖。

### 12.3 姿态与动态动作要区分

静态张掌不等于挥手，手在头附近也不自动等于敬礼。

如果动作库中的 `wave` 真正指左右挥动，应观察一段时间的轨迹，而不是把单帧 Open_Palm 直接映射为 wave。需要全身构图的鞠躬、下跪也不应假设普通桌面上半身摄像头一定能判断。

首版先支持实际能够验证的少量动作。输入组件可对当前目标判断摄像头能力；不支持该目标时禁用相机入口并说明原因，继续允许语音/文字。

### 12.4 预置 Gesture Recognizer 的用途与限制

MediaPipe Gesture Recognizer 的预置类别主要是握拳、张掌、指向、点赞等手势，并没有直接提供本项目的敬礼、动态挥手、鞠躬或下跪。它适合用来参考摄像头接入、手部类别输出和简单手势原型，不代表修改标签名就能识别另一种动作。[T3]

**不要把 Open_Palm 重命名为 salute 后声称完成敬礼识别。** 若需要同时知道手相对头部的位置，应引入必要的人体关键点，而不是仅依赖手部分类器。

### 12.5 自定义模型是备选，不是首版前置工作

如果动作数量增加、几何规则难以稳定区分，再考虑关键点分类器或自定义模型。其前提是已有可标注的动作定义、包含 unknown 的负样本，以及独立测试集。

v1.0 在 2026-09-16 核对时，Google 的 MediaPipe Model Maker 自定义手势教程已标注不再积极维护；本版保留该参考记录，实际使用前重新确认维护与版本状态。该教程仍可作为数据组织、none 类和导出流程的参考，但不能把旧教程当作无需版本验证的优先生产训练管线；也不能把这个弃用提示误读成整个 MediaPipe Tasks 推理库已弃用。[T5]

### 12.6 置信度、稳定性与拒识

只有达到足够证据、且在支持动作中没有明显歧义时才输出 known。目标外的画面、看不见手、多个候选接近、关键部位出框等情况统一 unknown，不强行取最高分。

可从较低采样频率和短时间稳定窗口开始实验，例如每秒 10–15 次识别、数百毫秒稳定观察。这些只是待测初值，不是性能承诺、固定玩法时限或行业标准。

状态惩罚来自确定的错误动作，不来自“模型猜测最大概率属于另一个动作”。需要单独测量未知动作被错认成已知动作的情况，这比只报告已知类别准确率更直接关系到玩家是否被误罚。

### 12.7 Worker 与资源释放

MediaPipe 的 Web 识别调用是同步的，官方提示视频识别会阻塞界面线程，并建议使用 Worker 隔离。[T4] 项目应将连续推理放在输入适配边界，不进入 React render、GameSession transition 或数据库提交。

最多保持一个在途识别任务，过时帧直接丢弃，不积压“帧队列”追赶实时画面。具体 Worker、GPU delegate、图像转移和 WASM 加载方式以选定版本及真实 WebView 实验为准。

输入结束后停止不再使用的媒体轨道、取消帧回调、释放帧对象；Worker/模型可以在内存预算内缓存，但不能继续偷偷采集。保存期间不得继续把模型结果排入游戏命令。

### 12.8 视觉考察应交付什么

至少交付：支持动作及构图说明、目标动作/另一个已知动作/unknown 的样本结果、镜像与左右手策略、遮挡/光照/距离/背景测试、持续错误姿态不重复处罚的录像或自动化回放，以及目标安装包中的加载和性能记录。

不要用同一段训练视频相邻帧同时作为训练和测试，再用高分证明泛化。不同参与者、不同录制轮次的测试结果应分开记录；不将小样本烟测描述为生产准确率保证。

---

## 13. UI、内容投影与无障碍替代

### 13.1 接入现有 StoryPlayer

StoryPlayer 对 `step.type === "actionInput"` 显示 `ActionInputPanel`，继续使用原来的内容 Overlay、层级和焦点处理。

```text
StoryOverlay
    └── StoryPlayer
          ├── text → 现有文字
          ├── video → 现有播片/降级
          ├── effect → 现有特效
          └── actionInput → 新输入面板
```

输入点不单独进入 Sidebar、不打开新的游戏场景，不与另一个 ChallengeOverlay 争抢焦点。

### 13.2 面板内容

面板展示当前内容说明、输入方式切换、对应输入控件、本轮状态，以及技术错误/unknown/明确错误的必要提示。

文字入口始终可用；相机或麦克风不可用时，不扣状态，也不自动当作通过。输入方式切换只是改变如何完成同一个动作，不改变目标或惩罚。

首版不需要展示识别模型的原始得分、动作概率柱状图或持久化尝试历史。开发诊断可以有独立开关，不进入正式玩法界面。

### 13.3 继续按钮与保存状态

显示 actionInput 时隐藏或禁用普通“继续”按钮。只有正确输入且保存成功才推进；Core 的 `completeStory` 检查仍是最终保护，不能只依赖按钮禁用。

保存中显示处理中，暂停本轮采集；失败时继续保留当前输入点。业务上明确错误但保存成功时，显示实际处罚；存储失败不能显示“已经扣除”。

### 13.4 UI 步骤索引与检查点同步

本地步骤位置继续由 StoryPlayer 维护，但以已提交检查点为恢复下限。进入新的会话/Story 时，从检查点或起点初始化。

正确输入提交后，将本地显示推进到已提交的 resumeStepId；错误提交后仍保持当前输入点。不要在每次 snapshot 更新或语言变化时将本地步骤重置为 0。

在同一 Story 内，普通内容已浏览到检查点之后时，语言更新或属性反馈不得把显示位置拉回检查点。需要因重载重新初始化时，用明确的会话/加载代际触发，而不是依赖本地化内容对象的引用变化。

### 13.5 焦点与可访问性

输入点出现后，将焦点放到说明或输入入口，不自动请求硬件权限。明确错误/unknown 用合适的实时提示区域说明，不每帧播报模型状态。

文字提交尊重输入法 composition，避免中文选字时 Enter 被误认为提交。提供键盘可操作的模式切换和重试按钮。

切换语种后更新提示与 aria 文案，不关闭内容或重新结算。模型加载失败提供文字替代，不让玩家为了完成内容必须安装摄像头。

---

## 14. 存档、内容版本与迁移

### 14.1 存档仍然是一份整局 JSON

增加 `storyCheckpoint` 不需要新建 SQL 表。Profile、SaveRepository、saveId/profileId 归属、revision 条件写入及保存后发布机制保持不变。

不保存音频、视频、识别出的整句文本、界面语言或模型运行状态。玩家正确输入后保留的是“这个内容位置已经通过”的事实，不是“他说了哪一种语言”。

### 14.2 版本现状与本版增量

当前 ActionInput 已使用 save schema v3、content schema v3，并保留旧 v2 内容包兼容。[C1][C10][C11] 本版仅为新旁白内容增加 content schema v4，不为 TTS 改存档：

| 版本 | 当前 / 本版处理 | 原因 |
| --- | --- | --- |
| save schema v3 | **保持，不升级为 v4** | narration、音频、播放状态不是游戏事实 |
| content schema v2 | 继续兼容 | 不含 actionInput 或 narration |
| content schema v3 | 继续兼容 | 已有 actionInput；不携带新增 narration 字段 |
| content schema v4 | 新增支持 | text/actionInput 可配置 narration，语言步骤可带匹配的 narrationText |
| 内容包 version | 新增包版本，例如 1.2.0 | 示例版本，尚未发布；不得覆盖既有 1.0.0 / 1.1.0 文件 |

文档 1.1、应用版本、save v3、content v4、内容包 version 与 SDK 版本是独立维度。严格 Schema 升级和兼容处理见第 24.1 节。

### 14.3 旧存档迁移

迁移仅在 storage/load 边界执行：旧 v1 先沿用已有事实化迁移得到 v2，再由 v2 增加 `storyCheckpoint: null` 得到 v3。

保持 saveId、profileId、revision、contentRef、已有数值、案件历史、剧情完成记录与时间不变。不能因为添加输入功能而重新执行案件或剧情效果，不能把旧存档绑定到最新内容包。

旧存档没有输入检查点是明确的历史事实：其原内容版本也没有这种输入步骤。不能把 null 当作理由，将新交互直接插进它绑定的旧内容文件。

上述 v1/v2 → v3 路径是已经存在的 ActionInput 兼容基线，继续保留读取、create、commit 与 SQL 版本条件的一致性。[C5][C10] **TTS 本身不需要再次修改这些版本条件，也不触发写回或额外迁移。**

### 14.4 旧内容版本的兼容

一局仍绑定精确 ContentRef。已有 v2/v3 内容包继续保留；发布旁白内容不改绑旧存档、不重写旧包。

现有读取/校验边界已兼容 v2/v3，TTS 在同一边界补 v4，Core 继续消费统一运行时类型。v2 拒绝 actionInput；v2/v3 均拒绝 narration / narrationText；只允许明确标为 v4 的新内容使用新字段。不要把新字段静默塞进旧格式。

可使用版本分支 Schema 加一个简单适配函数，不需要长期保留两套 Game Core。适配不能更改旧 packageId/version、规则含义或语言 ID。

语言包既有步骤 `blocks` 结构可复用，但覆盖规则必须将 v3 的 actionInput 包括在需要文案的步骤集合中。加载不同格式时以各自版本规则校验。

### 14.5 内容更新与识别实现的关系

首版词典与模型配置随应用发布，不纳入存档。模型或匹配词更新会改变未来输入的识别覆盖，但不重新计算已通过位置和已扣数值。

未来内容热更不能发布应用尚不认识的 ActionId，然后期待模型自动具备该能力。新动作 ID 必须同时有规则 Schema 支持、相应输入路径和测试；仅添加新的敬礼出现位置则可以复用既有动作能力。

如果以后要求“相同存档在不同应用版本中仍使用完全相同识别词典”，再把词典版本绑定进内容兼容策略。首版不为没有提出的回放确定性保存模型和词库副本。

### 14.6 回退与损坏

较旧应用读取 v3 存档时应明确拒绝不支持的格式，不得忽略检查点后从头重播。加载损坏的 checkpoint、缺失绑定内容或未来 schema 时，走现有可见错误与恢复路径，不创建新局覆盖旧数据。

---

## 15. 内容与字典校验

### 15.1 规则内容新增检查

| 检查 | 目的 |
| --- | --- |
| actionInput 的 ID 非空且同 Story 内唯一 | 稳定恢复与命令定位 |
| targetActionId 属于受支持集合 | 防止永远无法通过的目标 |
| wrongEffects 只包含允许的属性/Flag 操作 | 不让输入步骤偷带案件操作 |
| 属性和 Flag 引用存在 | 不产生隐式新状态字段 |
| 数值合法并满足既有整数契约 | 复用现有数值规则 |
| 含输入步骤的 Story 不可整体跳过 | 避免 UI 跳过绕过事实要求 |
| 所有需要展示的输入说明有语言内容 | 沿用现有严格 ID 覆盖校验 |
| v2 包不含 v3 步骤 | 明确格式兼容边界 |

不要只检验纯 TypeScript 编译。外部 JSON 必须经过实际运行时 Schema 和引用校验。

### 15.2 字典新增检查

检查空别名、规范化后空白、非法动作 ID、规范化后不同动作的完全重名、当前发布语言是否有目标动作的文字替代路径。

一词多义是配置问题，不能让两份字典以不同插入顺序得到不同结果。包含型匹配产生的组合歧义，由固定测试验证 unknown 路径。

不要求不同语种的同义词数量、字符串长度和视觉动作定义形状相同；这不是翻译字符串一一对应系统。

### 15.3 识别能力检查与内容校验分开

构建期可以检查“这个动作在注册的实现中声明为可支持”，但不能静态证明某台用户设备一定能识别。

运行期能力检查决定某种输入方式是否可用，例如设备权限、模型是否能加载、目标是否在视觉支持集合。技术失败要可降级到文字。不要因为相机不可用就把整局存档判为非法。

### 15.4 存档不变量扩展

除第 7.7 节检查点规则外，现有属性范围、案件引用、历史 actualDelta、队列去重与阶段规则继续生效。

新增输入后，不能假设“当前属性一定等于初始值加全部案件快照之和”。属性可能合法地被内容输入改变；案件快照仍然只记录该案件当时的后果。若已有测试或辅助工具作出该假设，应明确修正，不通过给输入伪造案件快照来满足它。

### 15.5 AI 与内容制作交接

作者或 AI 可以生成 actionInput 步骤及两种语言的说明，但不能凭空创造尚未实现的动作 ID 或规则字段。

提供可用动作清单、错误效果白名单和例子，生成后走同一个 Validator。新增故事位置不需要修改 React；新增一种真正的视觉动作则是识别实现工作，不能只靠内容生成完成。

---

## 16. 平台接入、资源与隐私

### 16.1 媒体权限

摄像头和麦克风采集需要用户授权；`getUserMedia()` 有安全上下文限制，授权被拒绝、无设备或设备不可读会产生不同异常，授权请求也可能一直等待用户处理。[T15]

项目应在用户明确选择对应方式后再请求权限，并显示“授权等待中/可取消或改用文字”。不要在启动、登录或浏览案件时自动打开硬件。

对于不能真正取消的授权/采集调用，“取消”至少应使本轮结果失效。若用户稍后授权、旧调用才返回 MediaStream，应立即停止这份迟到的流，避免相机在面板关闭后继续工作。

Tauri 的 capability 配置与 WebView/操作系统的媒体授权不是同一件事。原生调用所需权限、页面的安全来源和系统的隐私设置都要验证；不虚构一个通用 `camera:allow` 插件权限当作完整解决方案。

### 16.2 浏览器开发成功不等于桌面可用

Tauri 在不同桌面平台使用不同 WebView 集成，Windows 使用 WebView2。[T16] 需要在实际首发系统的安装包中验证采集、模型、WASM/Worker、视频/音频数据和断网场景。

能力检测不止看 API 是否存在，还要实际启动一次受授权采集并完成一轮输入。设备或功能不支持时明确降级，不扩大平台承诺。

### 16.3 本地模型与资源

模型、WASM 文件、词表/分词器、配置和必要运行库分别列入构建清单，固定版本，不在发布包里引用 `@latest` CDN 地址。

首版随应用打包的识别资源可以由平台层按固定资源映射读取，复用现有资源解析习惯；不必为此把游戏媒体 asset.kind 扩展成任意可执行模型/脚本容器。Tauri 提供附加资源打包和资源目录解析机制，具体路径和访问方式按目标平台验证。[T17]

断网运行的验收应使用干净环境或清理缓存，避免模型其实来自开发缓存却被误认为已随包部署。

### 16.4 原生语音接入

当前沿用受控 Tauri/Rust → sherpa-onnx shared API 路径，不为本次 TTS 再增 sidecar 或另一进程。KWS 与 TTS 的模型对象、请求槽和结果类型分开；复用可验证兼容的 SDK/DLL、资源准备与诊断机制。[C11][C14]

KWS 入口只识别音频，TTS 入口只把获准文本与逻辑音色转换成音频；都不能执行任意 shell、读取 UI 指定的任意模型路径或修改游戏状态。

模型是否缓存由服务自己的内存预算决定。若以后因隔离/平台原因确需 sidecar，再按官方部署机制处理，那是适配器内部变化，不是本次默认范围。[T18]

### 16.5 编码和线程

采集结果与识别引擎输入格式要显式转换。录音容器文件不能仅改后缀就当 PCM；具体 MIME/解码支持按当前 WebView 实测。媒体格式不一致属于适配器失败，不属于 unknown 动作后的处罚。

原生推理不能阻塞需要保持响应的 UI 调用路径；Web 连续视觉推理按第 12 节隔离。资源上限、最大采样长度、单个在途请求和取消逻辑是必要的工程约束，不是游戏计时机制。

### 16.6 数据最小化

默认只在内存中保留完成本轮所需的数据。结束后释放音频、视频帧和关键点；临时文件确有必要时，限制位置并在完成/取消后清理，不将它们写入 save JSON 或普通生产日志。

默认不保存用户原话，不上传摄像头画面，不以此建立人脸/声纹身份模型。如果后续选择联网语音，必须作为显式产品与隐私行为说明，不可在离线方案失败时静默上传。

开发录样本应征得参与者同意，并区分训练、验证和回归数据。发布前核对 SDK、模型权重与附加资源各自的许可和再分发要求；本文不替代具体许可证审查。

---

## 17. 目录、接口与实施分工

### 17.1 现有文件与本版增量

下面只表示落点；“新增建议”不是当前已经存在的文件。名字可遵循项目习惯，不要求创建空目录或为每层增加 class。

```text
src/
  shared/
    locale.ts / action.ts / recognizedAction.ts   已有；保持稳定契约
    voiceInput.ts                                已有语音输入接口，不改成 TTS 接口
    narration.ts                                 新增建议：旁白请求/结果/逻辑音色
  content/
    schema.ts / validate.ts                       新增 v4 narration 及语言字段校验
  game/
    storyInput.ts / effects.ts                    已有；TTS 不调用或改写其语义
    model.ts / commands.ts / transition.ts        保持 save v3 和输入命令
  input/
    actionLexicons.ts / matchTextAction.ts        同源输入词典；不混入 TTS 发音资源
  application/
    gameSession.ts                               保存事实，旁白不 dispatch
    gameContentView.ts                           新增只读 ContentNarrationView
    gameSessionView.ts                           沿用实际内容语种与加载规则
  ui/
    presentation/story/StoryPlayer.tsx            进入步骤触发旁白，离开停止
    presentation/story/ActionInputPanel.tsx       接收/协调收音前的静音边界
    presentation/narration/                      新增建议：hook、小型控件/provider
    input/VoiceInputProvider.tsx                 沿用；接入根级互斥约定
    i18n/messages/                               补停止/重播/不可用等实际文案
  platform/
    ...现有语音采集与原生桥接                    保留
    narration/                                   新增建议：TTS 桥接、音频播放
  storage/
    ...                                          不因 TTS 新增表或改 save 版本
  app/
    ...                                          装配一个旁白服务，协调既有 VoiceInputShell
src-tauri/
  src/voice_input.rs                             保持 KWS 入口
  src/narration.rs                               新增建议：有界本地 TTS 合成入口
  Cargo.toml / 资源声明                           复用兼容 sherpa 运行库，加入 TTS 资源
scripts/
  ...现有 voice:prepare                           继续生成 KWS 同源词库
  ...TTS 资源准备脚本                             新增建议；模型/发音资源独立清单
```

旁白生命周期 hook、provider 与播放函数很少时可以放在同一个模块。不要为了名字相似把 VoiceInputService 和 NarrationService 合成一个同时负责输入判罚、播放和存档的大服务。

### 17.2 核心纯函数边界

推荐把以下逻辑保持可独立测试：

| 函数职责 | 输入 | 输出 |
| --- | --- | --- |
| 文本匹配 | 文本、语种词典 | known / unknown |
| 定位下一输入 | 队首内容、检查点 | 输入步骤或没有未完成输入 |
| 应用游戏效果 | 当前数值/Flag、合法效果、属性定义 | 新值与实际变化 |
| 提交已知动作 | GameState、命令、规则内容 | TransitionResult |
| 生成内容表现 | 规则与语言包 | 只读输入说明，无 wrongEffects |

它们不需要成为单独 npm 包，也不依赖识别模型才能测试。

### 17.3 技术错误的归属

| 错误示例 | 所属层 | UI 行为 |
| --- | --- | --- |
| NO_MATCH / AMBIGUOUS | Matcher 内部原因，可统一 unknown | 提示再试，不处罚 |
| PERMISSION_DENIED / DEVICE_UNAVAILABLE | 输入适配器 | 解释原因，允许文字 |
| UNAVAILABLE / RECOGNITION_FAILED | 现有语音输入适配器 | 重试或切换方式 |
| SYNTHESIS_FAILED / PLAYBACK_FAILED / AUTOPLAY_BLOCKED | 新旁白服务 | 保留文字，允许停止/重播，不改游戏状态 |
| STALE_STORY_INPUT / STORY_INPUT_REQUIRED | Core / Application | 刷新当前输入位置，不当作错动作处罚 |
| SAVE_FAILED / RELOAD_REQUIRED | 现有会话/存储 | 沿用保存失败或重载路径 |

代码命名可服从现有错误码风格，不能将所有失败统称为“动作错误”。UI 存 code，在渲染时翻译，不缓存已翻译的异步错误句子。

### 17.4 并行实施分工

**ActionInput 基线维护**：已有规则/存储/文字链路不重做，保持固定回归。本次 TTS 不要求修改内存仓储、SQLite 或已知动作命令。

**TTS 内容与 UI 线**：先用 FakeNarrationService 完成旁白配置、只读投影、进入/离开、停止/重播和收音互斥，不等待最终模型。

**TTS 平台线**：选择一个固定声音模型，接入本地合成与播放、资源准备和有界取消；正式接入保持旁白契约。视觉仍可单独考察，不修改案件规则。

依赖边界检查脚本应同步允许必要的 shared/input 纯类型依赖，不以禁用边界检查来接入 SDK。

---

## 18. 测试、联调与验收

### 18.1 最小固定联调样本

保留现有两个案件回归样本，增加一个不改变案件裁定的 Story：

```text
前置文字
  → 输入敬礼，错误 authority -2
  → 中间文字或视频
  → 再次输入敬礼，错误 authority -1
  → 尾部文字
```

两个输入点使用相同目标、不同 Step ID，用来证明“通过的是位置，不是全局动作”。另加两个不同 Story ID 的相同模板，用来验证周期位置相互独立。

### 18.2 纯逻辑测试矩阵

| 编号 | 场景 | 必须结果 |
| --- | --- | --- |
| G01 | 正确目标动作 | 检查点到下一步，案件进度不变 |
| G02 | 其他已知动作 | 按配置处罚，仍在当前输入 |
| G03 | 无错误效果 | 仍要求正确动作，无额外处罚 |
| G04 | 属性已到下限 | actualDelta 符合裁剪，不显示虚假扣分 |
| G05 | 非法动作 ID | 拒绝命令，不当作合法错动作 |
| G06 | 命令不是队首 Story | 拒绝 |
| G07 | 命令指向后面的第二个输入 | 拒绝，不能跳过第一个 |
| G08 | 重复提交已通过的输入 | 拒绝，不重复推进 |
| G09 | 有未通过输入时 completeStory | 返回 STORY_INPUT_REQUIRED |
| G10 | 最后一步是正确输入 | 同次提交完成 Story 并清空检查点 |
| G11 | 正确输入后还有普通尾部 | 从下一步恢复，尾部完成后才完成 Story |
| G12 | ended 状态输入 | 拒绝 |
| G13 | ending 阶段合法队首输入 | 可完成当前内容，不重新选择已锁定 endingId |
| G14 | 输入修改 Flag | 只改声明的 Flag，不调用案件结算 |

### 18.3 存储与恢复测试

| 编号 | 场景 | 必须结果 |
| --- | --- | --- |
| S01 | 正确后、尾部播片中退出 | 从输入之后恢复，不再要求已通过动作 |
| S02 | 错误扣分后退出 | 从当前输入恢复，扣分不重复执行 |
| S03 | unknown 后退出 | 允许重播上次检查点以来的普通内容 |
| S04 | 保存明确失败 | 旧数值和位置保持不变，UI 不表现为已通过 |
| S05 | 写入结果不确定 | needsReload，不自动重发旧输入 |
| S06 | 旧 revision 提交 | 冲突处理与当前会话一致 |
| S07 | 两个连续输入 | 第一处完成不会自动完成第二处 |
| S08 | v2 迁移 | 增加 null 检查点，历史事实与 contentRef 不变 |
| S09 | checkpoint 引用缺失步骤 | 报兼容/存档错误，不静默从头 |
| S10 | 完成 Story 后重载 | 无旧检查点残留 |
| S11 | 同模板不同 Story 位置 | 已完成前一次不屏蔽后一次 |
| S12 | 历史案件回看 | 不重新执行输入效果或改变旧裁定 |

### 18.4 输入控制测试

使用假的识别器主动制造重复回调、乱序、取消后回调和长时间等待：

| 编号 | 场景 | 必须结果 |
| --- | --- | --- |
| I01 | 同一文字事件触发两次处理 | 本轮只消费一次 |
| I02 | 同一语音多个 KWS 命中/重复标签 | 按整轮聚合不同 ID，只提交一次最终结果 |
| I03 | 持续错误姿势几十帧 | 一轮最多一次处罚 |
| I04 | 相机短暂丢失后又看到相同错误姿势 | 不自动视为新动作连续处罚 |
| I05 | 保存期间还有识别结果 | 不排队处罚，不发新命令 |
| I06 | 切换 Profile 后旧回调返回 | 丢弃 |
| I07 | 切换输入方式后旧结果返回 | 丢弃 |
| I08 | 切换语种后旧 KWS 结果返回 | 丢弃，不用新词典重判 |
| I09 | 切换语种时已开始保存 | 原 ActionId 提交不变，之后用新文案展示 |
| I10 | permission 请求一直挂起 | 可改用文字，不冻结整个应用 |
| I11 | 用户没有设备 | 技术提示，不处罚、不自动通过 |
| I12 | 中文输入法确认字符 | 不误提交未完成的文字 |

### 18.5 本地化与表现测试

同一份存档可在两种语种中继续当前输入。语言切换不改变属性、案件进度、checkpoint 或 revision。

内容 fallback 与输入语种选择分开；Portal/Overlay 的说明、错误、按钮与 aria 文案按当前 UI 语言更新。字典别名不用 t()，不会把英文别名同步翻译成中文后再匹配。

普通内容已前进到检查点之后时，语言切换不将 StoryPlayer 步骤重置。保存中的错误也不应因为文案重新生成而消失或重复提交。

### 18.6 识别质量应分别报告三种误差

| 指标 | 关注的问题 |
| --- | --- |
| 正确动作被拒识为 unknown | 玩家需要重试多少次，是否难以完成 |
| unknown 被识别成已知动作 | 是否产生无依据的通过或误罚 |
| 一个已知动作被识别成另一个 | 三分支判定是否可信 |

另外记录：每轮意外重复提交数、平均/高分位通过尝试次数、冷启动/热启动时延、内存与 CPU、语种与设备可用性。

阈值应在产品认可的目标设备与样本上确定。可用少量不同参与者做初始烟测，但不能以几个开发者样本宣称全体玩家的准确率。本文不编造已测性能数字。

### 18.7 安装包验收

在真实首发系统运行打包应用，覆盖干净安装、断网、模型未缓存、权限允许/拒绝、设备被占用、相机拔出、语音取消和保存失败。

确认没有意外 CDN/远程模型依赖，没有离开交互后继续采集，没有原始音视频进入存档/日志。开发浏览器实验和现有项目的自动化通过记录不替代这些新能力的验收。

---

## 19. 实施顺序与完成标准

### 19.1 第一阶段：纯文字闭环（已实现基线，维护回归）

实现 actionInput 内容模型、普通按语种词典、Matcher、submitStoryInput、公共效果函数、检查点、completeStory 保护与对应迁移。UI 仅接文字和假的媒体入口。

完成标准：通过正确/错误/unknown、同 Story 多输入、退出恢复、重复回调和双语切换测试。此时功能已是完整可玩的内容交互，而不是等待模型才能验证的架构骨架。

### 19.2 第二阶段：语音适配（已接入 sherpa，继续验收）

当前 sherpa-onnx KWS 已接入，继续围绕这一后端完成真实短录音、设备和打包验收。保持“同源词典、统一 RecognizedAction、同一条命令”，不要求 KWS 经过 Text Matcher，也不在语音回调里复制处罚逻辑。[C11]

完成标准：模型/语种明确、未知可拒识、每轮一个结果、可取消、断网策略明确、真实安装包可用。未达到要求前保留文字默认路径。

### 19.3 视觉适配（后续独立，不阻塞 TTS）

用实际构图先验证敬礼和一个确定可区分的错误动作。候选规则可以调整，不修改 Core 的三分支语义。

完成标准：目标/已知错误/unknown 均经过样本验证，持续姿态不重复处罚，性能不会阻塞 UI，设备与资源失败可降级。

### 19.4 内容扩充与周期位置

在已有内容规则中加入需要的固定位置；周期次数有限时展开为唯一 Story ID。增加动作种类前先增加识别能力和样本，而不是先发布无法通过的内容。

### 19.5 首版停止条件

ActionInput 的文字、语音和视觉按各自范围验收；**不把未接入的摄像头作为这次旁白交付的前置条件**。本次 TTS 的实施与完成标准单列于第 25 节，不重跑已完成的架构建设。

不因实现过程中发现 SDK 还有更多能力，就顺带引入情绪识别、身份识别、复杂动作组合、自由对话、挑战任务线或通用剧本引擎。

---

## 20. 技术参考与优先考察项
21. TTS 旁白：范围、内容配置与语言
22. 旁白服务、固定音色与平台适配
23. 旁白播放、收音互斥与生命周期
24. TTS 内容版本、资源、缓存与诊断
25. TTS 实施步骤与验收矩阵

### 20.1 给实施团队的考察顺序

| 当前优先级 | 方向 | 优先资料 | 本次目标 / 边界 |
| --- | --- | --- | --- |
| 已选择，继续验收 | 语音输入 | sherpa-onnx KWS、中英模型、项目 Voice 实施记录 [T12][T13][C11] | 保留现有单后端，实测短词、拒识、取消与回声，不回到 Whisper 优先 |
| 新增主线 | 旁白输出 | sherpa-onnx TTS 总览与模型指南 [T19] | 一个固定声音的本地合成，作为供应无关 NarrationService 的实现 |
| 优先试听候选 | 固定中英音色 | VITS/Melo 中英模型 [T20] | 固定台词与单声音适配；不是对资源最小或效果最好的预判 |
| 对照候选 | 固定中英音色 | Kokoro 中英模型 [T21] | 从预置声音中选定一个配置；不要把多音色 UI、克隆或实时变声带进来 |
| 后续独立 | 视觉动作 | MediaPipe Pose / 必要的 Hand [T1][T2] | 少量关键点规则，输出同一个动作契约，不阻塞 TTS |
| 参考 | 简单手势 | MediaPipe Gesture Recognizer Web [T3][T4] | 类别/摄像头/Worker 示例，不是现成敬礼分类器 |
| 备选 | 自定义手势 | Model Maker 教程 [T5] | 保留维护状态警示，不是默认训练管线 |
| 历史替代资料 | 完整语音转写 | whisper.cpp / sherpa ASR [T6][T9][T10] | 只有未来需要完整转写或已验证的供应问题时重新评估，不同时交付 |

模型考察只选一个满足项目台词、平台和体积要求的 TTS 方案。两款试听候选不是要求同时部署，也不要求测试 SDK 支持的所有模型。

### 20.2 考察应交付什么

KWS 继续回答三分支识别是否可靠。TTS 单独回答：固定台词是否读对、选定音色是否符合内容、首次/重复播报等待是否可接受、随包资源是否完整、停止后是否真的安静、开麦后会不会识别到游戏自己。

测试集使用项目实际中英教程、短口令、人名、数字和标点，不以任意一句演示替代。记录模型、SDK、资源哈希及测试机器；未测试的数据不填成“通过”。

### 20.3 资料核对与版本约束

本版于 2026-09-18 重新核对 sherpa KWS/TTS 官方文档、TTS 两类候选和播放器 API 说明。沿用的视觉资料及历史 Whisper 资料见原版 2026-09-16 参考记录；不将原版考察顺序继续作为当前决策。

项目当前 SDK 固定为 1.13.8，**在线文档和 master 示例不等于这一固定版本的全部可用 API**。选定 TTS 模型后验证它与现有 Rust 绑定、DLL 和平台的兼容性，再决定是否升级；升级需回归已接入 KWS。[C14]

---

## 21. TTS 旁白：范围、内容配置与语言

### 21.1 功能定义与不变项

旁白是在教程或后续流程的固定内容位置，用一个选定声音朗读内容或固定提示。它是**内容表现能力**，不是交互判定，不是新的剧情分支，也不是系统对玩家进行自然语言对话。

```text
内容决定：什么时候说、说什么、用哪个逻辑声音
                         ↓
NarrationService 决定：合成、播放、停止与失败报告
                         ↓
sherpa-onnx TTS 决定：如何把文本转换成音频
```

Game Core 不关心文字是否被合成、播放器是否播完、玩家是否静音。旁白失败不能扣状态，旁白成功不能替玩家完成敬礼。`GameSession` 仍只处理现有游戏命令。

**本版默认朗读可以被停止或忽略。** 语音输入为避免自触发而暂时等待静音，这是设备协调，不是新增“必须听完”的游戏规则。需要强制收听、听完授予奖励、音频秒数影响剧情等规则时，必须另行提出，不从本次需求推导。

### 21.2 默认挂在已有步骤上，不新增必需的 narration 步骤

给 `text` 与 `actionInput` 增加可选 `narration`。`video` 与 `effect` 首版不挂同一自动朗读配置，避免视频已有声音与旁白重叠；需要片前播报时安排一个普通文字步骤即可。

```ts
export type NarrationVoiceId = "system";

export type NarrationDefinition = Readonly<{
  voiceId: NarrationVoiceId;
  textSource: "blocks" | "narrationText";
}>;
```

首版内容作者只使用 `voiceId: "system"`。这个逻辑 ID 不包含模型名称、speaker 数字、磁盘路径或 SDK 参数。以后增加角色声音是此集合的显式扩展，不要求现在建音色管理器。

配置示意：

```json
{
  "id": "inspection_salute",
  "type": "actionInput",
  "targetActionId": "salute",
  "narration": {
    "voiceId": "system",
    "textSource": "narrationText"
  },
  "wrongEffects": {
    "attributeDeltas": { "authority": -2 },
    "setFlags": {}
  }
}
```

`narration` 放在 game.json 的步骤结构中，是与视频 assetId、表现时序类似的内容元数据；**它不是 GameState 事实，也不得出现在命令 effects 中**。Core 可以随内容 Schema 读取到它，但不得据此改变规则分支。

### 21.3 两种文本来源

| textSource | 取值 | 作者职责 |
| --- | --- | --- |
| blocks | 当前步骤语言内容中的 blocks 按原顺序提取 text，以换行连接 | 直接读显示内容，不重复维护一份相同文案 |
| narrationText | 同一步骤语言记录中的 narrationText | 允许声音更短、口语化或使用明确数字读法 |

`blocks` 首版包含现有 paragraph / heading / quote 的纯文本，不加入 HTML、Markdown 标记、ARIA 标签或按钮说明。不要按页面 DOM 自动抓取整屏文字；旁白只读内容配置明确引用的文本。

例如显示内容可以说明三种输入方式，而 narrationText 只写“请敬礼。”。这是同一个内容步骤的两种表现，不另建全局旁白词典或识别词库。

语言步骤在 v4 增加可选 `narrationText: string`。Validator 对 `textSource = narrationText` 要求每个发布语言都有非空值；对未配置该来源的步骤，拒绝多余 narrationText，避免出现没有使用者的文案。UI 固有的“重播 / 停止朗读 / 朗读不可用”仍走既有 t()。

首版不支持 SSML、任意脚本、动态取属性表达式或从用户输入即时拼接台词。未来确需变量插值时，在受控内容投影中解析，不能让 TTS 适配器去读取游戏规则。

### 21.4 只读投影是 UI 的唯一内容来源

`createGameContentView` 从已校验的规则与语言记录得到只读旁白：

```ts
export type ContentNarrationView = Readonly<{
  voiceId: NarrationVoiceId;
  locale: AppLocale;
  text: string;
}>;
```

`ContentStoryStepView` 的 text/actionInput 分支增加 `narration?: ContentNarrationView`。UI 不重新遍历 rule target/effects，不从 DOM 反推台词，不把模型目录传到 React。

用与现有投影相同的加载边界，语言内容加载完成后再触发旁白。仅有 rule 已加载、语言仍未就绪时，不临时读 key/ID 顶替文字。

### 21.5 所选语种、实际内容语种与发音资源

| 对象 | 语言来源 | 与游戏事实的关系 |
| --- | --- | --- |
| UI chrome | I18nProvider 的选定语言 | 只影响界面 |
| 文字 Matcher / KWS 配置 | 玩家选定的输入语种 | 决定如何得到动作 ID，不写进命令 |
| 游戏旁白 | GameContentView 实际加载的 locale | 决定朗读哪一份内容，不改规则 |
| 纯 UI 教程提示的朗读 | 当前实际生成这段提示的 UI locale | 只播放传入文本 |
| TTS 发音规则、speaker | 平台按 voiceId + 朗读 locale 映射 | 模型资源配置，不是翻译或动作词典 |

英文内容失败回退为中文时，旁白使用中文文本和支持中文的声音；玩家主动选定的英语 KWS 不因此偷偷改成中文。相应的 input fallback 与叙事 fallback 分开处理。

三个名字相似的数据尤其不能混淆：**actionLexicons 是动作别名；KWS keywords 是由动作别名生成的检测资源；TTS lexicon/发音规则是合成文本所需资源。** 更新旁白人名读法，不应该改变玩家需要说什么动作。

### 21.6 固定时机由现有内容流程负责

教程由 Story 表达时直接在对应 text 步骤配置 narration；检查、口令等场景在 actionInput 上配置。有限的规律出现继续展开为不同 Story/Step 位置，复用相同旁白服务。

如果教程已有独立的 React 引导页，可以从该页进入时调用同一个旁白服务，传入当前已解析的固定文本与 locale；不要为接 TTS 新建一套教程进度或把 UI 提示强行伪造成案件。

旁白服务不监听“完成案件数达到多少”、不主动查询存档、不自行轮询阶段。业务/内容宿主决定触发，它只执行一次请求。

---

## 22. 旁白服务、固定音色与平台适配

### 22.1 两个服务，共用供应库

```text
VoiceInputService                 NarrationService
   ↓ 识别玩家                         ↓ 朗读内容
sherpa KWS 适配                   sherpa TTS 适配 + 音频播放
   ↓                                  ↓
RecognizedAction                  播放结束 / 取消 / 技术失败
   ↓                                  ↓
现有 GameSession                  所属界面的局部状态
```

不把 TTS 接进 `VoiceInferenceBackend.infer()`，也不让 KWS 服务返回音频。二者可以复用兼容的 sherpa shared 库、运行时部署和诊断基础，但**KeywordSpotter 与 TTS 的模型/实例/结果不相同**。[C13][C14][T19]

当前已部署 KWS 模型只能提供该识别任务，不能把其权重作为 TTS 权重。模型选择属于供应实现；内容只知道逻辑音色和文本。

### 22.2 最小公共契约

以下为新增建议，名字可沿用项目习惯。它覆盖业务宿主真正需要知道的事项，不暴露模型对象或音频传输细节。

```ts
export type NarrationPhase = "preparing" | "playing";

export type NarrationErrorCode =
  | "UNAVAILABLE"
  | "BUSY"
  | "INVALID_REQUEST"
  | "SYNTHESIS_FAILED"
  | "PLAYBACK_FAILED"
  | "AUTOPLAY_BLOCKED"
  | "TIMEOUT";

export type NarrationOutcome =
  | Readonly<{ type: "ended" }>
  | Readonly<{ type: "cancelled" }>
  | Readonly<{ type: "failed"; code: NarrationErrorCode }>;

export type NarrationRequest = Readonly<{
  requestId: string;
  text: string;
  locale: AppLocale;
  voiceId: NarrationVoiceId;
  signal: AbortSignal;
  onPhase?: (phase: NarrationPhase) => void;
}>;

export interface NarrationService {
  readonly available: boolean;
  speak(request: NarrationRequest): Promise<NarrationOutcome>;
  stop(requestId: string): Promise<void>;
}
```

`speak()` 返回 ended 指**本次实际音频播放自然结束**，不是模型合成完成、开始播出或请求被接受。停止返回 cancelled；失败返回稳定 code，UI 在渲染时翻译。异常实现仍应在调用边界兜底，不让旁白异常升级为游戏状态损坏。

`stop(requestId)` 只停止对应请求，旧页面的清理不能停止后来页面的新声音。它完成时必须保证该请求已经没有可听输出，并使尚未返回的合成结果失效；不承诺底层 SDK 已强制中断计算。处理状态见第 23 节。

诊断 context 可复用现有类型增补到正式契约，不在此复制第二套 operationId 系统。接口没有 `saveId`、GameState、目标动作或“成功奖励”。

### 22.3 全局单实例、单旁白，不建调度框架

在 app 装配处创建一个 NarrationService，注入需要朗读的 Story/教程宿主。最多一个旁白请求占用活动槽；新页面先取消自己的旧请求，再发新请求。不累积后台自动播报队列，不让已经离开的步骤晚几秒突然说话。

若底层计算尚未退出，新请求可以返回 BUSY 并保留文字；不要为“无缝切换”而起无限多个模型实例或等待队列。提示很短时，单模型与单任务已足够作为首版验证方案。

服务可由几个函数与闭包实现，不要求新增依赖注入容器、状态库或基于事件总线的 AudioManager。

### 22.4 合成和播放的内部边界

建议首版使用**Rust/sherpa 本地合成 + 前端统一播放**，便于与现有 WebView 麦克风和 UI 生命周期协调。实现团队需要先验证音频返回与播放链路，而不是默认 SDK 的生成回调就等于扬声器发声。

```text
有效 NarrationRequest
    ↓
受控 Tauri 合成入口（指定逻辑音色，不接受任意模型路径）
    ↓
非 UI 路径内加载/复用 TTS，生成一段有界音频
    ↓
音频数据 + 真实采样率
    ↓
再次检查 requestId / 归属 / abort
    ↓
音频播放器开始，收到实际结束或停止事件
    ↓
对应 NarrationOutcome
```

音频可以用 Web Audio 播放有界 PCM；选用 HTMLAudio 时也必须处理加载、play() 拒绝和结束事件。**只选一种正式播放路径**，不同时建设原生和前端两套播放器。

音频边界示意：

```ts
export type SynthesizedSpeech = Readonly<{
  samples: Float32Array;
  sampleRate: number;
}>;
```

这是解码后的应用内契约，不声称普通 JSON IPC 会自动保持 Float32Array。桥接需要明确序列化/二进制返回方式、字节长度和有限数值校验；不要把整章浮点数组无限制塞进 IPC，也不要复用 KWS 的 sampleRate: 16000 常量冒充 TTS 实际采样率。

首版使用单段短提示、整段合成后播放即可，不做低延迟流式 TTS、分片补播和精确逐字时间戳。

### 22.5 固定音色的配置边界

平台静态配置将 `system + zh-CN/en-US` 映射到选定的模型标识、模型版本、speaker ID 和固定语速。首版 UI 不提供任意 speaker/模型切换。

同一个双语模型可以服务两种语言，也可以由两个经过试听的固定配置承载同一逻辑角色。**逻辑 ID 相同不自动证明跨语种音色一致**；确需一致时，试听同一角色的中英文台词后选择模型和 speaker。

缺少某种朗读语言的模型时，报告旁白不可用并保留文字；不要用错误语种硬读，也不要自动切系统随机声音或云服务。模型加载与业务内容有效性分开。

### 22.6 当前优先考察的 TTS 模型

| 候选 | 官方资料明确的能力 | 本项目应优先验证 |
| --- | --- | --- |
| vits-melo-tts-zh_en | 支持中英文、单一 speaker；文档列模型文件约 163 MB，英文发音依赖其 lexicon 覆盖 [T20] | 固定教程/口令是否读对；人名和英文词覆盖；总包体和首次合成开销 |
| Kokoro 中英模型 | 官方列中英模型、预置 speaker 与不同版本，v1.1 另有 int8 包入口 [T21] | 选定一个角色声音；台词、数字读法、资源总量与量化效果 |

以上是两条试听入口，不是已经完成性能测试的优劣排名。可以先验证一个候选，满足台词、资源和平台要求就停止扩选。KWS 已经较小不代表 TTS 资源也同样小；计算库、模型、发音词典和附加资源要分别计量。

使用当前锁定的 sherpa 版本验证模型/API 可用性。模型指南的最新功能不能直接作为 1.13.8 Rust 绑定已经支持的证明。需要升级时一次更新兼容的依赖/运行库清单并回归 KWS，不在同一发布目录混放不兼容 DLL。[C14][T19][T22]

---

## 23. 旁白播放、收音互斥与生命周期

### 23.1 默认使用“先播报，再主动录音”

在 actionInput 出现时，可以先显示并朗读提示；文字入口始终可用。玩家选择语音输入后，必须在游戏声音已停止的边界后才真正开始采集。

```text
到达输入步骤
    ↓
显示提示 + 可选朗读（此时不开麦）
    ↓
朗读自然结束 / 玩家停止 / 失败降级
    ↓
确认输出已静音，经过必要的尾音保护
    ↓
玩家主动开始录音
    ↓
整轮 KWS → known/unknown → 原有输入路径
```

**“合成完成”不能打开麦克风，“play() 成功”也不能当作播放完成。** HTMLMediaElement.play() 的 Promise 说明播放启动成功，自动播放也可能被拒绝；最终播放结束由播放器结束信号判断。[T23]

首版不自动为玩家录音，不因为旁白中说出目标词就发送 ActionId，也不要求为此引入回声消除算法。

### 23.2 互斥需要在服务协调处成立，而不只是禁用按钮

沿用 app 中的 VoiceInputShell / 输入服务装配，在其与 NarrationService 之间增加一个小的**根级声音占用状态**即可。它可以是同模块共享的几行协调代码，不需要全局事件总线。[C11]

建议规则：

| 请求 | 当前情况 | 行为 |
| --- | --- | --- |
| 自动旁白 | 无有效录音轮次、无其他旁白 | 可启动 |
| 自动旁白/重播 | 正在请求麦克风、采集或处理该录音轮次 | 不启动；不等稍后自动排队播 |
| 开始录音 | 旁白在 preparing / playing | 先使旁白请求失效并停止输出，再进入收音边界 |
| 开始录音 | 输出仍不能确认停止 | 不开麦；提示可停止朗读或使用文字 |
| 旧合成返回 | 已被录音或切页取消 | 丢弃，不播放，不抢占新的声音状态 |
| 取消录音 | 权限请求/SDK 调用尚未退出 | 沿用现有取消与 BUSY 约束，不伪装为彻底结束 |

先**同步预留输入轮次**，再 await 停止旁白与尾音保护，最后请求麦克风；期间阻止新的旁白。这样不会出现“刚静音，另一个异步请求又开始播，随后麦克风才打开”的竞态。采集晚到授权流仍按既有逻辑立即释放。

停止播放只解决应用可控的输出，扬声器和房间混响仍需实测。必要时增加有界的短尾音保护；参数是音频实现配置，不是游戏倒计时。不要以静音一个 UI 元素作为已证明没有回声的结论。

### 23.3 文字、摄像头、视频与音效

文字提交不必等待旁白结束。正确输入保存后离开当前步骤时，立即取消旧旁白；未知或错误仍停在本步骤，不因反馈/保存刷新自动重新朗读。

摄像头不是麦克风，不必因为存在旁白就统一禁用。后续视觉实现仍维持它自己的单轮规则。

进入录音前还应暂停/停止**可能包含口令的游戏视频、录音配音和其他语音**。首版可以使用简单的“语音采集时暂停游戏叙事声音”策略，不要求建设完整多声道混音器。普通音效是否保留由回声测试决定；游戏自己的声音不得造成有依据之外的通过或处罚。

### 23.4 每次内容进入自动播一次，不跟随 revision

自动播报的归属是**当前会话中的这一次内容位置进入**。局部 entryKey 可以由会话实例标识、ContentRef、Story ID、Step ID 和必要的进入序号组成；不使用 revision 或整个 snapshot 对象作为触发依赖。

| 事件 | 默认行为 |
| --- | --- |
| 首次进入含 narration 的步骤 | 尝试自动播放一次 |
| 普通 React 重渲染、属性反馈、same-step 保存成功 | 不再次自动播 |
| 当前输入错误/unknown | 保持文本与提示，可手动重播 |
| 手动重播 | 建立新 requestId，仍属于当前 entry |
| 切步骤/Story/Profile、关闭宿主 | 取消该 entry 的请求并丢弃迟到合成 |
| 重启恢复到此步骤 | 可重新播放；不是重复游戏效果 |
| 切换语言 | 停止旧语言；不自动重播，后续手动重播使用最新可用投影 |
| 从设置关闭旁白 | 停止声音，只改本地偏好，不修改游戏事实 |

不要将“重播过一次”写入 GameState，也不要用完成整个 Story 来代替停止声音。普通步骤的继续仍是局部 UI 行为；当前 Story 原本需要的 completeStory 提交不因为朗读而增加或减少。

React effect 清理、开发 StrictMode 或快速切页都可能取消请求。必须测试“已取消的旧自动尝试不会永久占住新 entry，也不会与新请求叠播”。自动触发标记、当前请求与取消归属分别处理，不依靠组件重新渲染时再次调用 speak() 碰运气。

### 23.5 语言切换与迟到结果

一条已发起的 NarrationRequest 固定 text、locale、voiceId 和 requestId。语言切换取消这条请求；新语言投影尚未准备好时不能把旧中文文本标成英文去合成。

回调/音频返回前比较当前归属和取消标记。取消以后返回的音频可以释放；不在后台自动补播，不改变新页面的播放状态，也不能错误地把旧请求的结束信号当成当前旁白结束。

输入命令若已经提交，语言变化依然不能撤销或重新解释 ActionId。旁白取消与 GameSession 写入是两条独立生命周期。

### 23.6 TTS 原生取消与输出停止分别处理

UI stop 必须尽快停止可听输出，取消令牌使迟到音频无效。原生推理若不能被强制中断，可以继续在原在途槽中结束，但结果不得播放，槽释放前不得无限启动新的计算。

服务 outcome 已向旧页面报告 cancelled，并不代表 SDK 调用已经返回。内部需要分别记住“请求对用户已取消”和“原生任务仍在占用资源”。不要把同一个 busy 布尔值同时当作实际播放状态与原生推理结束证明。

麦克风能否开始由**输出静音与根级输入占用约束**决定，不要求为了等待已失效、不会再播放的 TTS 计算而卡死文字流程。是否为了 CPU 预算暂缓新的合成，是平台策略，不进入 Core。

### 23.7 失败、静音和无障碍

旁白不可用、模型损坏、合成超时、输出设备失败、自动播放被拒绝，都保留当前文字并允许继续。不要反复自动重试、永久禁用录音、自动通过输入或设置处罚 Flag。

若自动播放限制导致失败，显示“点击朗读”即可；重新播放失败后仍提供文字路径。UI 不声称玩家已经听到声音，只能报告应用播放事件；系统静音无法作为听过事实。

按钮与状态继续使用现有 i18n 和可访问标签。声音不是信息唯一来源；文字提示持续可读，旁白不能覆盖输入面板的焦点或迫使屏幕阅读器与 TTS 同时朗读。

---

## 24. TTS 内容版本、资源、缓存与诊断

### 24.1 内容 Schema v4，存档 Schema v3 不变

本版建议在读取与 Validator 中增加 content v4，保留 v2/v3 语义。一个新应用可以读取旧包；一个不支持 v4 的旧应用必须明确拒绝新格式，不能静默丢字段后自称已支持旁白内容。

| 格式 | text/video/effect | actionInput | narration / narrationText |
| --- | --- | --- | --- |
| content v2 | 原有规则 | 禁止 | 禁止 |
| content v3 | 原有规则 | 允许 | 禁止 |
| content v4 | 原有规则 | 允许 | 按本版位置和引用约束允许 |

v4 不要求所有步骤都有旁白，所有新字段都由显式配置启用。旧包不注入默认 narration，不迁移成有声音的另一个内容版本。若语言包本身没有独立 schemaVersion，按它绑定的规则包版本执行对应校验。

需要同步的校验：text/actionInput 可有 narration；video/effect 首版拒绝；voiceId 属于应用已注册逻辑集合；textSource 合法；所有发布语言的被引用文本非空；多余 narrationText 拒绝；旧格式拒绝新增字段。

模型文件不存在属于运行时能力失败，不能因此把一个结构正确的 GameState 判成坏存档。模型路径与 speaker 映射属于平台资源注册，不进入游戏存档。

### 24.2 资源与部署清单

TTS 资源清单与 KWS 清单区分：模型权重、token、发音词典、必要的 phonemizer/数字读法资源、固定 speaker 配置及校验值。哪些文件实际需要由选定模型决定，不因为另一个模型示例带有某目录就全部打包。[T20][T21][T22]

可以扩展现有资源准备脚本，也可以增加独立 TTS 准备命令；命令名由团队确定。本文件不假设已有 `tts:prepare`。准备时允许显式下载，正式游戏运行默认离线，不偷偷从 CDN 或用户 Python 环境获取缺失模型。

共享运行库只有在 SDK/ABI 兼容且模型能力验证后才复用。记录 SDK、模型来源、SHA-256、voice 配置版本与最低应用兼容条件；不要复制另一个版本的 DLL 到现有目录绕过依赖校验。

发布前分别核对 SDK、模型权重、speaker/发音资源的许可和再分发要求，不能从推理库许可推定全部模型可商用。本版没有替团队完成具体模型许可审查。

### 24.3 有界资源和失败退路

输入文本长度、单次输出音频时长/字节数、原生生成时限、模型实例数和缓存总量必须有明确上限，集中在平台配置。先用项目短台词确定上限，不把整章文本切成无限后台任务。

UI 等待、IPC 超时和原生任务完成是不同事实。超时以后不自动开启第二个仍会输出的同类任务；模型卡住时可继续用文字，不将游戏会话永久置为 saving。

数据传输要记录真实采样率与通道约定，验证有限数值与长度。音频资源的释放涵盖：PCM、AudioBuffer/节点、临时 Blob URL、原生句柄和取消回调。复用一个播放器/模型不意味着让前一条声音继续占用当前页面。

### 24.4 缓存和预生成：可选，不是首版前置

首版先运行时合成短文本，不做磁盘缓存也可以。确有重复等待成本时，增加有界内存缓存；缓存键至少区分**实际文本、locale、voiceId、模型/音色配置版本、合成语速及会影响结果的文本预处理配置**。

缓存命中也必须走相同的播放归属、停止和录音互斥逻辑。删除缓存只导致重新合成，不丢失游戏进度。不得把缓存键或“音频已经生成”写入 GameState。

固定文本最终稳定后，可以在内容制作阶段用同一 TTS 模型预生成音频。将来沿用 NarrationService 或同等播放边界即可，不要求本版同时建设运行时和预生成两套交付管线。

### 24.5 诊断接入

复用现有 operationId 和诊断面板，增加有界事件，例如 `narration.requested / synthesis_started / synthesis_finished / playback_started / playback_ended / cancelled / failed`。名字可服从当前事件规范。

记录 Story/Step ID、requestId、locale、逻辑音色、模型 ID、文本长度、音频长度、耗时、稳定错误码和取消原因。默认不记录完整台词、原始音频或玩家录音，不为排查旁白问题导出完整存档。[C11]

将“模型生成失败”“播放启动被拒绝”“已取消结果被丢弃”“等待麦克风静音边界”分开，避免一个笼统“语音失败”混淆输入与输出。现有 KWS 日志与 TTS 日志通过 source/task 区分，不改写输入处罚记录。

---

## 25. TTS 实施步骤与验收矩阵

### 25.1 实施顺序

**T1：内容与界面契约。** 增加 content v4、narration/narrationText、只读投影和双语样例。使用假的旁白服务验证触发、停止、重播、语言回退，不修改 Game Core 和存档模型。

**T2：单模型本地输出。** 选择一个固定声音，确认锁定 SDK 的模型/API/部署兼容，再接本地合成和一种播放器。用真实固定台词记录音质、读法、首次/重复延迟和资源，不以静音加载测试替代试听。

**T3：接现有 KWS 的互斥边界。** 将 NarrationService 与根级 VoiceInputShell 协调，验证开麦前真正停止输出、迟到合成不复播、取消/超时不积累任务。保持 VoiceInputService 与 submitStoryInput 原有语义。

**T4：打包与恢复验收。** 覆盖干净安装、断网、模型损坏、不同设备和语言、重启恢复、相同输入点多次错误及诊断报告。原 ActionInput 回归同时保持通过；未接摄像头不阻塞本轮。

### 25.2 文档/纯函数/内容检查

| 编号 | 场景 | 必须结果 |
| --- | --- | --- |
| N01 | textSource=blocks | 按已校验块顺序提取文本，不读 DOM/UI 按钮 |
| N02 | textSource=narrationText | 用明确配置的专用文本；缺失/空白为内容错误 |
| N03 | 未配置 narration | 不合成、不播放，不注入默认台词 |
| N04 | v2/v3 出现 narration 新字段 | 内容校验拒绝；v4 才允许 |
| N05 | 未引用的多余 narrationText / 非法 voiceId | 内容校验拒绝 |
| N06 | 英文内容回退到中文 | TTS 使用中文内容 locale；玩家选定英语 KWS 不改变 |
| N07 | 未就绪的语言投影 | 不朗读 ID、不把旧文本冒充新语种 |
| N08 | 加入/删除合法旁白配置 | 相同输入命令得到相同游戏事实，不新增 save 字段 |

### 25.3 播放、取消与界面检查

| 编号 | 场景 | 必须结果 |
| --- | --- | --- |
| P01 | 进入配置步骤 | 自动尝试一次，重渲染不叠播 |
| P02 | 同一步骤错误并保存 | 不因 revision 更新重复完整旁白 |
| P03 | 正确输入后切步骤 | 旧旁白停止，旧返回不污染新步骤 |
| P04 | 模型生成完但音频未播完 | 仍是播放阶段，不声称听完 |
| P05 | 点击停止 / 退出 Profile / 卸载 | 停止可听输出，丢弃迟到结果 |
| P06 | 新旧请求乱序返回 | 只有当前有效归属可播放；旧 stop 不停新请求 |
| P07 | 切换语言 | 停止旧语言，手动重播使用最新投影，不改 checkpoint |
| P08 | 自动播放被拒绝或设备失败 | 稳定错误提示、保留文字，可重试或继续 |
| P09 | 原生超时但未退出 | 保持有界资源；文字与游戏会话不被 TTS 锁死 |
| P10 | 开发 StrictMode / 快速挂载卸载 | 不叠播、不永久丢失新 entry 的有效播放机会 |
| P11 | 从 save 恢复到有旁白的步骤 | 可重播，不重新执行输入效果或保存“听过” |
| P12 | 点击重播、缓存命中 | 仍走同样归属/取消/互斥规则 |

### 25.4 与语音输入共同验收

| 编号 | 场景 | 必须结果 |
| --- | --- | --- |
| A01 | 旁白包含“敬礼 / salute” | 未开麦时绝不产生动作提交 |
| A02 | 播放中点击录音 | 停止并确认静音后才采集；先占用输入轮次，避免竞态 |
| A03 | 已取消合成在录音中返回 | 不播放，不向 KWS 注入游戏音频 |
| A04 | 录音/授权等待时手动重播 | 不启动，不排成延后自动播报 |
| A05 | 文字在朗读中正确提交 | 原有单次保存成功后推进；朗读随离开停止 |
| A06 | 静音/失败/禁用 TTS | 仍可用文字，允许满足静音条件后主动录音 |
| A07 | 扬声器、耳机、房间尾音 | 实测自触发情况；需要的尾音保护有界且不算玩家输入 |
| A08 | 含口令的视频/其他游戏配音 | 录音前统一处理，不只停止 TTS |
| A09 | 原 KWS 单轮/取消/unknown 回归 | 行为不变；不得额外提交或自动重试处罚 |
| A10 | TTS/KWS 共用运行库后的干净安装 | 两种模型都能按声明资源加载，缺失一个可解释降级 |

自动化可以用 FakeNarrationService/FakePlayer 重现乱序和取消；真实音色、输出硬件、回声和性能必须实测。不要使用合成成功测试证明“扬声器已播完”，也不要用播放器 ended 测试证明“玩家已经听见”。

### 25.5 本轮完成标准

一个固定声音完成需要的中英文台词朗读；content v4 与旧包兼容清楚；旁白不改 GameState/revision；当前 KWS 输入不被游戏自身语音误触发；故障与取消可降级；资源与许可检查有记录；真实打包环境验收有可追溯结果。

到此即可结束本次扩展，不把模型更多能力、音色管理、语音对话、云合成、移动端和字幕时间轴并入同一工程。

---

## 附录 A：完整交互内容片段与双语示例

以下是可合并到扩展后内容包中的**完整 Story 片段**，不是独立的全量 game.json。沿用原包的 manifest、案件、属性、资源和其他内容。本附录是**无旁白的 v3 ActionInput 回归示例**；带 narration 的 v4 示例见附录 E，二者不要混用格式声明。

假设原包已声明 `authority`，范围 0–100，示例开始时当前值为 50。两个输入点都要求敬礼，但代表不同内容位置。

### A.1 `game.json` 的 `stories` 条目

```json
{
  "inspection_003": {
    "skippable": false,
    "steps": [
      { "id": "arrival", "type": "text" },
      {
        "id": "salute_at_arrival",
        "type": "actionInput",
        "targetActionId": "salute",
        "wrongEffects": {
          "attributeDeltas": { "authority": -2 },
          "setFlags": {}
        }
      },
      { "id": "inspection_notice", "type": "text" },
      {
        "id": "salute_at_departure",
        "type": "actionInput",
        "targetActionId": "salute",
        "wrongEffects": {
          "attributeDeltas": { "authority": -1 },
          "setFlags": {}
        }
      },
      { "id": "departure", "type": "text" }
    ]
  }
}
```

### A.2 中文语言包的 `stories` 条目

```json
{
  "inspection_003": {
    "title": "例行检查",
    "steps": {
      "arrival": {
        "blocks": [
          { "type": "paragraph", "text": "监察官走进办公室，在你的桌前停下。" }
        ]
      },
      "salute_at_arrival": {
        "blocks": [
          { "type": "paragraph", "text": "请向监察官敬礼。你可以做出动作、说出“敬礼”，或输入文字。" }
        ]
      },
      "inspection_notice": {
        "blocks": [
          { "type": "paragraph", "text": "监察官检查了文书，提醒你继续遵守工作规程。" }
        ]
      },
      "salute_at_departure": {
        "blocks": [
          { "type": "paragraph", "text": "检查结束。请再次敬礼，送别监察官。" }
        ]
      },
      "departure": {
        "blocks": [
          { "type": "paragraph", "text": "监察官离开了办公室。你可以继续处理文书。" }
        ]
      }
    }
  }
}
```

### A.3 英文语言包的 `stories` 条目

```json
{
  "inspection_003": {
    "title": "Routine Inspection",
    "steps": {
      "arrival": {
        "blocks": [
          { "type": "paragraph", "text": "The inspector enters the office and stops in front of your desk." }
        ]
      },
      "salute_at_arrival": {
        "blocks": [
          { "type": "paragraph", "text": "Salute the inspector. You may perform the gesture, say 'salute', or type it." }
        ]
      },
      "inspection_notice": {
        "blocks": [
          { "type": "paragraph", "text": "The inspector reviews the documents and reminds you to follow office procedure." }
        ]
      },
      "salute_at_departure": {
        "blocks": [
          { "type": "paragraph", "text": "The inspection is complete. Salute once more as the inspector leaves." }
        ]
      },
      "departure": {
        "blocks": [
          { "type": "paragraph", "text": "The inspector leaves the office. You may return to your documents." }
        ]
      }
    }
  }
}
```

### A.4 触发规则条目

```json
{
  "id": "inspection_after_003",
  "when": {
    "all": [
      { "type": "resolvedCountAtLeast", "count": 3 }
    ]
  },
  "storyId": "inspection_003",
  "order": 300
}
```

这条规则合并进 `storyRules`，而不是由 UI 数“侧边栏里有几个案件”。独立调试 Story 时，也可在测试 fixture 的 `initial.storyIds` 中显式入队，不修改正式运行逻辑。

### A.5 提交与恢复样本

```json
{
  "type": "submitStoryInput",
  "storyId": "inspection_003",
  "stepId": "salute_at_arrival",
  "actionId": "wave"
}
```

这个命令是明确错误。保存后 authority = 48，检查点如下：

```json
{
  "storyId": "inspection_003",
  "resumeStepId": "salute_at_arrival"
}
```

接下来正确敬礼，检查点变成：

```json
{
  "storyId": "inspection_003",
  "resumeStepId": "inspection_notice"
}
```

完整预期：

| 操作 | authority | 保存的恢复位置 | 是否仍有待完成输入 |
| --- | --- | --- | --- |
| 进入 Story | 50 | null，起点 | 有，arrival 输入 |
| 第一处 unknown | 50 | 不变 | 有 |
| 第一处 wave | 48 | salute_at_arrival | 有 |
| 第一处 salute | 48 | inspection_notice | 有，departure 输入 |
| 此时退出再进入 | 48 | 从 inspection_notice 恢复 | 有，不能重新处罚 arrival |
| 第二处 wave | 47 | salute_at_departure | 有 |
| 第二处 salute | 47 | departure | 没有，但还需呈现尾部 |
| 完成尾部 | 47 | null；Story 加入 completed | 没有 |

表格中的 arrival/departure 输入分别指 `salute_at_arrival` 与 `salute_at_departure`，不是同名普通文字步骤。所有案件的节点和历史裁定在这些操作中保持不变。

---

## 附录 B：最小检查点算法参考

下面的纯函数只说明“如何定位未通过的输入”和“正确之后应保存到哪里”。它不是完整 transition，不替代正式的 Schema、phase、队首校验、effects、GameSession 或存储实现。

### B.1 步骤定位

```ts
export type CheckpointStep =
  | ActionInputStep
  | Readonly<{ id: string; type: "text" | "video" | "effect" }>;

export function getResumeIndex(
  storyId: string,
  steps: readonly CheckpointStep[],
  checkpoint: StoryCheckpoint | null,
): number {
  if (steps.length === 0) {
    throw new Error("STORY_EMPTY");
  }
  if (checkpoint === null) return 0;
  if (checkpoint.storyId !== storyId) {
    throw new Error("CHECKPOINT_STORY_MISMATCH");
  }
  const index = steps.findIndex((step) => step.id === checkpoint.resumeStepId);
  if (index < 0) {
    throw new Error("CHECKPOINT_STEP_MISSING");
  }
  return index;
}

export function getNextRequiredInput(
  storyId: string,
  steps: readonly CheckpointStep[],
  checkpoint: StoryCheckpoint | null,
): ActionInputStep | null {
  const start = getResumeIndex(storyId, steps, checkpoint);
  for (let index = start; index < steps.length; index += 1) {
    const step = steps[index];
    if (step.type === "actionInput") return step;
  }
  return null;
}
```

如果返回 null，表示从已提交恢复位置起没有未通过的输入。普通尾部内容仍由播放器完成后提交 completeStory。

### B.2 正确输入后的推进

```ts
export type InputContinuation =
  | Readonly<{ type: "checkpoint"; checkpoint: StoryCheckpoint }>
  | Readonly<{ type: "completeStory" }>;

export function continuationAfterCorrectInput(
  storyId: string,
  steps: readonly CheckpointStep[],
  checkpoint: StoryCheckpoint | null,
  inputStepId: string,
): InputContinuation {
  const required = getNextRequiredInput(storyId, steps, checkpoint);
  if (required === null || required.id !== inputStepId) {
    throw new Error("STALE_STORY_INPUT");
  }
  const index = steps.findIndex((step) => step.id === inputStepId);
  const next = steps[index + 1];
  if (next === undefined) return { type: "completeStory" };
  return {
    type: "checkpoint",
    checkpoint: { storyId, resumeStepId: next.id },
  };
}
```

正式处理器先校验队首和动作集合，再比较 required.targetActionId。只有相等时调用上述推进；否则应用 wrongEffects，并把 checkpoint 设到 required.id。这里返回的 completeStory 是内部推进结果，不是要求 UI 再发送第二条 GameCommand。

### B.3 处理器的业务顺序

```text
validate state / queue head / command IDs
       ↓
required = next input at or after committed checkpoint
       ↓
if no required or stepId differs → reject as stale/invalid
       ↓
if actionId == required.targetActionId:
    derive continuation
    update checkpoint, or finish current Story atomically
else:
    apply allowed wrongEffects
    checkpoint = current input
       ↓
validate nextState
       ↓
return nextState and optional transient feedback
```

辅助函数不判断视频是否已播放、文本是否已阅读。命令只允许越过下一处输入事实，不能跳到后续输入。现有 UI 负责正常播放顺序；这是内容播放器，不是防篡改系统。

### B.4 为什么无需全量步骤持久化

检查点只在 known 提交或 Story 完成时改变。纯演出本地步骤仍可前进；退出时最多重播最近检查点以来的无副作用内容。

已经通过的输入永远位于检查点之前，已施加的处罚已经包含在属性/Flag 里。因此恢复不需要回放命令日志，也不需要重新跑识别或计算历史效果。

---

## 附录 C：实施决策与禁止扩大的范围

### C.1 默认决策汇总

| 决策 | 采用方案 |
| --- | --- |
| 功能概念 | 内容输入交互点，命名 actionInput |
| 规则宿主 | 现有 GameStoryStep，不增加平行挑战单元 |
| 全局流程 | 复用 pendingStoryIds / completedStoryIds |
| 可重复内容 | 有限规律位置使用不同 Story ID，可复用动作与模板 |
| 输入输出 | known(ActionId) / unknown |
| 字典语言 | 普通配置按选定语种读取，不走 t() 或内容表现投影 |
| 成功效果 | 通过当前位置，无默认奖励 |
| 错误效果 | 可选属性/Flag，沿用公共效果函数 |
| unknown | 本地提示，不提交存档 |
| 恢复 | 单个 storyCheckpoint，不建 ChallengeProgress 表 |
| 防重 | 局部一次消费 + generation 取消 + 已有会话锁/步骤校验 |
| 宏观推进 | 默认不自动重排案件解锁与结局判断时点 |
| 语音 | **优先 sherpa-onnx KWS，已接入；同源词典生成、整轮归一化，不强制转写** |
| TTS 供应 | 优先 sherpa-onnx，另选固定声音模型；不复用 KWS 模型 |
| 旁白宿主 | text/actionInput 的可选 narration；不是独立游戏步骤或任务 |
| 旁白文案 | 当前内容语言 blocks 或同一步骤 narrationText |
| 播报/收音 | 默认互斥；播放已结束/停止且尾音边界满足后才开麦 |
| 旁白恢复 | 当前界面临时状态；重启可重播，不保存“听过”或音频秒数 |
| 版本 | save v3 不变，含旁白的新内容使用 v4；旧包精确绑定不变 |
| 视觉 | 先考察 Pose/必要的 Hand 关键点 + 简单规则 |
| 无设备 | 文字替代，不扣状态，不自动通过 |

### C.2 必须同时修改的契约

ActionInput 已形成的 Schema、命令、检查点、反馈和仓储边界继续以正式代码为准。TTS 本版必须同步的是：内容 Schema/版本加载/Validator、语言步骤 narrationText、GameContentView、StoryPlayer/教程宿主、旁白请求与平台适配、录音互斥、错误文案、资源清单和对应测试。

**不因 TTS 再改 GameState、GameCommand 或 SQL save_schema_version。** 内容 v4 的解析类型可被 Core 引用，但旁白不得参与规则判定。不能只改 TypeScript type 却忘记严格 Zod Schema，也不能把模型加载失败误判为整局存档损坏。

### C.3 不得自行扩大

不得因为“技术上可以”而追加独立挑战任务、动作连招、限时失败、误识别惩罚、永久尝试日志、所有平台同时上线、自动云端回退或音视频长期记录。TTS 同样不扩大为语音助手、克隆音色、自由对话、强制完整聆听或多后端调度平台。

任何把动作输入接入案件选项图、把识别 SDK 引入 Core、在动画结束回调里扣属性、将本地化字符串写入存档的实现，均违背本扩展边界。

### C.4 文档与实施状态

本版确认文字和 sherpa KWS 已进入仓库的 ActionInput 主链路；识别质量、实际设备与发布安装器以实施团队的验收记录为准。TTS 相关配置、服务、示例和内容 v4 是本版新增设计，不意味着已经进入仓库。[C11][C15]

类型与独立样例检查只证明文档自身的一致性，不替代正式 Zod Validator、Game Core 集成测试、Tauri 构建或音频设备验收。本次未改仓库、未执行任何模型推理或真人音频测试。

**本版独立自检：** 15 个 TypeScript 代码块在补入最小 AppLocale、TextBlock 及已有语音请求类型声明后通过 strict 类型检查；13 个 JSON 示例通过语法解析；检查点辅助函数、旁白文本投影及双语示例 ID 对齐共通过 42 项独立示例测试。参考编号与 Markdown 结构已检查。上述结果不包含正式项目 Validator、模型、音频播放器、收音互斥集成或实际安装包测试。

---

## 附录 D：代码依据及官方参考索引

### D.1 代码基线

以下链接固定到本次确认的提交，不随 main 后续变化。已实现的 ActionInput/KWS 与本版待实现的 TTS 要分开看；本次读取了最新语音实施记录、输入公共契约和 Cargo 依赖，并沿用前一轮已核对的 Core/UI 代码依据，不是新一轮完整 CR。

| 编号 | 资料 | 用途 |
| --- | --- | --- |
| C1 | [content/schema.ts](https://github.com/Deckard-YYZ/Judge-of-Eagleland-official-app/blob/169eee41ab2944cb577fa008952b4c5471e8c74b/src/content/schema.ts) | 当前 v2/v3 兼容规则/语言 Schema、StoryStep、效果与条件 |
| C2 | [game/commands.ts](https://github.com/Deckard-YYZ/Judge-of-Eagleland-official-app/blob/169eee41ab2944cb577fa008952b4c5471e8c74b/src/game/commands.ts) | 现有命令、反馈、纯 transition 契约 |
| C3 | [application/gameSession.ts](https://github.com/Deckard-YYZ/Judge-of-Eagleland-official-app/blob/169eee41ab2944cb577fa008952b4c5471e8c74b/src/application/gameSession.ts) | 统一提交、锁、保存后发布与不确定写入恢复 |
| C4 | [gameSessionView.ts](https://github.com/Deckard-YYZ/Judge-of-Eagleland-official-app/blob/169eee41ab2944cb577fa008952b4c5471e8c74b/src/application/gameSessionView.ts)；[gameContentView.ts](https://github.com/Deckard-YYZ/Judge-of-Eagleland-official-app/blob/169eee41ab2944cb577fa008952b4c5471e8c74b/src/application/gameContentView.ts) | locale 加载、只读表现投影与规则隔离 |
| C5 | [storage/sqliteSaveRepository.ts](https://github.com/Deckard-YYZ/Judge-of-Eagleland-official-app/blob/169eee41ab2944cb577fa008952b4c5471e8c74b/src/storage/sqliteSaveRepository.ts) | 整局 JSON、归属与 revision 条件写入、版本条件 |
| C6 | [ui/presentation/story/StoryPlayer.tsx](https://github.com/Deckard-YYZ/Judge-of-Eagleland-official-app/blob/169eee41ab2944cb577fa008952b4c5471e8c74b/src/ui/presentation/story/StoryPlayer.tsx) | 当前本地步骤与整段 Story 完成边界 |
| C7 | [game/resolution.ts](https://github.com/Deckard-YYZ/Judge-of-Eagleland-official-app/blob/169eee41ab2944cb577fa008952b4c5471e8c74b/src/game/resolution.ts) | 属性效果、解锁、剧情与结局的现有顺序 |
| C8 | [Localization Implementation](https://github.com/Deckard-YYZ/Judge-of-Eagleland-official-app/blob/169eee41ab2944cb577fa008952b4c5471e8c74b/Desktop_Case_Game_Localization_Implementation.md) | 已实施的显示/规则分离及事实存档 |
| C9 | [game/progression.ts](https://github.com/Deckard-YYZ/Judge-of-Eagleland-official-app/blob/169eee41ab2944cb577fa008952b4c5471e8c74b/src/game/progression.ts) | Story 规则排序与按 ID 去重 |
| C10 | [game/model.ts](https://github.com/Deckard-YYZ/Judge-of-Eagleland-official-app/blob/169eee41ab2944cb577fa008952b4c5471e8c74b/src/game/model.ts)；[storage/migrateSave.ts](https://github.com/Deckard-YYZ/Judge-of-Eagleland-official-app/blob/169eee41ab2944cb577fa008952b4c5471e8c74b/src/storage/migrateSave.ts) | 当前存档 v3、检查点与既有迁移边界 |
| C11 | [Desktop_Voice_Sherpa_Implementation.md](https://github.com/Deckard-YYZ/Judge-of-Eagleland-official-app/blob/169eee41ab2944cb577fa008952b4c5471e8c74b/Desktop_Voice_Sherpa_Implementation.md) | KWS 决策、同源词典、部署参数、取消与人工待测范围 |
| C12 | [game/storyInput.ts](https://github.com/Deckard-YYZ/Judge-of-Eagleland-official-app/blob/169eee41ab2944cb577fa008952b4c5471e8c74b/src/game/storyInput.ts) | 已知动作的纯判定、错误效果与原子检查点 |
| C13 | [ActionInputPanel.tsx](https://github.com/Deckard-YYZ/Judge-of-Eagleland-official-app/blob/169eee41ab2944cb577fa008952b4c5471e8c74b/src/ui/presentation/story/ActionInputPanel.tsx)；[voice_input.rs](https://github.com/Deckard-YYZ/Judge-of-Eagleland-official-app/blob/169eee41ab2944cb577fa008952b4c5471e8c74b/src-tauri/src/voice_input.rs) | 统一接收/提交和原生 KWS，二者不相互越界 |
| C14 | [Cargo.toml](https://github.com/Deckard-YYZ/Judge-of-Eagleland-official-app/blob/169eee41ab2944cb577fa008952b4c5471e8c74b/src-tauri/Cargo.toml) | 锁定 sherpa-onnx 1.13.8 shared 依赖及当前 Windows 接入 |
| C15 | [本次核对提交](https://github.com/Deckard-YYZ/Judge-of-Eagleland-official-app/commit/169eee41ab2944cb577fa008952b4c5471e8c74b) | 2026-09-18 新增语音测试工具，不等同真实质量验收 |
| C16 | [shared/voiceInput.ts](https://github.com/Deckard-YYZ/Judge-of-Eagleland-official-app/blob/169eee41ab2944cb577fa008952b4c5471e8c74b/src/shared/voiceInput.ts) | 现有输入服务/推理契约，取消与停止信号、有界 PCM |

### D.2 官方技术参考

本版 sherpa KWS/TTS 与播放资料核对日期：**2026-09-18**；沿用视觉及历史语音资料的原核对日期为 2026-09-16。表中链接是阅读入口，不是已安装版本或已测能力证明。

| 编号 | 官方资料 | 优先阅读内容 |
| --- | --- | --- |
| T1 | [MediaPipe Pose Landmarker — Web](https://developers.google.com/edge/mediapipe/solutions/vision/pose_landmarker/web_js)；[任务概述](https://developers.google.com/edge/mediapipe/solutions/vision/pose_landmarker) | 人体关键点输出、Web 视频接入；敬礼规则的优先底层参考 |
| T2 | [MediaPipe Hand Landmarker — Web](https://developers.google.com/edge/mediapipe/solutions/vision/hand_landmarker/web_js) | 需要细分手部姿态时的补充，而不是首版无条件同时加载 |
| T3 | [MediaPipe Gesture Recognizer — Overview](https://developers.google.com/edge/mediapipe/solutions/vision/gesture_recognizer) | 预置类别、输出；核对哪些动作并非现成功能 |
| T4 | [MediaPipe Gesture Recognizer — Web](https://developers.google.com/edge/mediapipe/solutions/vision/gesture_recognizer/web_js) | 摄像头示例、模型/WASM、同步推理与 Worker 提示 |
| T5 | [MediaPipe 手势模型自定义教程](https://developers.google.com/edge/mediapipe/solutions/customization/gesture_recognizer) | none 类与训练流程参考；页面已提示 Model Maker 不再积极维护 |
| T6 | [ggml-org/whisper.cpp](https://github.com/ggml-org/whisper.cpp) | 历史替代参考，**不作为当前优先或并行交付项** |
| T7 | [whisper.cpp 模型说明](https://github.com/ggml-org/whisper.cpp/blob/master/models/README.md) | 多语言与 .en 模型区别、模型选择与资源 |
| T8 | [whisper.cpp/examples/command](https://github.com/ggml-org/whisper.cpp/blob/master/examples/command/README.md) | 麦克风命令示例；特别核对 guided mode 与 unknown 要求 |
| T9 | [sherpa-onnx 官方文档](https://k2-fsa.github.io/sherpa/onnx/)；[官方仓库](https://github.com/k2-fsa/sherpa-onnx) | 本地语音能力与运行时接入 |
| T10 | [sherpa-onnx 预训练模型](https://k2-fsa.github.io/sherpa/onnx/pretrained_models/index.html) | 按任务和语种选择模型，不假设全部模型等价 |
| T11 | [sherpa-onnx WebAssembly](https://k2-fsa.github.io/sherpa/onnx/wasm/index.html) | Web/WASM 构建和中英示例入口 |
| T12 | [sherpa-onnx Keyword Spotting](https://k2-fsa.github.io/sherpa/onnx/kws/index.html) | 自定义关键词、token 转换、阈值与误触发权衡 |
| T13 | [KWS 预训练模型与中英示例](https://k2-fsa.github.io/sherpa/onnx/kws/pretrained_models/index.html) | 包含中英模型 `sherpa-onnx-kws-zipformer-zh-en-3M-2025-12-20` 的运行和关键词配置资料 |
| T14 | [MDN — SpeechRecognition](https://developer.mozilla.org/en-US/docs/Web/API/SpeechRecognition) | 浏览器兼容、服务端识别说明、语种和结果接口 |
| T15 | [MDN — getUserMedia](https://developer.mozilla.org/en-US/docs/Web/API/MediaDevices/getUserMedia) | 授权、安全上下文、设备错误与等待行为 |
| T16 | [Tauri — Webview Versions](https://v2.tauri.app/reference/webview-versions/) | 各目标系统 WebView 差异 |
| T17 | [Tauri — Embedding Additional Files](https://v2.tauri.app/develop/resources/) | 本地模型和必要资源的打包/解析基础 |
| T18 | [Tauri — Embedding External Binaries](https://v2.tauri.app/develop/sidecar/) | 原生识别 sidecar、目标架构、调用与发布配置 |
| T19 | [sherpa-onnx TTS 总览](https://k2-fsa.github.io/sherpa/onnx/tts/index.html) | 当前 TTS 模型/使用入口；任务和模型独立于 KWS |
| T20 | [VITS / Melo 中英模型](https://k2-fsa.github.io/sherpa/onnx/tts/pretrained_models/vits.html#vits-melo-tts-zh-en-chinese-english-1-speaker) | 单声音中英候选、资源与英语发音词典限制 |
| T21 | [Kokoro TTS 模型](https://k2-fsa.github.io/sherpa/onnx/tts/pretrained_models/kokoro.html) | 中英候选、预置 speaker、量化版本和附加资源 |
| T22 | [sherpa-onnx TTS FAQ](https://k2-fsa.github.io/sherpa/onnx/tts/faq.html) | 安装、模型入口与词表外发音等实现问题 |
| T23 | [MDN — HTMLMediaElement.play](https://developer.mozilla.org/en-US/docs/Web/API/HTMLMediaElement/play) | 播放启动 Promise、自动播放拒绝；不是播放完毕事件 |

**实施落点：** 保留已接入的文字与 sherpa KWS；在相同内容位置新增可选固定声音旁白。输入模型决定如何识别玩家，TTS 模型决定如何发声，内容决定时机与文字，Game Core 只决定游戏事实。旁白供应更换不应牵动案件、输入命令或存档。


---

## 附录 E：带旁白的双语内容与纯投影参考

### E.1 示例范围

以下是合并到新内容包中的完整 Story 条目，不是完整 game.json。假设原包已经声明 authority，目标动作 salute 已可通过文字和 KWS 完成。新包使用 contentSchemaVersion=4；package version 可采用新的 1.2.0，**这是建议版本，不代表仓库已发布该包**。旧 1.0.0/1.1.0 仍保留。

示例的三步是：读出教程说明 → 朗读短口令并要求输入 → 展示结束文字。通过输入保存的是 after_order 检查点，不保存第一步或口令是否被听过。

### E.2 规则内容的 stories 条目

```json
{
  "tutorial_voice_order": {
    "skippable": false,
    "steps": [
      {
        "id": "intro",
        "type": "text",
        "narration": {
          "voiceId": "system",
          "textSource": "blocks"
        }
      },
      {
        "id": "order",
        "type": "actionInput",
        "targetActionId": "salute",
        "narration": {
          "voiceId": "system",
          "textSource": "narrationText"
        },
        "wrongEffects": {
          "attributeDeltas": { "authority": -2 },
          "setFlags": {}
        }
      },
      { "id": "after_order", "type": "text" }
    ]
  }
}
```

### E.3 中文语言内容的 stories 条目

```json
{
  "tutorial_voice_order": {
    "title": "输入教程",
    "steps": {
      "intro": {
        "blocks": [
          { "type": "paragraph", "text": "接下来请按照提示完成一个简单输入。" }
        ]
      },
      "order": {
        "blocks": [
          { "type": "paragraph", "text": "请敬礼。你可以直接输入“敬礼”，或在朗读结束后主动开始录音。" }
        ],
        "narrationText": "请敬礼。"
      },
      "after_order": {
        "blocks": [
          { "type": "paragraph", "text": "输入已完成。你可以继续。" }
        ]
      }
    }
  }
}
```

### E.4 英文语言内容的 stories 条目

```json
{
  "tutorial_voice_order": {
    "title": "Input Tutorial",
    "steps": {
      "intro": {
        "blocks": [
          { "type": "paragraph", "text": "Follow the next prompt to complete a simple input." }
        ]
      },
      "order": {
        "blocks": [
          { "type": "paragraph", "text": "Please salute. Type 'salute', or start recording after the narration stops." }
        ],
        "narrationText": "Please salute."
      },
      "after_order": {
        "blocks": [
          { "type": "paragraph", "text": "Input complete. You may continue." }
        ]
      }
    }
  }
}
```

两个语言条目的 Story/Step 集合严格一致；只有 order 带专用 narrationText，因为只有它显式选择该来源。音色 ID 和 source 不重复写入语言包；ActionId 不从语音文本推断。

### E.5 纯文本投影参考

以下函数只是第 21 节的纯投影参考，沿用前文 NarrationDefinition、ContentNarrationView 和现有 AppLocale/TextBlock 类型。正式输入仍必须先过内容 Schema、版本与语言覆盖校验；函数不是完整 Validator 或 TTS 实现。

```ts
export type LocalizedNarrationSource = Readonly<{
  blocks: readonly Readonly<TextBlock>[];
  narrationText?: string;
}>;

export function projectNarration(
  definition: NarrationDefinition | undefined,
  source: LocalizedNarrationSource,
  actualContentLocale: AppLocale,
): ContentNarrationView | undefined {
  const usesDedicatedText = definition?.textSource === "narrationText";
  if (!usesDedicatedText && source.narrationText !== undefined) {
    throw new Error("NARRATION_TEXT_UNUSED");
  }
  if (definition === undefined) return undefined;

  const text = usesDedicatedText
    ? source.narrationText?.trim()
    : source.blocks.map((block) => block.text.trim()).filter(Boolean).join("\n");

  if (text === undefined || text.length === 0) {
    throw new Error("NARRATION_TEXT_MISSING");
  }

  return Object.freeze({
    voiceId: definition.voiceId,
    locale: actualContentLocale,
    text,
  });
}
```

该函数不读取 t()、输入词典、GameState 或模型。它也不计算“玩家应该说什么”；那仍由 Core 的 targetActionId 与既有 RecognizedAction 决定。

### E.6 一次完整流程的事实与表现

| 操作 | 声音/输入表现 | 游戏提交 |
| --- | --- | --- |
| 进入 intro | 朗读当前语言 blocks | 无 |
| 玩家点击继续 | 停止 intro；进入 order 并朗读短提示 | 无 |
| 旁白自然结束 | UI 结束播放状态；可主动开始录音 | 无 |
| 玩家录音说出 wave / 挥手 | KWS 输出已知错误动作，提交既有命令 | 保存 authority -2 和 order 检查点 |
| 同一步骤发布反馈 | 显示实际变化，不重新自动朗读 | 无额外提交 |
| 玩家手动重播提示 | 新的受控旁白请求；仍不收音 | 无 |
| 玩家通过文字或语音正确敬礼 | 仍由 submitStoryInput 判定与保存 | 检查点到 after_order |
| 随后退出重启 | 从 after_order 恢复，不再要求 order | 无额外输入/朗读事实 |
| 完成尾部文字 | 走原有 completeStory | 原有 Story 完成提交 |

错误、unknown、停止朗读、静音和模型故障均不能通过“旁白结束”替代玩家的正确动作。同一套内容关闭全部旁白后，游戏规则和可完成路径应保持一致。

### E.7 供应实现替换的边界检查

只将 sherpa TTS 替换为 FakeNarrationService，或将来替换为预生成音频播放器时，E.6 的游戏提交序列必须不变。声音可以不同、延迟可以不同，但不应改写 GameState、判断目标、增加 revision 或要求迁移 save。

只将 KWS 替换为另一个仍输出同一 RecognizedAction 的输入实现时，Core 的相同动作命令仍产生相同事实。识别质量和模型资源另行验收，不把供应选择硬编码进案件和存档。
