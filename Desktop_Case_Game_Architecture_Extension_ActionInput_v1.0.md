# 桌面案件模拟游戏架构扩展
## 多语言边界与内容输入交互点

**文档版本：** 1.0  
**日期：** 2026-09-16  
**状态：** 扩展设计与实施基线；尚未在项目中集成或验收  
**关联文档：** `Desktop_Case_Game_Architecture_v1.0.md`、`Desktop_Case_Game_Localization_Implementation.md`  
**技术基线：** React + TypeScript + Vite + Tauri 2 + SQLite  
**代码基线：** `Deckard-YYZ/Judge-of-Eagleland-official-app`，`main`，提交 `3c3776f23d2aafefcef9398bbc1852f351a00b96`。

> 本文是已有架构的增量扩展，不替换已完成的 Game Core、内容校验、存储和 UI。新增功能被定义为：**内容走到某个位置时，等待玩家做出动作、说话或输入文字的一个小交互点。**
>
> 它不是独立挑战玩法，不增加任务线、挑战队列、挑战存档、挑战等级或通用流程引擎。文档详述的是实现边界、失败行为和交接契约，不要求创建与章节数量相同的模块。
>
> “必须”表示架构约束；“首版默认”是本文为实现闭环确定的默认方案；“条件扩展”只在明确出现相应产品需求时实施。类型片段用于说明契约，正式工程仍以现有 Zod Schema 及其推导类型为源。

---

## 目录

1. 扩展范围与已确认语义
2. 现有架构基线与覆盖关系
3. 总体结构与最小改动集
4. 多语言与识别字典的边界
5. 内容中的 `actionInput` 模型
6. 触发时机与规律性出现
7. 状态归属与最小恢复检查点
8. 游戏命令、判定与原子提交
9. 输入轮次、取消与防止重复处罚
10. 文字匹配规则
11. 语音识别接入
12. 视觉动作识别接入
13. UI、内容投影与无障碍替代
14. 存档、内容版本与迁移
15. 内容与字典校验
16. 平台接入、资源与隐私
17. 目录、接口与实施分工
18. 测试、联调与验收
19. 实施顺序与完成标准
20. 技术参考与优先考察项

附录 A：完整交互内容片段与双语示例  
附录 B：最小检查点算法参考  
附录 C：实施决策与禁止扩大的范围  
附录 D：代码依据及官方参考索引

---

## 1. 扩展范围与已确认语义

### 1.1 交互点的定义

一个输入交互点属于当前正在呈现的内容。内容到达该步骤时，要求玩家完成一个指定动作，例如 `salute`。文字、语音、摄像头是完成同一要求的不同输入方式，而不是三个分别计分的玩法。

```text
前面的内容 / 播片
       ↓
actionInput：要求敬礼
       ↓
文字、语音或摄像头得到 ActionId / unknown
       ↓
正确 → 后面的内容
明确错误 → 可扣状态，继续等待
未知 → 不扣状态，继续等待
```

交互可以在固定内容位置出现，也可以按游戏进度规律出现。是否出现、何时出现，由内容和现有进度规则决定。识别器不决定剧情何时发生。

### 1.2 三种判定结果

| 结果 | 定义 | 游戏行为 |
| --- | --- | --- |
| 目标动作 | 识别得到的稳定动作 ID 等于当前目标 ID | 当前交互点通过，继续内容 |
| 其他已知动作 | 识别得到另一个受支持的动作 ID | 应用本步骤配置的错误效果，仍停在这里 |
| `unknown` | 没匹配到、证据不足、存在歧义或不属于支持的动作库 | 不修改游戏状态，继续等待 |

目标正确之前没有失败次数上限，也没有“达到次数就自动通过”的隐含规则。错误是否扣属性、扣多少、是否设置 Flag，由该内容步骤明确配置；未配置即没有惩罚。

**权限拒绝、设备不存在、识别服务失败不是错误动作。** 它们属于技术状态，不得施加游戏惩罚。

### 1.3 首版默认

| 事项 | 本文默认 |
| --- | --- |
| 成功后果 | 只通过当前输入点并继续内容，不增加奖励体系 |
| 输入方式 | 同一时间只启用一种；文字始终作为可完成游戏的替代路径 |
| 识别语言 | 跟随玩家当前选定的语种；一轮输入期间固定该语种 |
| 字典加载 | 普通应用配置模块，按语种索引；不新建翻译系统或远程 Provider |
| 输入时长 | 可设录音/采样的技术上限；到时无确定结果是 unknown，不是游戏失败 |
| 原始数据 | 不写入存档；默认不上传、不长期保存音视频和转写文本 |
| 正确或明确错误 | 经已有 GameSession 提交，保存后才发布游戏状态 |
| unknown、取消、设备错误 | 仅更新局部输入 UI，不增加存档 revision |
| 普通内容步骤 | 保持本地播放位置；不为了此功能把每个文字/视频步骤都写入存档 |
| 游戏逻辑 | 不接触媒体设备、模型 SDK、DOM、语言文案或识别置信度 |

支持语音不等于必须联网。本地语音方案列为优先考察项，但具体模型的准确率、资源占用和打包可用性需要实测，本文不将它们视为已完成能力。

### 1.4 明确不做

不做独立 `ChallengeSession`、`ChallengeRepository`、挑战排行榜、挑战结果页、成功奖励系统、任务分支图、全局事件总线或事件溯源。

不默认增加永久错误次数、完整尝试历史、无限增长的防重日志；不将案件伪装成挑战，也不将挑战伪装成一个案件。

不为这次扩展更换状态管理方案、重新拆分 SQLite 表、引入 Unity/Godot，或重做已经闭环的本地化。

---

## 2. 现有架构基线与覆盖关系

### 2.1 已落实且应保留的边界

代码基线中的核心链路是：`GameSession` 协调规则计算和条件提交，`transition` 为纯规则入口，SQLite 保存整局 JSON，React 通过只读会话快照展示已提交状态。多语言通过规则内容与语言内容分离实现；历史保存稳定 ID 与实际变化量，而不是本地化文案。[C1][C2][C3][C4][C5]

本次扩展沿用这些机制，不另起一套输入玩法的会话和持久化。

| 已有机制 | 扩展后的使用方式 |
| --- | --- |
| `pendingStoryIds` / `completedStoryIds` | 继续安排内容出现及内容完成，不增加平行队列 |
| `GameStoryStep` | 新增 `actionInput` 分支 |
| `GameSession.dispatch()` | 提交已知动作对应的事实变化 |
| `GameState` | 只补充一个当前内容的恢复检查点 |
| `ResolutionEffectsSchema` 中的属性/Flag 结构 | 抽为可复用的小型效果契约，案件与输入共用 |
| `FeedbackRequest` | 支持“来源是内容输入点”，不再强制要求 caseId |
| `GameSessionView` / `GameContentView` | 投影交互说明和可展示的输入信息 |
| 现有 i18n | 只增加实际需要的按钮、状态和错误提示 |

### 2.2 现有代码与本次改动直接相关的事实

当前 `GameStoryStepSchema` 只有 `text / video / effect`。当前 `StoryPlayer` 的步骤索引是 React 本地状态，整个 Story 完成时才提交。因此，把产生状态后果的输入步骤放进去，需要补充跨重启的检查点，但不需要新建独立玩法。[C1][C6]

当前 `GameCommand` 主要包含 `startCase / chooseOption / completeStory`。当前反馈结构要求 `caseId`，效果应用位于案件结算代码。这些是应当小幅扩展的公共契约，不应靠虚假案件 ID 绕过。[C2][C7]

### 2.3 对原 v1.0 文档的增量覆盖

| 原文档内容 | 本文的覆盖规则 |
| --- | --- |
| 历史结果快照包含显示内容的早期描述 | 以已经实施的 v2 事实存档为准，不恢复旧文案快照 |
| Story 步骤只有文字、视频、特效 | 增加 `actionInput`，不改变案件选项图 |
| 未完成 Story 一律从头恢复 | 纯演出不变；有输入事实时，从最近提交的输入检查点恢复 |
| `completeStory` 只检查队首 | 还必须检查该 Story 是否仍有未通过的输入点 |
| 属性反馈依附案件 | 增加内容输入来源，不创建虚假 caseId |
| 只有案件结算修改属性 | 内容输入也可通过同一纯效果函数修改允许的属性与 Flag |
| 结局选择顺序 | 默认不改；需要输入影响尚未确定的结局时另作内容时序约定，见第 8.8 节 |

现有通过 `useSyncExternalStore` 订阅的实现可直接保留，不需要重新引入原文档曾作为可选方案提到的 Zustand。[C3][C4]

---

## 3. 总体结构与最小改动集

### 3.1 扩展后的运行链路

```text
现有内容流程到达 actionInput
               ↓
         ActionInputPanel
               ↓
    当前选择的一种输入方式
    ┌──────────┼───────────────────┐
    ↓          ↓                   ↓
文字提交    麦克风 → 转写         摄像头 → 关键点/动作识别
    └─────→ 按语种 Matcher          │
                └─────────┬────────┘
                          ↓
                 RecognizedAction
                          ↓
             本轮输入控制：一次、取消、过期丢弃
                   ┌──────┴───────┐
                   ↓              ↓
                unknown         known
               局部提示           ↓
                         GameSession.dispatch
                                  ↓
                     Game Core：比较目标、计算效果
                                  ↓
                  完整 GameState + 临时反馈请求
                                  ↓
                         现有 SQLite 提交
                                  ↓
                   保存成功后更新步骤和属性
```

### 3.2 最少新增的概念

首版只新增以下概念：一个稳定的 `ActionId` 集合、一份按语言分组的文字匹配字典、一个 `actionInput` 内容步骤、一条已知动作提交命令、一个内容恢复检查点，以及三个输入入口。

“输入控制器”可以是一个 hook 加几个无框架函数，不需要复杂 class、依赖注入容器或任务运行时。识别实现通过构造参数或 props 装配，沿用当前项目做法。

### 3.3 必须保持的依赖方向

| 层 | 负责 | 不负责 |
| --- | --- | --- |
| `shared` | 无框架的语言与动作 ID 类型 | 翻译、识别、游戏状态 |
| `content` | 目标动作、错误效果、步骤顺序、显示内容与引用校验 | 相机、录音、玩家状态 |
| `game` | 已知动作与目标比较、效果、检查点、内容完成 | 字符串识别、模型、权限 |
| `application` | 统一提交及必要的输入提交桥接 | 绕过 Core 修改属性 |
| `input` | 字典 Matcher、识别结果归一化、适配器契约 | 保存数据库、决定惩罚 |
| `ui` | 设备入口、输入提示、局部轮次、展示已提交结果 | 直接结算状态、修改案件 |
| `platform` | 媒体设备、Worker/原生推理、必要的 Tauri 能力 | 案件或输入点业务规则 |

不要求新增一个 `input/` 目录就移动所有已有代码。只要依赖方向清楚，可按项目当前组织方式合并文件。

---

## 4. 多语言与识别字典的边界

### 4.1 既有多语言主链路不变

UI 自身文字继续走 `I18nProvider`；案件、剧情等显示内容继续由 `GameSessionView` 加载当前语言内容并投影；规则与存档不保存 locale 或语言文案。现有实现已将这三部分分离。[C1][C4][C8]

新增交互说明，例如“请向监察官敬礼”，属于内容语言包。新增按钮，例如“开始录音”“再试一次”“改用文字”，属于 UI 消息目录。

### 4.2 识别字典就是普通的按语种配置

```ts
// src/shared/action.ts：与 shared/locale.ts 一样保持无框架。
export const ACTION_IDS = ["salute", "wave"] as const;
export type ActionId = (typeof ACTION_IDS)[number];

// 以下类型中的 AppLocale 直接复用现有 shared/locale.ts。
export type ActionLexicon = Readonly<
  Partial<Record<ActionId, readonly string[]>>
>;

export const actionLexicons = {
  "zh-CN": {
    salute: ["敬礼", "行礼"],
    wave: ["挥手"],
  },
  "en-US": {
    salute: ["salute"],
    wave: ["wave"],
  },
} as const satisfies Readonly<Record<AppLocale, ActionLexicon>>;
```

这不是 UI 翻译目录。Matcher 直接读取 `actionLexicons[inputLocale]`，不调用 `t()`，不读取 `GameContentView`，也不需要专门的 `RecognitionI18nProvider`。

`salute` 是稳定的游戏动作 ID，不是英文显示文案。替换它会影响规则引用，不能作为普通翻译修改。新增 `bow / kneel` 等 ID 前，应明确对应的匹配词和实际可用的识别能力，不把示例列表当作已支持功能。

词典可以先随应用代码发布。未来确需随内容包更新，再把数据来源放到现有内容加载边界中；首版不建设独立远程词库系统。

### 4.3 词典与显示内容不互相推导

不能自动把显示名称当作唯一可接受词，也不能把识别别名全部展示成内容标题。译者改写剧情句子不应改变规则的目标 ID；开发者增加词典别名也不应修改存档。

词典的校验是“动作引用、别名冲突、交付能力”校验，不是通常的文案 key 一一对应校验。不同语种可以有不同数量的别名。对于当前交付内容，所支持语言必须存在可用的文字完成路径。

### 4.4 选择语言与一轮输入

首版只使用已有语言选择入口，不再增加第二套设置页面。每次开始录音、开始视觉采样或提交文字时，捕获当前选定语种。

| 事件 | 行为 |
| --- | --- |
| 切换界面语言，尚未开始输入 | 下一轮直接使用新语种字典 |
| 录音或识别中切换语言 | 取消本轮，丢弃迟到结果，下一轮使用新语种 |
| 已经提交 known，正在保存时切换语言 | 已解析的 ActionId 命令继续提交，不重新识别；完成后用新语言显示 |
| 内容语言回退 | 只影响显示，不静默修改玩家选择的识别语种 |
| 当前语种的模型不可用 | 显示可翻译的能力错误，允许文字输入；不自动使用另一语种猜测 |

取消尚未提交的媒体任务不能撤销已经进入 GameSession 的写入。两者需要区分。

### 4.5 视觉动作与语种

摄像头通常输出相同语义的动作 ID，不需要翻译关键点。确有不同语种对应不同动作约定时，可用 `gestureProfiles[inputLocale]` 选择该语种的识别配置；仍然是普通配置选择，不进入 UI 本地化管线。

Core 始终只收到 `salute / wave / ...`。设备状态、模型支持语种、词典版本都不写进游戏事实。

---

## 5. 内容中的 `actionInput` 模型

### 5.1 最小规则结构

在现有 `GameStoryStep` 联合类型中新增分支：

```ts
export type GameEffects = Readonly<{
  attributeDeltas: Readonly<Record<string, number>>;
  setFlags: Readonly<Record<string, boolean>>;
}>;

export type ActionInputStep = Readonly<{
  id: string;
  type: "actionInput";
  targetActionId: ActionId;
  wrongEffects?: GameEffects;
}>;
```

`GameEffects` 的正式 Schema 复用当前效果结构与整数限制，不另建一套数值体系。省略 `wrongEffects` 表示错误时没有数值/Flag 后果，但仍不能通过。

首版不加 `onSuccess`：成功本身就是通过当前步骤。以后明确需要成功改 Flag，可按相同效果结构增加字段，不要预建通用事件脚本。

### 5.2 一个内容序列

```json
{
  "skippable": false,
  "steps": [
    { "id": "arrival", "type": "text" },
    {
      "id": "request_salute",
      "type": "actionInput",
      "targetActionId": "salute",
      "wrongEffects": {
        "attributeDeltas": { "authority": -2 },
        "setFlags": {}
      }
    },
    { "id": "acknowledgement", "type": "text" }
  ]
}
```

这仍然是一个 Story，不需要把输入单独包装成另一种全局 `kind: challenge`，也不需要新建 ChallengeDefinition 表。

### 5.3 表现结构

现有语言包已经按 Story ID 和 Step ID 提供 `blocks`。新增输入步骤也可以使用同样的 `blocks` 存放交互说明，无需为了这一种文字另建层次。[C1]

建议新增的只读投影：

```ts
export type ActionInputStepView = Readonly<{
  id: string;
  type: "actionInput";
  targetActionId: ActionId;
  blocks: readonly Readonly<TextBlock>[];
}>;
```

`targetActionId` 可用于判断某种输入方式是否可用、选择示例图等，不授权 UI 判定成功。`wrongEffects` 不穿过内容表现边界；是否扣状态由 Core 读取规则决定。

显示某个动作名称时，优先直接使用内容说明或现有 UI 文案；不要仅为两个动作强制加入第二套动作名称翻译目录。确需公共名称时，再在现有消息目录增加少量 key。

### 5.4 不修改案件结构

`actionInput` 不放入 `ChoiceTarget`，不当作 `resolution`，不在 `CaseProgress.history` 中写入识别结果。识别器不得选择案件选项或提交最终判决。

内容可安排在案件之间、案件处理前后或现有全局剧情位置。未来明确要求在正文某一段中间显示时，可以让另一内容宿主使用同一个输入组件与规则入口，但不需要现在增加任意位置嵌入系统。

### 5.5 跳过与完成约束

首版默认：**包含 actionInput 的 Story 整体 `skippable: false`。** Validator 拒绝相反配置，避免全局“跳过剧情”直接绕过输入要求。

纯视频的失败替代文本仍可使用；技术故障不等于输入成功。改用文字、说出同义词或使用支持的另一输入方式，都是完成同一个输入要求，而不是跳过。

---

## 6. 触发时机与规律性出现

### 6.1 固定内容位置

直接把输入步骤写在所需 Story 的指定位置，继续由现有 `storyRules` 或 `initial.storyIds` 安排 Story。页面 mount、组件 render、摄像头启动都不能自行触发新的游戏交互事实。

例如“完成第一个案件后，出现监察官来访并要求敬礼”，使用现有 `caseResolved` 条件将对应 Story 入队即可。该 Story 内的文字、输入、后续文字按原数组顺序显示。

### 6.2 有限游戏流程中的规律出现

现有剧情队列按 Story ID 去重，已完成的同一个 Story 不会再次入队。这个行为保留，不应简单删除完成记录来实现循环。[C9]

对于首版有确定长度的流程，例如“每处理三案检查一次，共九案”，推荐仍用现有规则表达：

```text
完成案件数达到 3 → inspection_003
完成案件数达到 6 → inspection_006
完成案件数达到 9 → inspection_009
```

每个出现位置有不同 Story ID，内部可以使用相同 `targetActionId: salute` 和相同模板结构。三次出现不是三个独立玩法，只是三个内容位置。

```json
[
  {
    "id": "inspection_at_003",
    "when": { "all": [{ "type": "resolvedCountAtLeast", "count": 3 }] },
    "storyId": "inspection_003",
    "order": 300
  },
  {
    "id": "inspection_at_006",
    "when": { "all": [{ "type": "resolvedCountAtLeast", "count": 6 }] },
    "storyId": "inspection_006",
    "order": 600
  }
]
```

规则与内容可手写；数量多时用一个简单的构建脚本展开模板和唯一 ID。脚本产物仍是现有 `game.json` 与语言包，不需要建设运行时调度器或完整内容编译器。

### 6.3 模板、动作与出现位置不是同一个 ID

| 概念 | 示例 | 是否代表一次独立出现 |
| --- | --- | --- |
| 动作 ID | `salute` | 否，可在任意内容中重复使用 |
| 内容模板 | “监察检查” | 否，可以复用制作结构 |
| Story ID | `inspection_006` | 首版代表这一局的一个确定内容单元 |
| Step ID | `request_salute` | 在所属 Story 内唯一 |
| 本次输入位置 | `inspection_006 / request_salute` | 是，正确通过只影响这个位置 |

不能保存全局 `saluteCompleted = true` 并据此让后续所有敬礼要求自动通过。

### 6.4 真正无限重复或现实时间周期：条件扩展

如果产品后来明确需要“无限游戏里每三案一次，复用同一个 Story 定义”，才给**既有内容队列**增加出现实例，例如 `occurrenceId + storyId`，并用单调游戏进度序号防重复。检查点届时引用 occurrenceId，而不是建立独立挑战队列。

如果需要“游戏保持前台时每隔五分钟出现”，必须先确定计时口径、暂停/最小化、退出恢复、播片中是否延后、错过周期是否补发。不能直接在 React 内放 `setInterval` 并把每次回调当作内容已触发。本文不把这类未确定的现实时间调度加入首版。

有限进度中的周期性内容已由第 6.2 节覆盖；无限实例化与现实时间调度不是首版必做项。

---

## 7. 状态归属与最小恢复检查点

### 7.1 哪些数据放在哪里

| 数据 | 所有者 | 是否持久化到 GameState |
| --- | --- | --- |
| 当前 Story 队首、完成记录 | 现有内容流程 | 是，保持原字段 |
| 属性与 Flag 的实际值 | Game Core | 是 |
| 已通过输入点之后的恢复位置 | 内容恢复检查点 | 是 |
| 目标动作、错误效果 | 内容规则 | 否，随绑定内容版本读取 |
| 当前显示语言、输入语言选择 | App 设置 / 本轮输入快照 | 否 |
| 当前输入方式、录音提示、相机预览 | 输入 UI | 否 |
| 当前本地文字/视频步骤、播放秒数 | StoryPlayer | 否 |
| 本轮是否已消费结果、取消令牌 | 输入控制器 | 否 |
| 视频帧、音频、临时转写、关键点 | 识别适配器 | 否 |
| 错误总次数、历史每轮输入 | 首版不建模 | 不新增 |

### 7.2 新增一个检查点字段

当前队列一次只处理一个 Story，因此首版不需要按所有挑战建立进度表。建议在 GameState 中增加：

```ts
export type StoryCheckpoint = Readonly<{
  storyId: string;
  resumeStepId: string;
}>;

// 在现有 GameState 上增量增加，其他字段不变。
// storyCheckpoint: StoryCheckpoint | null
```

含义：**重新进入队首 Story 时，从 resumeStepId 开始显示；它之前的输入点已经通过。**

`null` 表示该 Story 尚无已提交检查点，从它的第一步恢复。队首 Story 仍由 `pendingStoryIds[0]` 决定；检查点中的 storyId 是归属校验，不是第二个可独立切换的“当前 Story”。

它不是实时播放游标，不跟随每次文字“继续”、视频进度或 CSS 动画变化。

### 7.3 首版的检查点更新规则

| 操作 | 保存行为 |
| --- | --- |
| 浏览普通文字/视频/特效步骤 | 不新增提交，仍是本地播放位置 |
| 到达输入点，尚未输入 | 不单独提交“开始挑战”命令 |
| unknown 或取消 | 不提交；退出后可重播最近检查点以来的普通内容 |
| 明确错误 | 一次保存惩罚及 `resumeStepId = 当前输入步骤` |
| 正确输入，后面还有步骤 | 一次保存 `resumeStepId = 紧接着的下一步骤` |
| 正确输入且已经是 Story 最后一步 | 同一次保存内完成 Story，移出队首并清空检查点 |
| 普通尾部内容完成 | `completeStory` 保存完成记录并清空检查点 |
| 队首切换为另一个 Story | 检查点必须清空，新 Story 从自身起点开始 |

选择“错误后从当前输入点恢复”，是为了不在已扣属性后还迫使玩家重看同一段前置内容。unknown 没有事实变化，不为它增加存档频率。

### 7.4 例子：成功之后不能重新要求输入

```text
步骤 A：文字
步骤 B：输入敬礼
步骤 C：视频
步骤 D：文字
```

在 B 正确输入后提交 `resumeStepId = C`。即使程序随后在 C 播片时退出，再进入也从 C 开始，不回到 B。

在 B 错误输入后，提交属性变化和 `resumeStepId = B`。再打开时仍在 B，已扣的值从存档读取，不执行“恢复时再次扣除”。

在 B 只有 unknown 时未产生提交，重启可回到 A。这是首版允许的重播范围，不代表玩家已完成事实丢失。

### 7.5 同一 Story 有多个输入点

```text
A 文字 → B 敬礼 → C 文字 → D 挥手 → E 文字
```

B 成功之后检查点到 C。D 发生错误后检查点到 D。重启时只要求 D，不重复 B。

规则层不能仅判断“该 Story 包含输入点”，必须找到**检查点及其后面的第一个 actionInput**，并只允许提交给这个输入位置。旧 B 回调不得作用于 D，即使两个输入点的目标恰好相同。

### 7.6 完成 Story 的规则

`completeStory(storyId)` 除原有校验外，必须确认检查点之后已经没有未通过的 actionInput。

检查点为 null 时从第一步开始扫描；检查点不为空时从 resumeStepId 开始扫描。若仍有输入点，返回 `STORY_INPUT_REQUIRED`，不能因 UI 发了“继续”或“跳过”而直接完成。

这不要求 Core 验证玩家是否真的看完每一段普通文字或视频。Core 保护的是不能跳过会产生游戏事实的输入要求，不是实现播放防作弊。

### 7.7 检查点不变量

必须检查：非空检查点属于当前队首；resumeStepId 在该 Story 中存在；已经完成的 Story 不能仍持有检查点；队列为空或本局 ended 时检查点必须为空。

有效写入只能把位置推进到当前输入点或其下一步，不回退到已经通过的输入点。保存中的检查点仍然是旧已提交值，候选位置不提前发布。

非法检查点是存档/内容兼容错误，不能静默回到故事起点。否则可能重复要求已经完成的输入。旧存档迁移的 null 默认值是明确的版本转换，不等于容忍任意损坏位置。

---

## 8. 游戏命令、判定与原子提交

### 8.1 识别结果契约

```ts
export type RecognizedAction =
  | Readonly<{ type: "known"; actionId: ActionId }>
  | Readonly<{ type: "unknown" }>;
```

适配器可以在自己的诊断对象里带置信度、拒识原因或时间，但提交给规则的业务值只有动作 ID。unknown 原因可用于 UI 提示，不扩展成新的惩罚分支。

### 8.2 新增一个命令

```ts
export type SubmitStoryInputCommand = Readonly<{
  type: "submitStoryInput";
  storyId: string;
  stepId: string;
  actionId: ActionId;
}>;
```

不带本地化字符串、原始音视频、玩家输入方式、模型得分、处罚数值或 `isCorrect`。正确与否必须由 Core 比较内容中的 `targetActionId`。

首版不为每轮输入增加持久化 attemptSeq；防重复主要通过一次性输入轮次和现有会话锁完成。这里不宣称对任意第三方重复投递提供分布式“恰好一次”语义，详见第 9 节。

### 8.3 规则校验顺序

收到 `submitStoryInput` 时依次检查：

1. 状态和内容满足既有不变量，本局不是 ended。
2. `storyId` 是当前队首且定义存在。
3. 命令 actionId 属于已实现的稳定动作集合。
4. 从恢复检查点计算下一个未通过的输入步骤。
5. 命令 stepId 与该输入步骤一致；否则按过期/非法输入拒绝。
6. 使用规则定义比较目标，再计算完整 nextState。
7. 再次验证不变量，经 GameSession 一次提交。

比较动作的函数只是 `actionId === targetActionId`。媒体识别难度不能被带进这个函数。

### 8.4 正确输入

没有额外奖励时，正确输入只推进检查点。若是 Story 最后一步，内部复用内容完成辅助函数，在同一个 nextState 中移除队首、追加完成记录、清空检查点，并按已有规则处理 ending → ended。

最后一步正确时，调用的是校验通过后的内部内容完成辅助函数，不是再 dispatch 一条公开 completeStory 命令；公开命令的“仍有输入就拒绝”保护不能因此被关闭。

不能先派发“正确输入成功”给 UI，再由 UI 另发一个命令保存通过位置。否则关闭进程可能让显示和存档不同步。

### 8.5 明确错误

读取当前步骤的 `wrongEffects`，使用公共效果函数更新属性/Flag。属性继续按已有 min/max 裁剪；反馈使用实际变化量，不显示超出边界的名义扣分。

把检查点设为当前输入点，保持 Story 未完成。不得改写案件节点、历史裁定或已有案件快照。

错误本身是正常的游戏结果，不是 `TransitionFailure`。保存成功后，可返回局部 `inputFeedback` 表示“识别到了其他动作，请重试”；真正的过期命令、内容非法或保存失败才走技术错误路径。

省略错误效果或属性已到下限时，仍应给出错误提示。沿用当前统一提交入口时，known 错误可以照常提交一次，即使数值恰好没变；首版不为这点优化引入复杂 no-op 提交协议。

### 8.6 unknown 与设备错误

unknown 不进入 `dispatch`，不保存 raw text、不增加 revision、不影响 Flag。UI 保持等待并允许重新输入。

设备错误不转换成一个假的动作 ID。对于 `PERMISSION_DENIED / DEVICE_UNAVAILABLE / MODEL_LOAD_FAILED` 等错误，显示稳定 code 对应的文案并提供替代输入方式。

### 8.7 效果与反馈的最小复用

当前案件结算中的属性/Flag 应用可抽为 `applyGameEffects` 纯函数。它接收 state 中对应的数值、效果与属性定义，返回新数值及实际变化量；不负责案件完成、剧情入队或结局判断。[C7]

建议的反馈来源：

```ts
export type FeedbackSource =
  | Readonly<{ type: "case"; caseId: string }>
  | Readonly<{ type: "storyInput"; storyId: string; stepId: string }>;
```

既有 `attributeFeedback` 使用该 source。错误但无属性变化时，可新增简单 `inputFeedback`，只描述 `wrongAction` 等稳定状态；它不是持久化事件日志。

同一输入点的反馈可锚定当前输入面板或属性栏，不能为复用动画填入一个不存在的 caseId。历史案件的变化快照不包含后来的输入处罚，历史回看也不重新执行这些处罚。

### 8.8 与案件解锁、全局 Flag、结局的关系

**首版默认不改变现有全局结算时机。** 输入命令只处理当前步骤及配置的属性/Flag，不自动调用完整案件结算或全局解锁流水线。

现有实现会在案件最终结算时选定结局并锁定 endingId。[C7] 因此默认语义是：

| 输入发生时点 | 对结局的含义 |
| --- | --- |
| 某次后续结局判断之前 | 已保存的属性/Flag 可被后续原有结局规则读取 |
| endingId 已经锁定之后 | 可按内容继续完成输入，属性可变化，但不重新选择结局 |
| ended 之后 | 拒绝新的游戏输入命令 |

只要仍有合法队首内容，输入可发生在 playing 或 ending 阶段；因此不会因原有“结局已锁定、普通前置剧情尚未播完”的顺序而卡死。

若策划明确要求“最后一次输入必须影响最终结局”，再将该输入安排在锁定之前，或局部增加“完成指定前置输入后再选择结局”的规则。不能隐式声称所有输入后果都会立即影响结局，也不必为了未确定需求现在全面重排推进系统。

错误 Flag 只能来自内容声明的合法 Flag，不得借它直接设置案件已完成、跳转案件节点或修改历史结果。是否允许后续全局规则读取某个 Flag，是明确的内容设计决定。

### 8.9 保存失败与操作可见性

| 情况 | 行为 |
| --- | --- |
| 成功提交 | 发布新数值/检查点，随后显示反馈或继续内容 |
| 明确未写入的保存失败 | 保留旧已提交位置和数值；提示重试，不假装已通过 |
| revision 冲突或写入结果不确定 | 按现有机制进入 needsReload；取消识别，显式重载，不自动重放输入 |
| 临时反馈监听器异常 | 不撤销已经成功的提交 |
| 播放器渲染失败 | 不从媒体回调重新施加惩罚；恢复只读已提交事实 |

这些处理复用当前 GameSession 的锁、条件写入与不确定写入恢复策略。[C3][C5]

---

## 9. 输入轮次、取消与防止重复处罚

### 9.1 一轮输入的最小定义

一轮是一次明确文字提交、一段录音，或一次摄像头采样过程。每轮最多产生一个最终 RecognizedAction，并且这个最终结果最多被消费一次。

控制器用局部 `consumed` 标记、递增 generation 和取消信号即可。不需要把这些状态写入存档。

```text
idle → capturing / recognizing → final result → consumed
                                      ↓
                            unknown：等待下一轮
                            known：提交并等待保存
```

标记 consumed 必须发生在第一次 await 之前，不能等数据库保存成功后再锁住本轮。否则同一个回调源可能在等待期间重复提交。

### 9.2 三个输入入口分别如何划分

| 方式 | 一轮结束 | 下一轮开始 |
| --- | --- | --- |
| 文字 | 点击提交或确认 Enter；输入法组合输入期间不提交 | 用户再次明确提交 |
| 语音 | 本轮录音结束并获得最终转写/拒识结果 | 再次点击录音 |
| 摄像头 | 得到一个稳定动作，或本轮采样到技术上限 | 用户明确“再试一次”，或经稳定中立姿态重新启用 |

首版可以使用显式“开始识别 / 再试一次”，不强制自动连续监听。若做自动重试，必须区分新的动作与持续保持同一个姿态。

**短暂遮挡、掉帧或置信度下降不等于用户回到了中立姿态。** 不能用几帧 unknown 自动解锁并不断处罚。

### 9.3 摄像头的最低保护

```text
用户开始本轮
     ↓
连续采样，形成一个动作判断
     ↓
锁住本轮并停止继续投递
     ↓
若 known，等待保存结果
     ↓
等待用户显式重试，或观察到明确稳定的中立姿态
```

只使用固定冷却时间不足以定义新动作。持续保持错误手势时，冷却结束后仍不应自动再扣一次。

用户显式点击“再试一次”属于重新发起输入。需要更强的动作自然交互时，可再要求先放下手；这个交互体验可以调整，但不得退回按帧计罚。

### 9.4 迟到回调与会话归属

本轮开始时捕获：当前会话实例、Story ID、Step ID、语种、输入方式和本地 generation。回调处理前确认这些归属仍然有效。

切换 Profile/存档、离开输入点、切换输入方式、改变输入语种、进入 needsReload 或销毁页面时，取消本轮并使 generation 失效。SDK 的 cancel 不能保证不再回调时，也必须在接收端丢弃结果。

不允许一个旧麦克风请求完成后，对新 Profile 的同名 Story 提交动作。仅比较 stepId 不足以识别会话切换。

### 9.5 已提交命令与媒体取消的区别

点击正确输入后已经进入 GameSession 的命令不能通过关闭相机撤销。UI 等待保存，媒体设备可以停止；最终以保存结果为准。

保存中禁用下一轮输入。不要把尚未处理的语音片段/视频结果排成提交队列，等锁释放后逐个处罚。

### 9.6 Core 的保护及首版限制

Core 的队首、Step ID 和检查点校验会拒绝已经通过步骤的旧成功结果；GameSession 的 BUSY 会拒绝重叠命令。尚停在同一输入点的两次错误，业务上可以都合法，所以 Core 不能仅凭相同 actionId 判断重复。

因此首版的防重边界明确为：**受控本地输入入口保证一轮只消费一次，统一会话不自动重放。** 不增加永久尝试日志，也不声称仅靠 BUSY 就能抵御所有迟到重复。

未来确实引入跨进程命令重放、服务器回执或可靠重试协议时，再给命令和存档增加有界幂等信息；那不是目前三个输入按钮必须承担的复杂度。

---

## 10. 文字匹配规则

### 10.1 目标是词典匹配，不是自然语言理解

首版直接检测当前语种词典中的词/短语。语音转写后使用同一函数。Matcher 不知道当前目标是谁，先识别输入对应的动作，再由 Core 比较目标。

不能先只找目标动作，找不到就当 unknown；那样永远无法识别动作库中的其他动作并执行明确错误分支。

### 10.2 推荐匹配流程

```text
输入和词典别名使用相同规范化
       ↓
查找当前语种的全部受支持别名
       ↓
收集不同的 ActionId
       ↓
0 个 ID → unknown
1 个 ID → known
多个不同 ID → unknown（歧义，不处罚）
```

同一个动作的多个别名同时出现，只得到一个 ID。不要因“敬礼”和“行礼”同时命中同一个动作就误判为歧义。

建议的规范化是 Unicode NFKC、首尾空白处理、连续空白合并，以及英文大小写统一。不要随意删除所有分隔符，把原本不连续的字母/文字拼成动作词。

英文按单词/短语边界匹配，不能把 `waveform` 中的 `wave` 当作挥手。中文可按明确别名子串匹配；不自动引入繁简转换、拼音、音近字容错或跨语种翻译。

### 10.3 包含语义与否定句

本需求是“是否包含字典中的词”，不是判断玩家的真实意图。按这个默认规则，中文“不要敬礼”仍然包含“敬礼”；英文 “do not salute” 也会匹配 `salute`。

这必须在内容和测试中明确，而不是声称系统已经理解否定。UI 应引导玩家直接说动作词或短句。若产品以后要求严格口令，可以把模式改为规范化后的整句相等，或配置有限的明确短语规则；不能在首版悄悄接入大模型语义裁决。

### 10.4 示例预期

以下基于第 4 节两个动作的示例词典：

| 语种 | 输入 | 预期 |
| --- | --- | --- |
| zh-CN | 敬礼 | known(salute) |
| zh-CN | 我现在行礼 | known(salute) |
| zh-CN | 挥手 | known(wave) |
| zh-CN | 敬礼，然后行礼 | known(salute) |
| zh-CN | 敬礼还是挥手 | unknown，多动作歧义 |
| zh-CN | salute | unknown，不查英文词典 |
| zh-CN | 不要敬礼 | known(salute)，这是字面包含规则 |
| en-US | SALUTE! | known(salute) |
| en-US | I salute you. | known(salute) |
| en-US | waveform | unknown，不匹配单词内部 |
| en-US | salute or wave | unknown，多动作歧义 |
| en-US | 敬礼 | unknown，不查中文词典 |
| 任意 | 空白输入 | unknown |

文字和语音都执行这套规则，区别只在输入字符串的来源。ASR 识别错字不能让 Matcher 临时放宽规则；增加别名应作为显式词典变更，有相应回归测试。

### 10.5 冲突与安全边界

规范化后的同一个别名不能属于两个不同动作。存在包含关系的别名也要测试多动作歧义，不能靠词典对象插入顺序选择结果。

首版采用固定字符串匹配，不让内容包携带任意正则表达式或执行代码。输入过长时可限制长度并给出提示，不能把巨大文本丢给复杂的回溯表达式。

---

## 11. 语音识别接入

### 11.1 默认路径

```text
用户点击录音
    ↓
获取本轮音频
    ↓
按选定语种执行 Speech-to-Text
    ↓
获得本轮最终文本
    ↓
复用 Text Matcher
    ↓
known / unknown
```

语音模块负责“听到了什么”，而不是“应该扣多少属性”。它可以替换实现，不影响 actionInput、GameState 或案件逻辑。

### 11.2 最小适配契约

```ts
export type PcmAudio = Readonly<{
  samples: Float32Array;
  sampleRate: number;
}>;

export type TranscriptionResult =
  | Readonly<{ type: "text"; text: string }>
  | Readonly<{ type: "noSpeech" }>;

export interface SpeechToText {
  transcribe(
    audio: PcmAudio,
    options: Readonly<{ locale: AppLocale; signal: AbortSignal }>,
  ): Promise<TranscriptionResult>;
}
```

这是适配边界示例，不要求所有实现都通过 WebView 传输 Float32Array。采集和推理都在原生层时，可由平台层内部直接传递；识别结果仍统一归一化。

sampleRate 必须描述实际数据。不能把 48 kHz 音频只改一个字段就冒充模型所需采样率。不同后端需要的通道数、采样率、编码由适配器转换，并使用固定样本测试。

### 11.3 一轮录音只产生一个游戏结果

首版推荐“开始录音 → 结束录音/说完 → 识别一次”，而不是永久监听。录音时长上限只是技术资源控制，到时无结果不得处罚。

流式转写可以显示 interim 文本，但它不能触发游戏命令。有些实现的一次 final 只代表一个语音片段结束，不代表整轮录音结束；适配器应按本轮边界聚合片段，最后只发布一次结果。

切换语言或输入方式后，旧转写即使成功也必须丢弃。不能用新的字典重新解释旧轮次的结果。

### 11.4 无语音、低证据与技术失败

无语音返回 unknown；错误设备/模型状态走单独 code。对纯噪声、媒体回声、过短采样和截断音频应优先拒识，不要为了“总有一个结果”强制选择动作。

识别器的 score/log probability 不能不经校准就当作跨模型统一的准确率。阈值是输入适配器的实现参数，不属于 game.json 中的游戏规则。

需要测试游戏自己的播片/提示音是否触发输入。首版进入录音时不并行播放含口令的内容音频，能避免一类不必要的自触发。

### 11.5 优先考察路径

**第一考察项：whisper.cpp 的本地短录音转写。** 官方仓库提供本地 C/C++ 推理、模型与命令行示例；适合作为“音频转文本 → 现有 Matcher”的验证起点。模型和运行库要单独选择并随目标平台验证。[T6][T7]

中文和英文应使用支持相应语言的模型；`.en` 模型是英语专用，不能直接拿英语演示配置代表中文可用。优先选择足够小的多语言模型做真实短口令实验，再决定是否提高模型规模，而不是默认下载最大模型。[T7]

`examples/command` 是语音命令采集和小词表交互的参考。其 guided mode 会把转写引导到指定候选命令，不能照搬成“永远从动作库挑一个”的业务判断。项目必须保留 unknown；目标以外的无关语音和噪声也要测试。[T8]

**第二考察项：sherpa-onnx 的本地 ASR。** 官方提供本地语音能力、不同模型入口与 WebAssembly 接入资料，可作为部署体积、延迟或平台适配不同的替代候选。语种与能力取决于所选模型，不是“接上库就支持全部语言”。[T9][T10][T11]

这两个是考察顺序，不是已做性能比较后的胜负判断。首版只选一个满足验收的实现，不并行建设两套正式后端和自动切换系统。

### 11.6 关键词检测：可考察，但不必先改默认流水线

sherpa-onnx 另有可配置关键词检测（KWS），能按指定词/短语输出命中或空结果，官方还提供中文、英文及中英模型资料。[T12][T13]

它与本需求接近，但接入时应满足：

- 从同一份当前语种词典生成关键词配置，不人工维护第二份语音词表。
- 注册本轮语言下所有可区分动作的词，不是只注册目标；否则无法区分其他已知动作与 unknown。
- 一轮中多个动作命中时保留歧义处理，不能先命中哪个就立即处罚。
- 检测分数/触发阈值需针对短词和相似词验证，不把“可定制关键词”理解为无需测试。

KWS 可在适配器内部直接输出 RecognizedAction，不强求伪造一份完整转写。初始主路径仍是转写后 Matcher，除非考察证明 KWS 更合适。

### 11.7 浏览器 SpeechRecognition 的位置

MDN 当前仍将 SpeechRecognition 标为兼容性有限，并说明部分浏览器实现会将音频发往服务端；不能把它当作跨 Tauri WebView、离线、所有语言均可用的保证。[T14]

它可以用于受控开发环境的快速实验。需要作为正式后端时，必须验证目标安装包、语种、离线要求和数据去向；不能仅检测全局对象存在就宣告可交付。即使某环境提供端侧识别，也要验证对应语言包与运行环境。

### 11.8 语音考察应交付什么

考察交付物应包括：固定的 SDK/模型版本，短录音测试集与逐项结果，正确口令/其他动作/无关语音的混淆情况，冷启动与已加载时延，CPU/内存与资源体积，以及离线安装包验证记录。

不要只给“能够识别一句英语”的视频。需要证明目标动作、其他已知动作和 unknown 三种结果都能安全进入同一判定链。

---

## 12. 视觉动作识别接入

### 12.1 优先使用关键点与少量规则

对于“敬礼”这种有限动作，优先考察 MediaPipe Pose Landmarker；需要更细手指姿态时再加 Hand Landmarker。它们分别提供人体和手部关键点，Web 端有官方接入示例。[T1][T2]

这提供的是可观察信息，不是现成的“敬礼正确”规则。动作条件由项目定义，在本地识别模块中输出 ActionId / unknown。

```text
摄像头帧
    ↓
人体关键点（必要时增加手部关键点）
    ↓
少量几何条件 + 短时稳定性
    ↓
候选动作 / 无明确证据
    ↓
稳定且唯一 → known
不足或冲突 → unknown
```

不需要把全套人脸、手部、人体追踪全部加载才能开始。首个样本应先验证最小关键点方案能否满足实际构图。

### 12.2 “敬礼”规则的考察方向

下面是待校准的设计方向，不是已经验证的模型或通用标准：

| 观察量 | 候选规则用途 |
| --- | --- |
| 手腕与头部侧边位置 | 判断手是否接近规定区域 |
| 肩、肘、腕关系 | 排除手只是自然放置或伸向其他方向 |
| 可选手指/手掌信息 | 根据美术与玩法定义区分近似姿态 |
| 关键部位可见性 | 遮挡或超出画面时拒识 |
| 时间上的保持/变化 | 避免偶然经过目标位置被立即判定 |

距离和角度应在一致坐标系中计算，按身体尺度归一化，避免不同分辨率和距离导致阈值失效。归一化图像的 x/y 缩放不相同时，计算像素距离/角度前处理纵横比；不能直接混合手部与人体模型各自不同的世界坐标系。

镜像预览可以只作用于显示层，规则明确使用原始帧坐标；不要显示镜像和算法镜像重复处理。首版是否左右手都接受需要明确，在测试样本中覆盖。

### 12.3 姿态与动态动作要区分

静态张掌不等于挥手，手在头附近也不自动等于敬礼。

如果动作库中的 `wave` 真正指左右挥动，应观察一段时间的轨迹，而不是把单帧 Open_Palm 直接映射为 wave。需要全身构图的鞠躬、下跪也不应假设普通桌面上半身摄像头一定能判断。

首版先支持实际能够验证的少量动作。输入组件可对当前目标判断摄像头能力；不支持该目标时禁用相机入口并说明原因，继续允许语音/文字。

### 12.4 预置 Gesture Recognizer 的用途与限制

MediaPipe Gesture Recognizer 的预置类别主要是握拳、张掌、指向、点赞等手势，并没有直接提供本项目的敬礼、动态挥手、鞠躬或下跪。它适合用来参考摄像头接入、手部类别输出和简单手势原型，不代表修改标签名就能识别另一种动作。[T3]

**不要把 Open_Palm 重命名为 salute 后声称完成敬礼识别。** 若需要同时知道手相对头部的位置，应引入必要的人体关键点，而不是仅依赖手部分类器。

### 12.5 自定义模型是备选，不是首版前置工作

如果动作数量增加、几何规则难以稳定区分，再考虑关键点分类器或自定义模型。其前提是已有可标注的动作定义、包含 unknown 的负样本，以及独立测试集。

截至本次查阅，Google 的 MediaPipe Model Maker 自定义手势教程已明确标注不再积极维护。该教程仍可作为数据组织、none 类和导出流程的参考，但不能把旧教程当作无需版本验证的优先生产训练管线；也不能把这个弃用提示误读成整个 MediaPipe Tasks 推理库已弃用。[T5]

### 12.6 置信度、稳定性与拒识

只有达到足够证据、且在支持动作中没有明显歧义时才输出 known。目标外的画面、看不见手、多个候选接近、关键部位出框等情况统一 unknown，不强行取最高分。

可从较低采样频率和短时间稳定窗口开始实验，例如每秒 10–15 次识别、数百毫秒稳定观察。这些只是待测初值，不是性能承诺、固定玩法时限或行业标准。

状态惩罚来自确定的错误动作，不来自“模型猜测最大概率属于另一个动作”。需要单独测量未知动作被错认成已知动作的情况，这比只报告已知类别准确率更直接关系到玩家是否被误罚。

### 12.7 Worker 与资源释放

MediaPipe 的 Web 识别调用是同步的，官方提示视频识别会阻塞界面线程，并建议使用 Worker 隔离。[T4] 项目应将连续推理放在输入适配边界，不进入 React render、GameSession transition 或数据库提交。

最多保持一个在途识别任务，过时帧直接丢弃，不积压“帧队列”追赶实时画面。具体 Worker、GPU delegate、图像转移和 WASM 加载方式以选定版本及真实 WebView 实验为准。

输入结束后停止不再使用的媒体轨道、取消帧回调、释放帧对象；Worker/模型可以在内存预算内缓存，但不能继续偷偷采集。保存期间不得继续把模型结果排入游戏命令。

### 12.8 视觉考察应交付什么

至少交付：支持动作及构图说明、目标动作/另一个已知动作/unknown 的样本结果、镜像与左右手策略、遮挡/光照/距离/背景测试、持续错误姿态不重复处罚的录像或自动化回放，以及目标安装包中的加载和性能记录。

不要用同一段训练视频相邻帧同时作为训练和测试，再用高分证明泛化。不同参与者、不同录制轮次的测试结果应分开记录；不将小样本烟测描述为生产准确率保证。

---

## 13. UI、内容投影与无障碍替代

### 13.1 接入现有 StoryPlayer

StoryPlayer 对 `step.type === "actionInput"` 显示 `ActionInputPanel`，继续使用原来的内容 Overlay、层级和焦点处理。

```text
StoryOverlay
    └── StoryPlayer
          ├── text → 现有文字
          ├── video → 现有播片/降级
          ├── effect → 现有特效
          └── actionInput → 新输入面板
```

输入点不单独进入 Sidebar、不打开新的游戏场景，不与另一个 ChallengeOverlay 争抢焦点。

### 13.2 面板内容

面板展示当前内容说明、输入方式切换、对应输入控件、本轮状态，以及技术错误/unknown/明确错误的必要提示。

文字入口始终可用；相机或麦克风不可用时，不扣状态，也不自动当作通过。输入方式切换只是改变如何完成同一个动作，不改变目标或惩罚。

首版不需要展示识别模型的原始得分、动作概率柱状图或持久化尝试历史。开发诊断可以有独立开关，不进入正式玩法界面。

### 13.3 继续按钮与保存状态

显示 actionInput 时隐藏或禁用普通“继续”按钮。只有正确输入且保存成功才推进；Core 的 `completeStory` 检查仍是最终保护，不能只依赖按钮禁用。

保存中显示处理中，暂停本轮采集；失败时继续保留当前输入点。业务上明确错误但保存成功时，显示实际处罚；存储失败不能显示“已经扣除”。

### 13.4 UI 步骤索引与检查点同步

本地步骤位置继续由 StoryPlayer 维护，但以已提交检查点为恢复下限。进入新的会话/Story 时，从检查点或起点初始化。

正确输入提交后，将本地显示推进到已提交的 resumeStepId；错误提交后仍保持当前输入点。不要在每次 snapshot 更新或语言变化时将本地步骤重置为 0。

在同一 Story 内，普通内容已浏览到检查点之后时，语言更新或属性反馈不得把显示位置拉回检查点。需要因重载重新初始化时，用明确的会话/加载代际触发，而不是依赖本地化内容对象的引用变化。

### 13.5 焦点与可访问性

输入点出现后，将焦点放到说明或输入入口，不自动请求硬件权限。明确错误/unknown 用合适的实时提示区域说明，不每帧播报模型状态。

文字提交尊重输入法 composition，避免中文选字时 Enter 被误认为提交。提供键盘可操作的模式切换和重试按钮。

切换语种后更新提示与 aria 文案，不关闭内容或重新结算。模型加载失败提供文字替代，不让玩家为了完成内容必须安装摄像头。

---

## 14. 存档、内容版本与迁移

### 14.1 存档仍然是一份整局 JSON

增加 `storyCheckpoint` 不需要新建 SQL 表。Profile、SaveRepository、saveId/profileId 归属、revision 条件写入及保存后发布机制保持不变。

不保存音频、视频、识别出的整句文本、界面语言或模型运行状态。玩家正确输入后保留的是“这个内容位置已经通过”的事实，不是“他说了哪一种语言”。

### 14.2 建议的版本增量

当前代码支持 save schema v2 和 content schema v2。[C1][C10] 本次正式合并时建议分别升级为 v3：

| 版本 | 为什么升级 | 增量 |
| --- | --- | --- |
| save schema v3 | 保存结构增加检查点字段 | `storyCheckpoint: null` 或合法检查点 |
| content schema v3 | 严格规则 Schema 增加新步骤类型 | `actionInput`、目标 ID、可选错误效果 |
| 内容包版本 | 新内容的规则/文本不同 | 发布新的 package version，不覆盖旧版本目录 |

文档版本、应用版本、存档 Schema 版本、内容 Schema 版本、内容包版本是不同概念，不要求一起等于 3。

### 14.3 旧存档迁移

迁移仅在 storage/load 边界执行：旧 v1 先沿用已有事实化迁移得到 v2，再由 v2 增加 `storyCheckpoint: null` 得到 v3。

保持 saveId、profileId、revision、contentRef、已有数值、案件历史、剧情完成记录与时间不变。不能因为添加输入功能而重新执行案件或剧情效果，不能把旧存档绑定到最新内容包。

旧存档没有输入检查点是明确的历史事实：其原内容版本也没有这种输入步骤。不能把 null 当作理由，将新交互直接插进它绑定的旧内容文件。

当前仓储使用显式版本检查与 SQL 条件，并写入明确的 schema version，因此必须同步更新读取、迁移、create、commit 和 SQL 版本条件；只改模型常量不够。[C5][C10]

### 14.4 旧内容版本的兼容

一局仍绑定精确 ContentRef。发布新输入内容时，旧 v2 内容包继续保留。

首版可在内容读取/校验边界支持 v2 与 v3 两种格式，Core 接收统一的运行时联合类型。v2 校验器仍拒绝 actionInput，v3 才允许新分支；不要把含新字段的文件继续标成 v2。

可使用版本分支 Schema 加一个简单适配函数，不需要长期保留两套 Game Core。适配不能更改旧 packageId/version、规则含义或语言 ID。

语言包既有步骤 `blocks` 结构可复用，但覆盖规则必须将 v3 的 actionInput 包括在需要文案的步骤集合中。加载不同格式时以各自版本规则校验。

### 14.5 内容更新与识别实现的关系

首版词典与模型配置随应用发布，不纳入存档。模型或匹配词更新会改变未来输入的识别覆盖，但不重新计算已通过位置和已扣数值。

未来内容热更不能发布应用尚不认识的 ActionId，然后期待模型自动具备该能力。新动作 ID 必须同时有规则 Schema 支持、相应输入路径和测试；仅添加新的敬礼出现位置则可以复用既有动作能力。

如果以后要求“相同存档在不同应用版本中仍使用完全相同识别词典”，再把词典版本绑定进内容兼容策略。首版不为没有提出的回放确定性保存模型和词库副本。

### 14.6 回退与损坏

较旧应用读取 v3 存档时应明确拒绝不支持的格式，不得忽略检查点后从头重播。加载损坏的 checkpoint、缺失绑定内容或未来 schema 时，走现有可见错误与恢复路径，不创建新局覆盖旧数据。

---

## 15. 内容与字典校验

### 15.1 规则内容新增检查

| 检查 | 目的 |
| --- | --- |
| actionInput 的 ID 非空且同 Story 内唯一 | 稳定恢复与命令定位 |
| targetActionId 属于受支持集合 | 防止永远无法通过的目标 |
| wrongEffects 只包含允许的属性/Flag 操作 | 不让输入步骤偷带案件操作 |
| 属性和 Flag 引用存在 | 不产生隐式新状态字段 |
| 数值合法并满足既有整数契约 | 复用现有数值规则 |
| 含输入步骤的 Story 不可整体跳过 | 避免 UI 跳过绕过事实要求 |
| 所有需要展示的输入说明有语言内容 | 沿用现有严格 ID 覆盖校验 |
| v2 包不含 v3 步骤 | 明确格式兼容边界 |

不要只检验纯 TypeScript 编译。外部 JSON 必须经过实际运行时 Schema 和引用校验。

### 15.2 字典新增检查

检查空别名、规范化后空白、非法动作 ID、规范化后不同动作的完全重名、当前发布语言是否有目标动作的文字替代路径。

一词多义是配置问题，不能让两份字典以不同插入顺序得到不同结果。包含型匹配产生的组合歧义，由固定测试验证 unknown 路径。

不要求不同语种的同义词数量、字符串长度和视觉动作定义形状相同；这不是翻译字符串一一对应系统。

### 15.3 识别能力检查与内容校验分开

构建期可以检查“这个动作在注册的实现中声明为可支持”，但不能静态证明某台用户设备一定能识别。

运行期能力检查决定某种输入方式是否可用，例如设备权限、模型是否能加载、目标是否在视觉支持集合。技术失败要可降级到文字。不要因为相机不可用就把整局存档判为非法。

### 15.4 存档不变量扩展

除第 7.7 节检查点规则外，现有属性范围、案件引用、历史 actualDelta、队列去重与阶段规则继续生效。

新增输入后，不能假设“当前属性一定等于初始值加全部案件快照之和”。属性可能合法地被内容输入改变；案件快照仍然只记录该案件当时的后果。若已有测试或辅助工具作出该假设，应明确修正，不通过给输入伪造案件快照来满足它。

### 15.5 AI 与内容制作交接

作者或 AI 可以生成 actionInput 步骤及两种语言的说明，但不能凭空创造尚未实现的动作 ID 或规则字段。

提供可用动作清单、错误效果白名单和例子，生成后走同一个 Validator。新增故事位置不需要修改 React；新增一种真正的视觉动作则是识别实现工作，不能只靠内容生成完成。

---

## 16. 平台接入、资源与隐私

### 16.1 媒体权限

摄像头和麦克风采集需要用户授权；`getUserMedia()` 有安全上下文限制，授权被拒绝、无设备或设备不可读会产生不同异常，授权请求也可能一直等待用户处理。[T15]

项目应在用户明确选择对应方式后再请求权限，并显示“授权等待中/可取消或改用文字”。不要在启动、登录或浏览案件时自动打开硬件。

对于不能真正取消的授权/采集调用，“取消”至少应使本轮结果失效。若用户稍后授权、旧调用才返回 MediaStream，应立即停止这份迟到的流，避免相机在面板关闭后继续工作。

Tauri 的 capability 配置与 WebView/操作系统的媒体授权不是同一件事。原生调用所需权限、页面的安全来源和系统的隐私设置都要验证；不虚构一个通用 `camera:allow` 插件权限当作完整解决方案。

### 16.2 浏览器开发成功不等于桌面可用

Tauri 在不同桌面平台使用不同 WebView 集成，Windows 使用 WebView2。[T16] 需要在实际首发系统的安装包中验证采集、模型、WASM/Worker、视频/音频数据和断网场景。

能力检测不止看 API 是否存在，还要实际启动一次受授权采集并完成一轮输入。设备或功能不支持时明确降级，不扩大平台承诺。

### 16.3 本地模型与资源

模型、WASM 文件、词表/分词器、配置和必要运行库分别列入构建清单，固定版本，不在发布包里引用 `@latest` CDN 地址。

首版随应用打包的识别资源可以由平台层按固定资源映射读取，复用现有资源解析习惯；不必为此把游戏媒体 asset.kind 扩展成任意可执行模型/脚本容器。Tauri 提供附加资源打包和资源目录解析机制，具体路径和访问方式按目标平台验证。[T17]

断网运行的验收应使用干净环境或清理缓存，避免模型其实来自开发缓存却被误认为已随包部署。

### 16.4 原生语音接入

whisper.cpp 等原生推理可从一个受控 Tauri 桥接调用进入，也可先通过 sidecar 做概念验证。官方 sidecar 机制需要配置外部二进制及对应目标平台的命名/构建，不能把开发电脑上的可执行文件路径直接当作发布方案。[T18]

只开放必要操作，例如提交音频进行转写、取消当前识别；UI 不得到任意执行 shell 命令或任意文件读取权限。

最初用一次性调用跑通可以，但应测量重复加载模型的成本；需要时仅在输入服务内缓存模型或进程，不引入游戏系统生命周期框架。内容、Core 和存档不依赖具体 sidecar 是否存在。

### 16.5 编码和线程

采集结果与识别引擎输入格式要显式转换。录音容器文件不能仅改后缀就当 PCM；具体 MIME/解码支持按当前 WebView 实测。媒体格式不一致属于适配器失败，不属于 unknown 动作后的处罚。

原生推理不能阻塞需要保持响应的 UI 调用路径；Web 连续视觉推理按第 12 节隔离。资源上限、最大采样长度、单个在途请求和取消逻辑是必要的工程约束，不是游戏计时机制。

### 16.6 数据最小化

默认只在内存中保留完成本轮所需的数据。结束后释放音频、视频帧和关键点；临时文件确有必要时，限制位置并在完成/取消后清理，不将它们写入 save JSON 或普通生产日志。

默认不保存用户原话，不上传摄像头画面，不以此建立人脸/声纹身份模型。如果后续选择联网语音，必须作为显式产品与隐私行为说明，不可在离线方案失败时静默上传。

开发录样本应征得参与者同意，并区分训练、验证和回归数据。发布前核对 SDK、模型权重与附加资源各自的许可和再分发要求；本文不替代具体许可证审查。

---

## 17. 目录、接口与实施分工

### 17.1 建议的增量目录

```text
src/
  shared/
    locale.ts                       已有，保持唯一语种定义
    action.ts                       新增，动作 ID / 纯类型

  content/
    schema.ts                       扩展 actionInput
    validate.ts                     新增引用与跳过约束
    ...                             沿用现有加载与精确版本机制

  game/
    model.ts                        新增 storyCheckpoint
    commands.ts                     新增 submitStoryInput / 反馈来源
    storyInput.ts                   小型纯判定与检查点推进
    storyProgress.ts                可选，抽出内容完成/下一个输入定位
    effects.ts                      从案件结算抽取公共效果应用
    transition.ts                   增加命令路由
    initialization.ts               新状态默认 null
    invariants.ts                   检查点及已有不变量
    resolution.ts                   调用共享 effects，保留案件结算语义

  input/
    contracts.ts                    识别与技术错误契约
    actionLexicons.ts                按语种的普通词典
    matchTextAction.ts              纯匹配函数
    ...                             仅增加实际采用的适配实现

  application/
    gameSession.ts                  主要沿用，不另建 Session
    gameContentView.ts              新步骤表现投影
    gameSessionView.ts              沿用 locale/事实隔离

  ui/
    presentation/story/
      StoryPlayer.tsx              接入检查点与输入步骤
      ActionInputPanel.tsx         新输入面板
      useActionInput.ts            局部轮次、取消、一次消费
    i18n/messages/
      zh-CN.ts / en-US.ts           仅补实际新增 UI 文案

  platform/
    recognition/                   按选型添加媒体/Worker/原生桥接

  storage/
    migrateSave.ts                 v2 → v3 检查点默认值
    sqliteSaveRepository.ts        版本读写条件同步
```

`storyProgress.ts` 只是复用辅助函数的建议位置；代码很少时可留在现有文件。不要因目录示例创建没有使用者的抽象接口或空服务。

### 17.2 核心纯函数边界

推荐把以下逻辑保持可独立测试：

| 函数职责 | 输入 | 输出 |
| --- | --- | --- |
| 文本匹配 | 文本、语种词典 | known / unknown |
| 定位下一输入 | 队首内容、检查点 | 输入步骤或没有未完成输入 |
| 应用游戏效果 | 当前数值/Flag、合法效果、属性定义 | 新值与实际变化 |
| 提交已知动作 | GameState、命令、规则内容 | TransitionResult |
| 生成内容表现 | 规则与语言包 | 只读输入说明，无 wrongEffects |

它们不需要成为单独 npm 包，也不依赖识别模型才能测试。

### 17.3 技术错误的归属

| 错误示例 | 所属层 | UI 行为 |
| --- | --- | --- |
| NO_MATCH / AMBIGUOUS | Matcher 内部原因，可统一 unknown | 提示再试，不处罚 |
| PERMISSION_DENIED / DEVICE_UNAVAILABLE | 输入适配器 | 解释原因，允许文字 |
| MODEL_LOAD_FAILED / TRANSCRIPTION_FAILED | 输入适配器 | 重试或切换方式 |
| STALE_STORY_INPUT / STORY_INPUT_REQUIRED | Core / Application | 刷新当前输入位置，不当作错动作处罚 |
| SAVE_FAILED / RELOAD_REQUIRED | 现有会话/存储 | 沿用保存失败或重载路径 |

代码命名可服从现有错误码风格，不能将所有失败统称为“动作错误”。UI 存 code，在渲染时翻译，不缓存已翻译的异步错误句子。

### 17.4 并行实施分工

**规则与存储线**先确定 ActionId、actionInput Schema、检查点和命令契约，完成纯状态迁移与恢复测试。公共契约变化应同时更新内存仓储、SQLite 和固定 fixtures。

**UI 与输入线**先接文字和 FakeRecognizer，不等待真实模型；验证统一提交流程、局部轮次和故障提示。

**识别考察线**独立验证语音、视觉样本及安装包能力，只交付适配器与评估记录，不修改案件规则。正式接入时保持输出契约不变。

依赖边界检查脚本应同步允许必要的 shared/input 纯类型依赖，不以禁用边界检查来接入 SDK。

---

## 18. 测试、联调与验收

### 18.1 最小固定联调样本

保留现有两个案件回归样本，增加一个不改变案件裁定的 Story：

```text
前置文字
  → 输入敬礼，错误 authority -2
  → 中间文字或视频
  → 再次输入敬礼，错误 authority -1
  → 尾部文字
```

两个输入点使用相同目标、不同 Step ID，用来证明“通过的是位置，不是全局动作”。另加两个不同 Story ID 的相同模板，用来验证周期位置相互独立。

### 18.2 纯逻辑测试矩阵

| 编号 | 场景 | 必须结果 |
| --- | --- | --- |
| G01 | 正确目标动作 | 检查点到下一步，案件进度不变 |
| G02 | 其他已知动作 | 按配置处罚，仍在当前输入 |
| G03 | 无错误效果 | 仍要求正确动作，无额外处罚 |
| G04 | 属性已到下限 | actualDelta 符合裁剪，不显示虚假扣分 |
| G05 | 非法动作 ID | 拒绝命令，不当作合法错动作 |
| G06 | 命令不是队首 Story | 拒绝 |
| G07 | 命令指向后面的第二个输入 | 拒绝，不能跳过第一个 |
| G08 | 重复提交已通过的输入 | 拒绝，不重复推进 |
| G09 | 有未通过输入时 completeStory | 返回 STORY_INPUT_REQUIRED |
| G10 | 最后一步是正确输入 | 同次提交完成 Story 并清空检查点 |
| G11 | 正确输入后还有普通尾部 | 从下一步恢复，尾部完成后才完成 Story |
| G12 | ended 状态输入 | 拒绝 |
| G13 | ending 阶段合法队首输入 | 可完成当前内容，不重新选择已锁定 endingId |
| G14 | 输入修改 Flag | 只改声明的 Flag，不调用案件结算 |

### 18.3 存储与恢复测试

| 编号 | 场景 | 必须结果 |
| --- | --- | --- |
| S01 | 正确后、尾部播片中退出 | 从输入之后恢复，不再要求已通过动作 |
| S02 | 错误扣分后退出 | 从当前输入恢复，扣分不重复执行 |
| S03 | unknown 后退出 | 允许重播上次检查点以来的普通内容 |
| S04 | 保存明确失败 | 旧数值和位置保持不变，UI 不表现为已通过 |
| S05 | 写入结果不确定 | needsReload，不自动重发旧输入 |
| S06 | 旧 revision 提交 | 冲突处理与当前会话一致 |
| S07 | 两个连续输入 | 第一处完成不会自动完成第二处 |
| S08 | v2 迁移 | 增加 null 检查点，历史事实与 contentRef 不变 |
| S09 | checkpoint 引用缺失步骤 | 报兼容/存档错误，不静默从头 |
| S10 | 完成 Story 后重载 | 无旧检查点残留 |
| S11 | 同模板不同 Story 位置 | 已完成前一次不屏蔽后一次 |
| S12 | 历史案件回看 | 不重新执行输入效果或改变旧裁定 |

### 18.4 输入控制测试

使用假的识别器主动制造重复回调、乱序、取消后回调和长时间等待：

| 编号 | 场景 | 必须结果 |
| --- | --- | --- |
| I01 | 同一文字事件触发两次处理 | 本轮只消费一次 |
| I02 | 同一语音多个 interim/final 片段 | 按一轮边界只提交一次最终结果 |
| I03 | 持续错误姿势几十帧 | 一轮最多一次处罚 |
| I04 | 相机短暂丢失后又看到相同错误姿势 | 不自动视为新动作连续处罚 |
| I05 | 保存期间还有识别结果 | 不排队处罚，不发新命令 |
| I06 | 切换 Profile 后旧回调返回 | 丢弃 |
| I07 | 切换输入方式后旧结果返回 | 丢弃 |
| I08 | 切换语种后旧转写返回 | 丢弃，不用新词典重判 |
| I09 | 切换语种时已开始保存 | 原 ActionId 提交不变，之后用新文案展示 |
| I10 | permission 请求一直挂起 | 可改用文字，不冻结整个应用 |
| I11 | 用户没有设备 | 技术提示，不处罚、不自动通过 |
| I12 | 中文输入法确认字符 | 不误提交未完成的文字 |

### 18.5 本地化与表现测试

同一份存档可在两种语种中继续当前输入。语言切换不改变属性、案件进度、checkpoint 或 revision。

内容 fallback 与输入语种选择分开；Portal/Overlay 的说明、错误、按钮与 aria 文案按当前 UI 语言更新。字典别名不用 t()，不会把英文别名同步翻译成中文后再匹配。

普通内容已前进到检查点之后时，语言切换不将 StoryPlayer 步骤重置。保存中的错误也不应因为文案重新生成而消失或重复提交。

### 18.6 识别质量应分别报告三种误差

| 指标 | 关注的问题 |
| --- | --- |
| 正确动作被拒识为 unknown | 玩家需要重试多少次，是否难以完成 |
| unknown 被识别成已知动作 | 是否产生无依据的通过或误罚 |
| 一个已知动作被识别成另一个 | 三分支判定是否可信 |

另外记录：每轮意外重复提交数、平均/高分位通过尝试次数、冷启动/热启动时延、内存与 CPU、语种与设备可用性。

阈值应在产品认可的目标设备与样本上确定。可用少量不同参与者做初始烟测，但不能以几个开发者样本宣称全体玩家的准确率。本文不编造已测性能数字。

### 18.7 安装包验收

在真实首发系统运行打包应用，覆盖干净安装、断网、模型未缓存、权限允许/拒绝、设备被占用、相机拔出、语音取消和保存失败。

确认没有意外 CDN/远程模型依赖，没有离开交互后继续采集，没有原始音视频进入存档/日志。开发浏览器实验和现有项目的自动化通过记录不替代这些新能力的验收。

---

## 19. 实施顺序与完成标准

### 19.1 第一阶段：纯文字闭环

实现 actionInput 内容模型、普通按语种词典、Matcher、submitStoryInput、公共效果函数、检查点、completeStory 保护与对应迁移。UI 仅接文字和假的媒体入口。

完成标准：通过正确/错误/unknown、同 Story 多输入、退出恢复、重复回调和双语切换测试。此时功能已是完整可玩的内容交互，而不是等待模型才能验证的架构骨架。

### 19.2 第二阶段：语音适配

先用固定短录音验证一个候选后端，再接实际麦克风。保持“同一个 Matcher、同一条命令”，不要在语音回调里复制处罚逻辑。

完成标准：模型/语种明确、未知可拒识、每轮一个结果、可取消、断网策略明确、真实安装包可用。未达到要求前保留文字默认路径。

### 19.3 第三阶段：视觉适配

用实际构图先验证敬礼和一个确定可区分的错误动作。候选规则可以调整，不修改 Core 的三分支语义。

完成标准：目标/已知错误/unknown 均经过样本验证，持续姿态不重复处罚，性能不会阻塞 UI，设备与资源失败可降级。

### 19.4 内容扩充与周期位置

在已有内容规则中加入需要的固定位置；周期次数有限时展开为唯一 Story ID。增加动作种类前先增加识别能力和样本，而不是先发布无法通过的内容。

### 19.5 首版停止条件

当文字闭环完整、选定的语音与视觉方案满足目标平台验收、状态恢复可靠，即可结束本次架构扩展。

不因实现过程中发现 SDK 还有更多能力，就顺带引入情绪识别、身份识别、复杂动作组合、自由对话、挑战任务线或通用剧本引擎。

---

## 20. 技术参考与优先考察项

### 20.1 给实施团队的考察顺序

| 顺序 | 方向 | 优先资料 | 考察目标 | 不应误读为 |
| --- | --- | --- | --- | --- |
| 1 | 视觉动作 | MediaPipe Pose Landmarker Web；必要时 Hand Landmarker Web | 能否用少量关键点定义本项目敬礼 | 现成敬礼分类器 |
| 2 | 本地语音 | whisper.cpp；模型说明；command 示例 | 短录音转写、语种、资源与未知输入 | 候选命令里强制选一个就满足三分支 |
| 3 | 语音替代 | sherpa-onnx ASR、模型与 WASM 文档 | 不同部署/模型路径是否更适合目标 PC | 应同时维护多个正式后端 |
| 4 | 纯词库优化 | sherpa-onnx KWS 与中英模型资料 | 关键词命中能否可靠替代完整转写 | 只检测目标就能识别其他错误动作 |
| 参考 | 简单手势原型 | MediaPipe Gesture Recognizer Web | 采集、类别输出、Worker 示例 | 张掌等于挥手或敬礼 |
| 备选 | 自定义手势训练 | Model Maker 教程 | 理解数据与 none 类流程 | 当前积极维护的默认训练管线 |
| 开发实验 | Web SpeechRecognition | MDN 文档与兼容表 | 指定环境的快速语音原型 | 正式离线跨 WebView 保障 |

这一顺序是基于本项目需求的工程建议，不是第三方性能排行榜。实际选型以相同输入样本和目标安装包实验为准。

### 20.2 考察不需要做成新研究项目

第一轮只需回答：目标输入是否能通过、另一个已知动作是否能区分、未知是否不会被强行分类、是否能稳定跑在目标打包应用里。

语音可以从文件转写开始，视觉可以从关键点可视化和录制片段开始；得到足够证据后才接实时设备。不要在业务闭环未完成前同时训练自定义模型、接云服务、做移动端与优化 GPU。

### 20.3 参考资料状态说明

参考资料核对日期为 2026-09-16。本文明确区分官方能力描述、项目接入建议和待验证性能；SDK、示例、模型权重与构建参数仍需由实施团队在锁定版本后重新确认。

尤其注意：自定义手势 Model Maker 教程的维护状态已变化；Web SpeechRecognition 的兼容和联网行为不能按普通网页演示推断；KWS 模型中的词典编码格式也不等于本项目普通字符串词典，应在适配/构建步骤生成，不要求内容作者手写音素或 token。

---

## 附录 A：完整交互内容片段与双语示例

以下是可合并到扩展后内容包中的**完整 Story 片段**，不是独立的全量 game.json。沿用原包的 manifest、案件、属性、资源和其他内容；正式新增包必须使用新的内容版本与 v3 内容 Schema。

假设原包已声明 `authority`，范围 0–100，示例开始时当前值为 50。两个输入点都要求敬礼，但代表不同内容位置。

### A.1 `game.json` 的 `stories` 条目

```json
{
  "inspection_003": {
    "skippable": false,
    "steps": [
      { "id": "arrival", "type": "text" },
      {
        "id": "salute_at_arrival",
        "type": "actionInput",
        "targetActionId": "salute",
        "wrongEffects": {
          "attributeDeltas": { "authority": -2 },
          "setFlags": {}
        }
      },
      { "id": "inspection_notice", "type": "text" },
      {
        "id": "salute_at_departure",
        "type": "actionInput",
        "targetActionId": "salute",
        "wrongEffects": {
          "attributeDeltas": { "authority": -1 },
          "setFlags": {}
        }
      },
      { "id": "departure", "type": "text" }
    ]
  }
}
```

### A.2 中文语言包的 `stories` 条目

```json
{
  "inspection_003": {
    "title": "例行检查",
    "steps": {
      "arrival": {
        "blocks": [
          { "type": "paragraph", "text": "监察官走进办公室，在你的桌前停下。" }
        ]
      },
      "salute_at_arrival": {
        "blocks": [
          { "type": "paragraph", "text": "请向监察官敬礼。你可以做出动作、说出“敬礼”，或输入文字。" }
        ]
      },
      "inspection_notice": {
        "blocks": [
          { "type": "paragraph", "text": "监察官检查了文书，提醒你继续遵守工作规程。" }
        ]
      },
      "salute_at_departure": {
        "blocks": [
          { "type": "paragraph", "text": "检查结束。请再次敬礼，送别监察官。" }
        ]
      },
      "departure": {
        "blocks": [
          { "type": "paragraph", "text": "监察官离开了办公室。你可以继续处理文书。" }
        ]
      }
    }
  }
}
```

### A.3 英文语言包的 `stories` 条目

```json
{
  "inspection_003": {
    "title": "Routine Inspection",
    "steps": {
      "arrival": {
        "blocks": [
          { "type": "paragraph", "text": "The inspector enters the office and stops in front of your desk." }
        ]
      },
      "salute_at_arrival": {
        "blocks": [
          { "type": "paragraph", "text": "Salute the inspector. You may perform the gesture, say 'salute', or type it." }
        ]
      },
      "inspection_notice": {
        "blocks": [
          { "type": "paragraph", "text": "The inspector reviews the documents and reminds you to follow office procedure." }
        ]
      },
      "salute_at_departure": {
        "blocks": [
          { "type": "paragraph", "text": "The inspection is complete. Salute once more as the inspector leaves." }
        ]
      },
      "departure": {
        "blocks": [
          { "type": "paragraph", "text": "The inspector leaves the office. You may return to your documents." }
        ]
      }
    }
  }
}
```

### A.4 触发规则条目

```json
{
  "id": "inspection_after_003",
  "when": {
    "all": [
      { "type": "resolvedCountAtLeast", "count": 3 }
    ]
  },
  "storyId": "inspection_003",
  "order": 300
}
```

这条规则合并进 `storyRules`，而不是由 UI 数“侧边栏里有几个案件”。独立调试 Story 时，也可在测试 fixture 的 `initial.storyIds` 中显式入队，不修改正式运行逻辑。

### A.5 提交与恢复样本

```json
{
  "type": "submitStoryInput",
  "storyId": "inspection_003",
  "stepId": "salute_at_arrival",
  "actionId": "wave"
}
```

这个命令是明确错误。保存后 authority = 48，检查点如下：

```json
{
  "storyId": "inspection_003",
  "resumeStepId": "salute_at_arrival"
}
```

接下来正确敬礼，检查点变成：

```json
{
  "storyId": "inspection_003",
  "resumeStepId": "inspection_notice"
}
```

完整预期：

| 操作 | authority | 保存的恢复位置 | 是否仍有待完成输入 |
| --- | --- | --- | --- |
| 进入 Story | 50 | null，起点 | 有，arrival 输入 |
| 第一处 unknown | 50 | 不变 | 有 |
| 第一处 wave | 48 | salute_at_arrival | 有 |
| 第一处 salute | 48 | inspection_notice | 有，departure 输入 |
| 此时退出再进入 | 48 | 从 inspection_notice 恢复 | 有，不能重新处罚 arrival |
| 第二处 wave | 47 | salute_at_departure | 有 |
| 第二处 salute | 47 | departure | 没有，但还需呈现尾部 |
| 完成尾部 | 47 | null；Story 加入 completed | 没有 |

表格中的 arrival/departure 输入分别指 `salute_at_arrival` 与 `salute_at_departure`，不是同名普通文字步骤。所有案件的节点和历史裁定在这些操作中保持不变。

---

## 附录 B：最小检查点算法参考

下面的纯函数只说明“如何定位未通过的输入”和“正确之后应保存到哪里”。它不是完整 transition，不替代正式的 Schema、phase、队首校验、effects、GameSession 或存储实现。

### B.1 步骤定位

```ts
export type CheckpointStep =
  | ActionInputStep
  | Readonly<{ id: string; type: "text" | "video" | "effect" }>;

export function getResumeIndex(
  storyId: string,
  steps: readonly CheckpointStep[],
  checkpoint: StoryCheckpoint | null,
): number {
  if (steps.length === 0) {
    throw new Error("STORY_EMPTY");
  }
  if (checkpoint === null) return 0;
  if (checkpoint.storyId !== storyId) {
    throw new Error("CHECKPOINT_STORY_MISMATCH");
  }
  const index = steps.findIndex((step) => step.id === checkpoint.resumeStepId);
  if (index < 0) {
    throw new Error("CHECKPOINT_STEP_MISSING");
  }
  return index;
}

export function getNextRequiredInput(
  storyId: string,
  steps: readonly CheckpointStep[],
  checkpoint: StoryCheckpoint | null,
): ActionInputStep | null {
  const start = getResumeIndex(storyId, steps, checkpoint);
  for (let index = start; index < steps.length; index += 1) {
    const step = steps[index];
    if (step.type === "actionInput") return step;
  }
  return null;
}
```

如果返回 null，表示从已提交恢复位置起没有未通过的输入。普通尾部内容仍由播放器完成后提交 completeStory。

### B.2 正确输入后的推进

```ts
export type InputContinuation =
  | Readonly<{ type: "checkpoint"; checkpoint: StoryCheckpoint }>
  | Readonly<{ type: "completeStory" }>;

export function continuationAfterCorrectInput(
  storyId: string,
  steps: readonly CheckpointStep[],
  checkpoint: StoryCheckpoint | null,
  inputStepId: string,
): InputContinuation {
  const required = getNextRequiredInput(storyId, steps, checkpoint);
  if (required === null || required.id !== inputStepId) {
    throw new Error("STALE_STORY_INPUT");
  }
  const index = steps.findIndex((step) => step.id === inputStepId);
  const next = steps[index + 1];
  if (next === undefined) return { type: "completeStory" };
  return {
    type: "checkpoint",
    checkpoint: { storyId, resumeStepId: next.id },
  };
}
```

正式处理器先校验队首和动作集合，再比较 required.targetActionId。只有相等时调用上述推进；否则应用 wrongEffects，并把 checkpoint 设到 required.id。这里返回的 completeStory 是内部推进结果，不是要求 UI 再发送第二条 GameCommand。

### B.3 处理器的业务顺序

```text
validate state / queue head / command IDs
       ↓
required = next input at or after committed checkpoint
       ↓
if no required or stepId differs → reject as stale/invalid
       ↓
if actionId == required.targetActionId:
    derive continuation
    update checkpoint, or finish current Story atomically
else:
    apply allowed wrongEffects
    checkpoint = current input
       ↓
validate nextState
       ↓
return nextState and optional transient feedback
```

辅助函数不判断视频是否已播放、文本是否已阅读。命令只允许越过下一处输入事实，不能跳到后续输入。现有 UI 负责正常播放顺序；这是内容播放器，不是防篡改系统。

### B.4 为什么无需全量步骤持久化

检查点只在 known 提交或 Story 完成时改变。纯演出本地步骤仍可前进；退出时最多重播最近检查点以来的无副作用内容。

已经通过的输入永远位于检查点之前，已施加的处罚已经包含在属性/Flag 里。因此恢复不需要回放命令日志，也不需要重新跑识别或计算历史效果。

---

## 附录 C：实施决策与禁止扩大的范围

### C.1 默认决策汇总

| 决策 | 采用方案 |
| --- | --- |
| 功能概念 | 内容输入交互点，命名 actionInput |
| 规则宿主 | 现有 GameStoryStep，不增加平行挑战单元 |
| 全局流程 | 复用 pendingStoryIds / completedStoryIds |
| 可重复内容 | 有限规律位置使用不同 Story ID，可复用动作与模板 |
| 输入输出 | known(ActionId) / unknown |
| 字典语言 | 普通配置按选定语种读取，不走 t() 或内容表现投影 |
| 成功效果 | 通过当前位置，无默认奖励 |
| 错误效果 | 可选属性/Flag，沿用公共效果函数 |
| unknown | 本地提示，不提交存档 |
| 恢复 | 单个 storyCheckpoint，不建 ChallengeProgress 表 |
| 防重 | 局部一次消费 + generation 取消 + 已有会话锁/步骤校验 |
| 宏观推进 | 默认不自动重排案件解锁与结局判断时点 |
| 语音 | 先考察本地转写 → Matcher，KWS 可替代但不必先接 |
| 视觉 | 先考察 Pose/必要的 Hand 关键点 + 简单规则 |
| 无设备 | 文字替代，不扣状态，不自动通过 |

### C.2 必须同时修改的契约

内容 Schema 与 Validator、GameState 与初始化/不变量、命令 Schema 与 transition、GameContentView 与 StoryPlayer、反馈 Schema 与 UI 消费、存档迁移与仓储版本条件，都要同步更新对应 fixtures 和测试。

不能只修改 TypeScript type 却忘记严格 Zod Schema；不能只修改 SQLite 迁移却忘记内存仓储；不能只修改 UI 继续按钮却遗漏 Core 完成保护。

### C.3 不得自行扩大

不得因为“技术上可以”而追加独立挑战任务、动作连招、限时失败、误识别惩罚、永久尝试日志、所有平台同时上线、自动云端回退或音视频长期记录。

任何把动作输入接入案件选项图、把识别 SDK 引入 Core、在动画结束回调里扣属性、将本地化字符串写入存档的实现，均违背本扩展边界。

### C.4 文档与实施状态

本文件描述建议的新增契约和实现规则，不意味着仓库已经具备 actionInput、识别模型或 v3 存档支持。

文档中的示例适合用于实施对齐与测试起点。实际 SDK API、模型资源、性能阈值、媒体权限和安装包行为由实施团队依据锁定版本验收，不能用文档示例的类型检查替代项目集成测试。

**本文件的独立自检：** 10 个 TypeScript 代码块在补入最小共享类型声明后通过 strict 类型检查；9 个 JSON 示例通过语法解析；附录 A 的中英步骤 ID 与规则片段一致；附录 B 的检查点辅助函数及示例状态序列通过 20 项独立测试。该自检没有执行仓库的正式 Validator、Game Core 集成测试、Tauri 构建或真实媒体设备测试，也没有测量识别准确率。

---

## 附录 D：代码依据及官方参考索引

### D.1 代码基线

以下代码链接固定到本次核对的提交，不随 main 后续变化。它们说明已有结构，不代表新增方案已经写入仓库。

| 编号 | 资料 | 用途 |
| --- | --- | --- |
| C1 | [content/schema.ts](https://github.com/Deckard-YYZ/Judge-of-Eagleland-official-app/blob/3c3776f23d2aafefcef9398bbc1852f351a00b96/src/content/schema.ts) | v2 规则/语言 Schema、StoryStep、效果与条件 |
| C2 | [game/commands.ts](https://github.com/Deckard-YYZ/Judge-of-Eagleland-official-app/blob/3c3776f23d2aafefcef9398bbc1852f351a00b96/src/game/commands.ts) | 现有命令、反馈、纯 transition 契约 |
| C3 | [application/gameSession.ts](https://github.com/Deckard-YYZ/Judge-of-Eagleland-official-app/blob/3c3776f23d2aafefcef9398bbc1852f351a00b96/src/application/gameSession.ts) | 统一提交、锁、保存后发布与不确定写入恢复 |
| C4 | [gameSessionView.ts](https://github.com/Deckard-YYZ/Judge-of-Eagleland-official-app/blob/3c3776f23d2aafefcef9398bbc1852f351a00b96/src/application/gameSessionView.ts)；[gameContentView.ts](https://github.com/Deckard-YYZ/Judge-of-Eagleland-official-app/blob/3c3776f23d2aafefcef9398bbc1852f351a00b96/src/application/gameContentView.ts) | locale 加载、只读表现投影与规则隔离 |
| C5 | [storage/sqliteSaveRepository.ts](https://github.com/Deckard-YYZ/Judge-of-Eagleland-official-app/blob/3c3776f23d2aafefcef9398bbc1852f351a00b96/src/storage/sqliteSaveRepository.ts) | 整局 JSON、归属与 revision 条件写入、版本条件 |
| C6 | [ui/presentation/story/StoryPlayer.tsx](https://github.com/Deckard-YYZ/Judge-of-Eagleland-official-app/blob/3c3776f23d2aafefcef9398bbc1852f351a00b96/src/ui/presentation/story/StoryPlayer.tsx) | 当前本地步骤与整段 Story 完成边界 |
| C7 | [game/resolution.ts](https://github.com/Deckard-YYZ/Judge-of-Eagleland-official-app/blob/3c3776f23d2aafefcef9398bbc1852f351a00b96/src/game/resolution.ts) | 属性效果、解锁、剧情与结局的现有顺序 |
| C8 | [Localization Implementation](https://github.com/Deckard-YYZ/Judge-of-Eagleland-official-app/blob/3c3776f23d2aafefcef9398bbc1852f351a00b96/Desktop_Case_Game_Localization_Implementation.md) | 已实施的显示/规则分离及事实存档 |
| C9 | [game/progression.ts](https://github.com/Deckard-YYZ/Judge-of-Eagleland-official-app/blob/3c3776f23d2aafefcef9398bbc1852f351a00b96/src/game/progression.ts) | Story 规则排序与按 ID 去重 |
| C10 | [game/model.ts](https://github.com/Deckard-YYZ/Judge-of-Eagleland-official-app/blob/3c3776f23d2aafefcef9398bbc1852f351a00b96/src/game/model.ts)；[storage/migrateSave.ts](https://github.com/Deckard-YYZ/Judge-of-Eagleland-official-app/blob/3c3776f23d2aafefcef9398bbc1852f351a00b96/src/storage/migrateSave.ts) | 当前存档 v2 与既有迁移边界 |

### D.2 官方技术参考

核对日期：**2026-09-16**。表中链接为实施团队的阅读入口；模型/SDK 选型仍要锁定具体版本并记录实验结果。

| 编号 | 官方资料 | 优先阅读内容 |
| --- | --- | --- |
| T1 | [MediaPipe Pose Landmarker — Web](https://developers.google.com/edge/mediapipe/solutions/vision/pose_landmarker/web_js)；[任务概述](https://developers.google.com/edge/mediapipe/solutions/vision/pose_landmarker) | 人体关键点输出、Web 视频接入；敬礼规则的优先底层参考 |
| T2 | [MediaPipe Hand Landmarker — Web](https://developers.google.com/edge/mediapipe/solutions/vision/hand_landmarker/web_js) | 需要细分手部姿态时的补充，而不是首版无条件同时加载 |
| T3 | [MediaPipe Gesture Recognizer — Overview](https://developers.google.com/edge/mediapipe/solutions/vision/gesture_recognizer) | 预置类别、输出；核对哪些动作并非现成功能 |
| T4 | [MediaPipe Gesture Recognizer — Web](https://developers.google.com/edge/mediapipe/solutions/vision/gesture_recognizer/web_js) | 摄像头示例、模型/WASM、同步推理与 Worker 提示 |
| T5 | [MediaPipe 手势模型自定义教程](https://developers.google.com/edge/mediapipe/solutions/customization/gesture_recognizer) | none 类与训练流程参考；页面已提示 Model Maker 不再积极维护 |
| T6 | [ggml-org/whisper.cpp](https://github.com/ggml-org/whisper.cpp) | 本地推理、基本转写、构建与可选部署路径 |
| T7 | [whisper.cpp 模型说明](https://github.com/ggml-org/whisper.cpp/blob/master/models/README.md) | 多语言与 .en 模型区别、模型选择与资源 |
| T8 | [whisper.cpp/examples/command](https://github.com/ggml-org/whisper.cpp/blob/master/examples/command/README.md) | 麦克风命令示例；特别核对 guided mode 与 unknown 要求 |
| T9 | [sherpa-onnx 官方文档](https://k2-fsa.github.io/sherpa/onnx/)；[官方仓库](https://github.com/k2-fsa/sherpa-onnx) | 本地语音能力与运行时接入 |
| T10 | [sherpa-onnx 预训练模型](https://k2-fsa.github.io/sherpa/onnx/pretrained_models/index.html) | 按任务和语种选择模型，不假设全部模型等价 |
| T11 | [sherpa-onnx WebAssembly](https://k2-fsa.github.io/sherpa/onnx/wasm/index.html) | Web/WASM 构建和中英示例入口 |
| T12 | [sherpa-onnx Keyword Spotting](https://k2-fsa.github.io/sherpa/onnx/kws/index.html) | 自定义关键词、token 转换、阈值与误触发权衡 |
| T13 | [KWS 预训练模型与中英示例](https://k2-fsa.github.io/sherpa/onnx/kws/pretrained_models/index.html) | 包含中英模型 `sherpa-onnx-kws-zipformer-zh-en-3M-2025-12-20` 的运行和关键词配置资料 |
| T14 | [MDN — SpeechRecognition](https://developer.mozilla.org/en-US/docs/Web/API/SpeechRecognition) | 浏览器兼容、服务端识别说明、语种和结果接口 |
| T15 | [MDN — getUserMedia](https://developer.mozilla.org/en-US/docs/Web/API/MediaDevices/getUserMedia) | 授权、安全上下文、设备错误与等待行为 |
| T16 | [Tauri — Webview Versions](https://v2.tauri.app/reference/webview-versions/) | 各目标系统 WebView 差异 |
| T17 | [Tauri — Embedding Additional Files](https://v2.tauri.app/develop/resources/) | 本地模型和必要资源的打包/解析基础 |
| T18 | [Tauri — Embedding External Binaries](https://v2.tauri.app/develop/sidecar/) | 原生识别 sidecar、目标架构、调用与发布配置 |

**实施落点：** 先让文字输入在现有内容、Core、存储和 UI 中闭环，再将语音与视觉作为同一输入契约的适配器接入。模型决定输入如何被识别，内容决定何时要求输入，Game Core 决定它对已提交游戏事实的影响。
